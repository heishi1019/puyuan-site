# AGENTS.md — 溥源科技官网项目 · 多 Agent 协作交接

> 本文件是项目的**单一事实来源(SSOT)**,供任何参与本项目的 AI agent(Claude / Codex / 其他)独立读懂后接手。
> 你**无法**看到此前的人机对话,所需背景全部在本文件与所引用的项目文件中。开工前请通读本文件 + `DESIGN.md` + `BRIEF.md`。
> 本文件与 `DESIGN.md` / `BRIEF.md` 冲突时,**以本文件为准**(后两者尚未随最新决策全量更新)。

---

## 0. 一句话项目定义

为 **杭州溥源智能科技有限公司(溥源科技 / PuYuan Tech)** —— 一家 AI-native 公司 —— 做 **品牌 logo + 产品文案 + 官网**,最终落到 **Next.js + Tailwind + Vercel**,并满足 **GEO(生成式引擎优化)**。

**站点形态(用户 2026-08-05 明确)**:**多页站点**,不是单页长滚动。
公司主页(讲公司)→ 页内产品矩阵区 → 可**跳转**到 **智小申** 和 **科小文** 两个 agent 的**独立产品页**。

---

## 1. 公司与产品事实(写文案/结构化数据的权威来源)

### 公司
- **全称**:杭州溥源智能科技有限公司
- **简称**:溥源科技 · **英文**:PuYuan Tech(字标可用 `PUYUAN`)
- **命名含义**:「溥源」= 广博之源(知识与创造之源)。视觉/文案围绕「源点扩散 · 一源驱动多 agent」。
- **实体命名务必全站统一**(GEO 权威信号):溥源科技 / 杭州溥源智能科技有限公司 / PuYuan Tech。
- **定位句(✅ 用户 2026-08-05 确认)**:「从知识源头，驱动专业 agent」（品牌 tagline，不枚举产品，可扩展）。元数据描述层：「溥源科技（PuYuan Tech）是一家 AI-native 公司，专注构建专业场景 agent，持续扩展中。」

### 产品矩阵(两款 agent,同源家族;**两者都要在主页矩阵中出现并各有独立页**)
| 产品 | 英文名 | 定位 | 内容深度 | PRD 状态 |
|---|---|---|---|---|
| **智小申** | **ProposalPilot Agent** | 企业政府项目申报 agent | **主推**,内容做深 | ✅ `_prd_zhishen.md`(v3.4) |
| **科小文** | **ScholarPilot Agent** | 学术写作 agent(选题/文献综述、初稿、润色、投稿) | **轻量占位**,不做深 | ❌ 无 PRD |

> ✅ **已定**:对外品牌名统一「**智小申**」(不用「智申」);英文名 **ProposalPilot Agent**(含 `Agent` 后缀,用户 2026-08-06 确认)。
> ✅ **已定**:**科小文暂缓** = 不投入文案/视觉深度,但**仍出现在主页产品矩阵中且有可跳转的独立页**。
> 其页面按占位处理:一句 slogan + 一段介绍 + 3 个能力点(取自 `BRIEF.md` 任务 2)+ 「即将上线 / 内测申请」CTA。**不要**为它编造具体数据、客户案例或功能细节——无 PRD 支撑。

### 智小申 产品内核(取自 `_prd_zhishen.md`,主页矩阵卡片 + 产品页 + FAQ 的素材)
- **定义(GEO 友好,可被 AI 引擎直接摘引)**:Agent-native 企业项目申报协作平台,自主完成「发现机会 → 诊断资格 → 编制材料 → 质检定稿 → 申报提醒与立项陪伴」,让中小企业拥有大企业级政府事务能力。
- **护城河 = 证据核查引擎**:不判断技术是否先进,而是跨 200 页材料核查证据的一致/完整/可信(6 大类 14 项),发现互相矛盾的数字、断裂的证据链——通用大模型做不到跨文件一致性追踪。
- **诚实能力边界**:硬门槛类(高企/研发费加计)给强判断;竞争性评审类只提升竞争力,**不承诺"包过"**。
- **数据主权**:私有化(数据不出内网)/ 云端(签约不保存、不训练、加密)。
- **真实性红线**:每个亮点/结论须映射企业真实证据,无据标红不写入,逐句可溯源。
- **系统主驱、人做决策**:6 道人工门控 G1–G6,G4「材料定稿」不可省。
- **目标用户**:中小企业主(90%+ 无专职申报人)、行政/财务兼职申报人、大企业政府事务专员、第三方申报代理。
- **申报体系背景(可作数据点)**:覆盖科技、发改、工信、人才、市场监管、商务六大主管部门,国家级/省部级/地市级三个层级,项目类型超 150 项。
- 智小申自身技术栈(仅供理解产品,**非本官网技术栈**):Vite+React+MUI+Tailwind / Python FastAPI / LangGraph;2 个真 Agent(申报总管、撰稿)。

> 注:`_prd_zhishen.md` 内部标题为「智申」,**对外一律写「智小申」**;引用 PRD 时按此对齐,勿把「智申」带进官网文案。
> 注:智小申做的是**政府项目申报**(高企认定、研发费加计、专项资金),**不是**国自然/课题/基金等学术申报。`DESIGN.md`/`BRIEF.md` 旧稿把它写成「国自然/课题申报书」,**是错的**,勿沿用。

---

## 2. 目标受众(按页分层,不要混着写)

多页结构让受众可以按页拆开,不必在同一屏里同时讨好所有人:

- **公司主页(`/`)**:伞级视角。读者可能是企业决策者、也可能是科研人员,首屏只需让人一眼看懂「溥源是谁、有哪两个 agent、各解决什么」,然后靠矩阵分流到子页。语气务实可信,不预设单一身份。
- **智小申页(`/proposalpilot`)**:面向**企业** —— 中小企业主、行政/财务兼职申报人、大企业政府事务专员、第三方申报代理。强调政策匹配、证据核查、数据主权、诚实边界(不承诺包过)。
- **科小文页(`/scholarpilot`)**:面向**科研个人**(研究生/博士/青年教师)与**科研机构/实验室**。个人侧语气亲和、强调省时省力与按需订阅;机构侧强调私有化部署、数据安全、批量席位、合规。因无 PRD,内容保持轻量。

---

## 3. 品牌契约(硬约束,详见 `DESIGN.md`)

**视觉:极客科技 · 克制**
- 色板:`--bg #0A0A0B` / `--surface #141416` / `--border #26262A` / `--text #F5F5F7` / `--text-muted #8A8A93` / `--accent #00E5A0`(荧光青绿,只点睛) / `--accent-dim #00B37E`。
- 字体:标题几何无衬线(Inter/Geist/HarmonyOS Sans);数据/标签/代码用等宽(JetBrains Mono/Geist Mono)。
- 元素语言:源点 + 涟漪环 + 卫星节点;细描边、微光晕、等宽标签、终端质感点缀。大量留白。
- 两个 agent 是同源兄弟产品,**视觉需家族一致**(同一 logo 语言、同色系),可用不同强调色微区分。
- **禁忌**:彩色渐变堆砌、拟物阴影、emoji 当装饰、廉价 3D 机器人插画。

**文案语气**
- 中文为主,专业不端着。**禁用**「赋能 / 颠覆 / 革命性 AI」等空话。
- 说具体的事 + 给数字/流程/边界。可信 > 炫技。诚实说明 AI 能做和不能做。

---

## 4. GEO(生成式引擎优化)—— 从一开始并入,不后补
目标是被 ChatGPT/Perplexity/豆包/元宝等**原样引用**,而非传统 SEO 排名。落地要求:
- **结构化事实**:公司/产品/能力用「一句话可被摘出」的定义句(定义+数据+主体明确),非营销长句。
- **机器可读层**:Schema.org —— 主页 `Organization`,每个产品页 `SoftwareApplication`,FAQ 用 `FAQPage`;干净语义 HTML;根部放 `llms.txt`。
- **多页对 GEO 是加分**:每个 agent 独立 URL + 独立 `SoftwareApplication` 实体,比挤在单页里更容易被当作独立产品识别和引用。
- **FAQ/问答块**:围绕真实提问建 Q&A(主页 + 各产品页各自一组)。
- **可被抓取**:SSR/静态输出(Next.js 合适)、清晰 sitemap 含全部三页,内容不锁在 JS 里。
- **权威信号**:全站实体命名统一、可溯源表述、页间互链(主页↔产品页双向)。

---

## 5. 站点结构(**已按用户要求改为多页**,取代 `DESIGN.md` 第 7 节的单页方案)

### 页面 1 · 公司主页 `/`
1. **Hero**:公司一句话定位 + 主 CTA(免费试用)+ 次 CTA(预约 demo)
2. **产品矩阵**:两款 agent 并排卡片 —— 各含中英文名、一句话定位、3 个能力点、**「了解详情 →」跳转到各自产品页**。智小申卡片为主视觉重心,科小文卡片可标「即将上线」。
3. **工作流展示**:申报 / 写作的 before-after 或步骤流
4. **为什么选溥源**:懂场景 / 全流程而非工具箱 / 数据安全可私有化
5. **FAQ**(GEO 用,公司与产品通识问题)
6. **底部**:再 CTA + 联系方式 + 公司全称「杭州溥源智能科技有限公司」+ 备案位

### 页面 2 · 智小申 `/proposalpilot`(内容做深)
Hero(产品定位句)→ 解决什么问题(不知道能报什么 / 材料证据打架)→ 五步工作流(发现机会 → 诊断资格 → 编制材料 → 质检定稿 → 申报提醒与立项陪伴)→ **证据核查引擎**(护城河,重点展开)→ 能力边界与真实性红线(诚实说明,不承诺包过)→ 数据主权(私有化 / 云端)→ 面向个人企业 vs 机构/代理 → FAQ → CTA → 返回主页链接

### 页面 3 · 科小文 `/scholarpilot`(轻量占位)
Hero(slogan)→ 一段介绍 → 3 个能力点(选题/文献综述、初稿生成、润色投稿)→ 「即将上线 / 内测申请」CTA → 返回主页链接

> 路由用英文产品名(`/proposalpilot`、`/scholarpilot`)—— URL 干净、对 GEO 与国际化都友好。此为可改的次要决定,如需拼音路由(`/zhixiaoshen`)告知即可。

---

## 6. Agent 分工(用户 2026-08-05 明确)

### 责任边界

| 负责方 | 负责页面 / 产物 |
|---|---|
| **Claude** | 公司主页 `/`、科小文占位页 `/scholarpilot`、**共享基础层**(Next.js 项目骨架、Tailwind 配置、Nav/Footer 组件、设计令牌 CSS 变量) |
| **Codex** | 智小申产品页 `/proposalpilot`(内容深度页,基于 `_prd_zhishen.md` v3.4) |

> 注:用户原话为「你完成官网部分，codex完成智小申子页面」—— 这里「官网部分」= 主页 + 科小文占位 + 共享层,「智小申子页面」= `/proposalpilot` 全页。

---

### 执行顺序(有硬依赖)

```
Step 1 · Claude  先交付 → 共享基础层
         Next.js 多页骨架、tailwind.config.ts(含设计令牌)、
         组件:Nav、Footer、Button、Card
         + 公司主页 / + 科小文占位页 /scholarpilot

Step 2 · Codex   再开始 → 读本文件 + _prd_zhishen.md
         在已有骨架上实现 /proposalpilot
         不要自建 tailwind config 或重写 Nav/Footer
```

**Codex 必须等 Step 1 完成后再动手**,否则设计令牌和共享组件尚不存在。

### 交付状态(2026-08-05)
| 产物 | 状态 |
|---|---|
| 共享基础层(骨架/令牌/Nav/Footer/Button/Card/`lib/schema.ts`) | ✅ 已交付 |
| 公司主页 `/` | ✅ 已交付 |
| 科小文占位页 `/scholarpilot` | ✅ 已交付 |
| GEO 技术层 `app/sitemap.ts` + `app/robots.ts` | ✅ 已交付(robots 不屏蔽 AI 抓取,含 sitemap 指向) |
| 智小申页 `/proposalpilot`(Codex) | ✅ 已交付并验收 —— 未越界改共享层;`auditTypes` 编造问题已修(见 §7.1 事实性红线) |

全站 build:8 条静态路由(`/`、`/proposalpilot`、`/scholarpilot`、`/robots.txt`、`/sitemap.xml`、`/_not-found`),0 TypeScript 错误。

---

### Codex 接口契约(Claude 交付时保证,Codex 直接使用)

| 项目 | 路径 / 用法 |
|---|---|
| Tailwind 设计令牌 | `tailwind.config.ts` 内 `colors.accent`、`colors.bg`、`colors.surface` 等,与 `DESIGN.md` §5 一一对应 |
| 全局 CSS 变量 | `app/globals.css` 中 `--bg`、`--accent` 等(同 design-system.tokens.md) |
| 共享组件 | `components/Nav.tsx`、`components/Footer.tsx`、`components/Button.tsx`、`components/Card.tsx` |
| 页面布局 | `app/layout.tsx`(含 Nav + Footer,Codex 的页面自动套用) |
| Schema.org 工具函数 | `lib/schema.ts` —— 导出 `orgSchema`(Organization),Codex 页面只需导出 `softwareAppSchema('智小申 ProposalPilot Agent', ...)` |

---

### Codex 任务说明(智小申页 `/proposalpilot`)

> 读完本文件再读 `_prd_zhishen.md`(v3.4),产品事实全部从 PRD 里提取,**不要编造**。

页面结构(按 §5 页面 2 展开):
1. Hero:产品中英文名 + 一句话定位 + 主 CTA(免费试用)+ 次 CTA(预约 demo)
2. 解决什么问题:不知道能报什么 / 材料证据打架
3. 五步工作流:发现机会 → 诊断资格 → 编制材料 → 质检定稿 → 申报提醒与立项陪伴
4. 证据核查引擎(护城河,重点展开):6 大类 14 项,跨 200 页材料核查
5. 能力边界与真实性红线:硬门槛强判断 / 竞争性评审不承诺包过
6. 数据主权:私有化 vs 云端两种方案
7. 面向中小企业 vs 大企业/代理 双栏
8. FAQ(GEO 用,围绕企业申报真实问题,见 §4)
9. CTA + 返回主页链接

技术要求:
- 在 `app/proposalpilot/page.tsx` 下新建,**不要**修改 `app/layout.tsx` 或共享组件
- 页面 `<head>` 用 Next.js `metadata` 导出,包含 `title`、`description`
- `Schema.org` 用 `SoftwareApplication` + `FAQPage` JSON-LD,调用 `lib/schema.ts` 里已有工具
- 文案语气遵守 §3 品牌契约:禁用「赋能/颠覆/革命性 AI」,说具体的事,给数字/流程/边界

---

### 视觉设计来源
设计令牌已通过 open-design 生成(`design-system.tokens.md` 已成功喂入,产出 24 个文件含 `variables.dark.css`)。编码时直接从 `tailwind.config.ts` 和 `globals.css` 取值即可,**不需要再打开 open-design**。logo 草稿在 `assets/logo-mark.svg` / `assets/logo-full.svg`,可作参考。

---

## 7. 当前仓库产物(工作目录 `E:\Claude\puyuan-site`)
| 文件 | 作用 | 权威性 |
|---|---|---|
| `AGENTS.md`(本文件) | 多 agent 交接 SSOT | ⭐ **先读这个,冲突时以此为准** |
| `DESIGN.md` | 品牌契约(视觉/语气) | ⭐ 视觉与语气硬约束。**2026-08-06 已修**:§1 定位句、§2 智小申=政府项目申报(不是国自然/课题)、§3 受众按页分层、§7 改多页 |
| `BRIEF.md` | 喂给 open-design 的分任务 prompt | ⭐ **2026-08-06 已重写**。站点代码已落地,故其范围收窄为 logo 定稿 / favicon / OG 卡三项品牌资产,**不再包含生成官网页面**(会与现有代码冲突) |
| `design-system.tokens.md` | YAML 设计令牌 | ⭐ 令牌源。注:其 `scale` 封顶 `3xl:64`,已按 §6「技术约定」重映射到 Tailwind 槽位 |
| `_prd_zhishen.md` | 智小申完整 PRD v3.4 | ⭐ 产品事实源 |
| `assets/logo-mark.svg`、`assets/logo-full.svg` | logo 手写草稿 | 仅作 open-design **输入参考**,非最终稿 |

---

## 7.1 技术层约定与已知陷阱(改代码前必读)

### ⚠️ 陷阱一:Tailwind v4 不自动加载 `tailwind.config.ts`
本项目用 Tailwind **v4** + `@tailwindcss/postcss`。v4 已改为 CSS-first,**不再隐式读取 JS config**。
`app/globals.css` 顶部必须保留:
```css
@import "tailwindcss";
@config "../tailwind.config.ts";
```
**删掉 `@config` 那行,全部设计令牌 utility 会静默消失**(`bg-bg`/`text-accent`/`border-border`/`py-section`/`rounded-pill`/`shadow-glow` 等全部不生成),但 **build 依然成功、不报任何错** —— 页面只是靠 `globals.css` 里 `:root` 变量兜住 body 底色,看起来"有点像对的",实际全站失去令牌。
此 bug 曾真实发生并被修复。**验证方法**(不要只看 build 是否通过):
```bash
grep -c 'text-accent' .next/static/css/*.css   # 必须 ≥1,为 0 即已失效
```

### 字号阶梯:令牌槽位已重映射,勿按令牌文件字面命名使用
`design-system.tokens.md` 的 scale 封顶于 `3xl: 64`(那是 **hero 展示字号**),但代码 h1 用 `text-4xl sm:text-5xl`。若把令牌名直接对齐 class 名,会出现 **h2(64px)比 h1(36px)大**的倒挂。
因此 `tailwind.config.ts` 已把令牌值**上移两档**落到实际使用的槽位,令牌值一个不丢:

| class | 解析值 | 对应令牌 | 用途 |
|---|---|---|---|
| `text-lg` | 18px | (补位) | lead 段落 |
| `text-xl` | 20px | 令牌 `lg: 20` | h3 |
| `text-2xl` | 24px | (补位) | h3 大号 / h2 小号 |
| `text-3xl` | 28px | 令牌 `xl: 28` | **h2 标准** |
| `text-4xl` | 40px | 令牌 `2xl: 40` | h1 移动端 |
| `text-5xl` | 64px | 令牌 `3xl: 64` | **h1 桌面端(hero)** |

改字号前先看这张表。**h1 = `text-4xl sm:text-5xl`,h2 = `text-3xl`,h3 = `text-xl`/`text-2xl`** 为全站基线,勿单页破例。

### 事实性红线(适用于所有产品页文案)
产品页任何**看起来权威的数据面板/分类清单**,必须逐项能在 `_prd_zhishen.md` 里搜到原文,**不允许为凑数量或凑排版而改写、简化、增删分类名**。
已发生案例:`/proposalpilot` 的证据核查 6 大类曾被写成「事实一致/数值一致/时间逻辑/主体归属/证据完整/论点支撑」,其中「时间逻辑」「主体归属」PRD 中不存在,且丢掉了真实存在的「量化表述密度」「检测机构可信度」。已按 PRD §590 修正为:
**数值一致性 / 证据-主张映射 / 证据链闭环 / 跨维度逻辑一致性 / 量化表述密度 / 检测机构可信度**。

### 7.2 变更记录(跨 Agent 交接必读)

所有影响页面、视觉、共享组件、构建流程或 GEO 的改动，都要追加到仓库根目录的 [`CHANGELOG.md`](CHANGELOG.md)。交接时先读本文件最新条目，再读相关源码；不要仅凭 git diff 推断设计意图。每条记录必须包含改动内容、改动原因和验证结果。

---

## 8. 决策记录 & 未决事项

### 渐进式重构约定（2026-08-06）
- 用户反馈当前官网未通过内部评审，后续进入渐进式视觉与内容重构，不进行一次性全站推翻。
- 用户将分批提供具体参考网页；每次只拆解并吸收参考中的版式、配色、字体和动效规律，再结合溥源品牌与内容落地，不直接复制参考站。
- `https://www.lapa.ninja/` 仅作为高级网页案例库，不能在没有具体参考页面时随机选取风格并改造全站。
- 内容可以参考此前网站及用户补充资料，但公司/产品事实仍按本文件、`_prd_zhishen.md` 和已确认决策的优先级执行，冲突内容不得直接上线。
- 在用户逐项确认前，保持现有 `/`、`/proposalpilot`、`/scholarpilot` 路由、GEO 结构、Schema 和已确认 Logo 稳定；每次改动必须记录参考来源、借鉴点、涉及页面和验证结果。
- 交互提示文案约定（2026-08-10）：不要添加“移入查看”“点击展开”“向下滚动”等用于解释悬停、点击或动效的小字号操作提示。交互应依靠布局、状态变化和视觉反馈被理解；只有无法通过界面本身表达且影响任务完成时，才考虑必要说明。

### 补充资料登记（2026-08-06）
用户提供了外部文档 `D:/应用/xwechat_files/wxid_ou495i7pfewy12_78cb/msg/file/2026-08/溥源智能_公司官网介绍.md`，作为公司介绍与长期方向的补充信息。

可吸收的共识信息：
- 溥源智能专注 AI 智能体应用研发与产品化，从真实场景和用户痛点出发。
- 核心方法包括大语言模型、专业知识库、智能工作流、多智能体协作和工具执行，让智能体理解任务、参与流程并交付成果。
- 产品理念可概括为场景驱动、痛点导向、专业可靠、任务执行、人机协同、持续进化。
- 长期方向是围绕高信息量、复杂多步骤、高重复性、专业门槛和长期知识积累的工作，扩展多领域 agent 产品矩阵。

与本文件及 PRD 不一致的内容，暂不用于官网事实文案：
- 公司的已确认对外定位句仍是「从知识源头，驱动专业 agent」；资料中的「让智能体真正解决问题」只能作为候选辅助文案，不能替换定位句。
- 科小文仍按“轻量占位页”处理，只使用已确认的选题/文献综述、初稿生成、润色与投稿准备三项能力；资料中的科研数据分析、基金申请等扩展能力未经确认，不写入官网。
- 智小申官网仍聚焦企业政府项目申报，产品事实以 `_prd_zhishen.md` 为准；资料中的科研项目/基金申请描述不作为智小申页面依据。
- 「Puyuan AI」「PYAI」及资料中的英文口号尚未获得品牌决策确认，不替换现有实体命名、Schema 或 metadata。

**已定(2026-08-05,勿再动摇)**
- 对外品牌名统一「**智小申**」(不用「智申」)。
- 智小申英文名 = **ProposalPilot Agent**(与科小文 **ScholarPilot Agent** 构成 `*Pilot Agent` 家族命名)。
  - `Agent` 是**名称的一部分**,不是品类描述词 —— 出现在字标、页面标题、Schema.org `name`、Footer、产品卡。
  - **路由不含后缀**:`/proposalpilot`、`/scholarpilot` 保持不变(URL 干净,且改动会破坏 sitemap/canonical/已有外链)。
  - 页面标题避免叠词:写「智小申 ProposalPilot Agent · 企业政府项目申报」,**不要**写「... Agent | ... Agent」。
- **科小文暂缓**:内容轻量占位、不做深;但**保留在主页产品矩阵并有独立页**。
- **站点为多页结构**:公司主页 → 产品矩阵 → 两个 agent 独立页(取代原单页长滚动方案)。
- **Agent 分工**:Claude 负责主页 `/` + 科小文占位页 + 共享基础层;Codex 负责智小申页 `/proposalpilot`。Claude 先交付共享层,Codex 再开始。

**已定(追加,2026-08-05)**
- **公司伞级定位句** = 「**从知识源头，驱动专业 agent**」。可见层(Hero/h1)只用这句,**不枚举产品**,新 agent 上线不必改动;产品枚举只放在机器可读层(`metadata.description` / `orgSchema` / `llms.txt`)。
- 由此推出的**文案硬规则**:全站可见文案**不得锁定 agent 数量**(「两款/两个 agent」一律禁用,改「agent 家族」等不锁数量表述)。局部对比两个具名产品时用「两者」可以。

**仍未决(需向用户确认,勿自行拍板)**
1. ✅ **英文名 `Agent` 后缀** — 已确认(2026-08-06):**带后缀**。智小申 = `ProposalPilot Agent`,科小文 = `ScholarPilot Agent`。
   - ⚠️ 用户仅明确确认了智小申。**科小文的后缀是 Claude 按家族一致性推断的** —— 两者在 Footer 和主页产品卡里并排出现,一个带一个不带会是可见的品牌缺陷。若科小文不应带后缀,改 `app/scholarpilot/page.tsx`、`app/page.tsx`(`nameEn` + 2 条 FAQ)、`components/Footer.tsx`、`lib/schema.ts`、`public/llms.txt`。
2. ✅ **已完成(2026-08-06)**:`BRIEF.md` 已按本文件重写。旧稿三处错误(科小文排在前、智小申写成「国自然/课题申报书」、单页方案)已全部修正。**且重新定界了范围**——站点页面已用代码落地,不再需要 open-design 出页面原型;`BRIEF.md` 现在只覆盖**代码无法产出的视觉资产**(logo 定稿 / favicon / OG 卡)。
   > ⚠️ 其中一项需用户决策:**logo 卫星节点数量**。现草稿只有 1 个卫星,但品牌故事是「一源驱动**多** agent」、tagline 也已改为可扩展写法,单节点与之矛盾。见 `BRIEF.md` 任务 1。
3. 用户此前明确表示「还有更多信息未使用、多个要求」——**可能仍有需求未给全**,动工前确认是否已齐。
4. 未部署:尚未接 Vercel,`SITE_URL` = `https://www.puyuan.tech` 为预设域名,域名若变需同步改 `lib/schema.ts`(sitemap/robots/schema 全部取自该常量)。
5. 缺 favicon 与 OG 图:`app/layout.tsx` 的 `openGraph` 目前无 `images`,分享卡片无预览图。

---

## 9. 交付目标
open-design 出高保真原型/设计系统(三页)→ 编码 agent 落 **Next.js + Tailwind** 多页站点(含 GEO 技术层)→ 部署 **Vercel**。所有产物可导出为真实文件,便于跨 agent 接续。
