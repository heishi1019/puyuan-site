# 溥源科技官网变更记录

这份日志记录影响页面结构、视觉设计、共享组件、构建流程和 GEO 的重要改动。每次 agent 接手前先读最新条目；完成工作后追加一条，保留原因、涉及文件和验证结果。

## 2026-08-06 · Codex

### 导航栏独立层级

- 全站导航由贴边横条改为悬浮设备条：距离视口边缘 12px，增加完整细边框、独立 surface 背景、背景模糊、内高光与下方阴影。
- 根据用户反馈将导航背景调整为 40% 半透明磨砂层，使用 24px 背景模糊并轻微提升亮度和饱和度，使滚动内容能够隐约透出而非被完全遮挡。
- 导航链接增加局部底色 hover 与键盘 focus 状态，主 CTA 增加轻微抬升反馈。
- `app/layout.tsx` 的页面顶部占位由 64px 调整为 80px，避免悬浮导航遮挡 Hero。

### 首页源点鼠标吸附光点

- 首页 Hero 的 `SourceField` 增加指针吸附光点：光点位于鼠标与中央“源”之间，并随鼠标靠近源点而提高吸附比例、亮度和尺寸。
- 中央源点光晕与缩放同步响应接近程度，鼠标离开后恢复默认状态。
- 实现使用 DOM 事件与 CSS 变量，不使用 React state 高频重渲染；触屏和 `prefers-reduced-motion` 环境自动关闭。

### 智小申 Hero 次按钮

- `/proposalpilot` Hero 次按钮由“预约 Demo”改为“看看能做什么”。
- 点击后平滑跳转到五步工作流区域，目标锚点为 `#capabilities`。

### 科小文应用场景矩阵

- 按用户提供的科研产品参考图，为 `/scholarpilot` 增加“应用场景”卡片矩阵，并适配溥源既有暗色主题、青绿色强调和圆角规范。
- 根据用户提供的 6.3 秒交互参考视频补全卡片 hover：当前卡片整块切换为 accent 高亮，标题和正文反色，图标轻微旋转放大，并浮出三条倾斜信息标签；移开后恢复深色默认态。
- 卡片只呈现已确认的三项能力：选题与文献综述、初稿生成、润色与投稿准备；未照搬参考图中的数据分析、PPT、审稿回复等未经产品资料确认的功能。
- 新增统一 Phosphor 图标、鼠标 hover、键盘 focus 和底部指示线反馈；新增 `#applications` 锚点。
- 新增组件 `components/ScholarCapabilityGrid.tsx`，新增依赖 `@phosphor-icons/react`。
- 使用 `design-taste-frontend` 技能完成设计审计，选择 `DESIGN_VARIANCE 5 / MOTION_INTENSITY 4 / VISUAL_DENSITY 4`。
- `npm.cmd run check` 通过，Next 静态构建和设计令牌检查 `9/9 passed`。

### 首页交互动效

- “为什么选溥源”四项增加 hover / keyboard focus 反馈：轻微上浮、顶线和标题高亮、编号位移、底部指示线展开；移动端保持稳定静态布局。
- Hero“源点场”增强扫描线、连接束流动、涟漪加速和节点高亮，鼠标移入时进入更活跃状态。
- 动效继续遵守 `prefers-reduced-motion`，系统设置减少动态效果时回退为静态展示。
- 涉及文件：`app/page.tsx`、`app/globals.css`。

### 视觉与页面

- 继续迭代公司主页 `/`、智小申 `/proposalpilot`、科小文 `/scholarpilot` 的深色极客视觉：增加源点/节点语言、细描边、等宽标签和更明确的 section 层级。
- 保留既有产品事实、Schema.org、canonical、FAQ 和共享 `Nav` / `Footer` / `Button` / `Card` 契约。
- 智小申页面重点呈现五步工作流、跨文件证据核查、6 大类审计项、能力边界、数据主权和人工门控。

### 构建缓存修复

- 根因：Next 15 的 Turbopack 开发产物和生产构建共用 `.next`，重叠运行时可能互相覆盖，导致页面采集时报 `Cannot find module ../chunks/ssr/[turbopack]_runtime.js`。
- `next.config.ts`：开发环境使用 `.next-dev`，生产环境使用 `.next`。
- `scripts/clean-next.mjs`：构建前只清理生产目录 `.next`；开发目录 `.next-dev` 保持运行，避免验证命令中断本地预览。
- `package.json`：`build` / `check` 接入缓存清理脚本。
- `.gitignore`：忽略 `.next-dev/`。

### 验证

- `npm.cmd run check` 通过。
- Next 静态构建通过，页面路由和 GEO 路由均生成。
- 设计令牌检查 `9/9 passed`。

## 记录规范

## 2026-08-06 · Codex

### Logo 概念预览
- 根据用户对现有 Logo 的反馈，新增 4 个不替换现有品牌资产的 SVG 预览：开放源点、放射分支、模块化 P、模块化波形，文件位于 `assets/logo-concepts/`。
- 四个方向统一使用近黑底、`#00E5A0` 单色标记和 `PUYUAN` 字标，便于直接比较品牌语言与小尺寸辨识度。
- 当前仅用于方向评审，未修改 `assets/logo-mark.svg`、`assets/logo-full.svg` 或页面引用。
- 同目录新增 4 张 `720x520` PNG 预览，便于直接查看和并排比较；仍未接入站点。
- 验证：逐张检查 SVG 结构、PNG 渲染、文字与颜色；等待用户选择方向后再制作正式 SVG/favicon。

### Logo A 方向微调
- 在用户偏好“开放源点”方向的基础上新增 3 个候选：`a1-aperture`（紧凑光圈）、`a2-propagation`（受控扩散）、`a3-source-p`（源点 P 字母标）。
- 每个候选同时保留 SVG 与 `720x520` PNG，均位于 `assets/logo-concepts/`，没有替换现有品牌资产或页面 Logo。
- 视觉检查结论：A1 的品牌叙事与小尺寸完整性最均衡；A2 存在无线通信图标联想；A3 字母识别强但“多 agent”表达较弱。

### A1 第二轮微调
- 按用户选择继续深化 A1，新增 `a1-r1-ordered`（节点渐进）、`a1-r2-layered`（双弧层次）、`a1-r3-offset`（偏心源点）三个候选，均提供 SVG 与 PNG。
- 视觉检查结论：R1 最克制清晰；R2 的内外弧更有独特性且仍保留源点叙事；R3 的连接线容易产生“眼睛”联想。
- 当前推荐继续以 R2 为主方向，等待用户确认后再制作正式 Logo、favicon 并接入页面。

### R2 节点间距调整
- 按用户选择保留 R2 双弧结构，新增 `a1-r2-spread` 候选。
- 上下节点分别向外拉开，中间节点进一步外移，形成更舒展的扇形分布；环体、中心源点和字标比例保持不变。
- 当前仍为方向预览，未替换网站 Logo。

### R2 节点尺寸微调
- 新增 `a1-r2-spread-sized` 预览，保持 R2 散开版结构不变，将三个外部节点调整为 `7 / 11 / 9px`。
- 通过轻微尺寸差异增加节奏和视觉重心，同时避免产生明显的产品等级暗示。

### 新 Logo 正式接入
- 用户确认采用 `a1-r2-spread-sized`：开放源点、双弧结构与 `7 / 11 / 9px` 渐进节点。
- 更新 `assets/logo-mark.svg`、`assets/logo-full.svg`、`public/logo-mark.svg`、`public/logo-full.svg`，并制作适配 16px 小尺寸的 `app/icon.svg`。
- 导航和 Schema.org 继续沿用原有 `/logo-mark.svg` 地址，因此无需修改共享组件或结构化数据引用。

### 公司介绍补充资料登记
- 阅读用户提供的 `溥源智能_公司官网介绍.md`，提取“场景驱动、痛点导向、专业执行、任务交付”和 AI agent 产品化方向作为后续文案参考。
- 对资料中与现有事实源冲突的产品范围、品牌口号和英文简称做了边界记录；未因此修改页面内容、产品 Schema 或品牌实体命名。

### 渐进式官网重构约定
- 根据内部评审反馈，后续改为按用户提供的具体参考网页逐区块重构，不启动无明确参考的一次性全站改版。
- Lapa Ninja 仅作为案例索引；内容可参考旧站，事实仍以 `AGENTS.md` 与产品 PRD 为准。
- 本次只更新协作与交接约定，没有修改页面、路由、Schema 或视觉资产。

## 2026-08-10 · Codex

### 智小申黑白叙事重构
- 决策依据：本次对话经 `grill-me` 逐项确认，智小申主线为“帮企业找到并完成政府项目申报的全流程 agent”，优先受众为中小企业主与兼职申报人；转化路径调整为先理解工作流和能力，再决定是否试用。
- 视觉体系：纠正早期蓝白方案，改为与最新首页一致的黑白交替体系；白色 Hero、黑色困境区、白色工作流、黑色证据核查、白色信任区、黑色 CTA。荧光黄只用于当前状态和关键节点，不使用蓝色作为页面主色。
- Hero：主 CTA 改为“查看申报流程”，免费试用降为次级入口；新增代码原生申报工作台示意，明确标注为示意界面，不使用虚构客户、项目、审批结果或成功率。
- 困境区：改为左侧三个困境标签、右侧对应解法与产品界面；桌面支持悬停和点击，移动端使用点击切换，不添加“移入查看”等交互提示小字。三个结果固定为“少漏机会 / 资格判断更清楚 / 材料前后一致”。
- 工作流：五阶段改为桌面滚动驱动叙事，左侧阶段导航与右侧工作台保持 sticky，随滚动更新当前阶段；移动端取消长滚动占位，保留横向阶段选择和连续阅读。
- 深度内容：重点保留证据核查、能力边界、真实性红线和数据主权；六类核查名称严格保持为“数值一致性 / 证据-主张映射 / 证据链闭环 / 跨维度逻辑一致性 / 量化表述密度 / 检测机构可信度”。适用对象与 FAQ 压缩，Schema.org、canonical 和产品命名保持不变。
- 共享交互：`InteractiveGridBackground` 新增向后兼容的 `mono` 色调，智小申 CTA 使用黑白网格与荧光黄传播；其他页面继续使用原蓝色模式。智小申路由的共享导航通过页面作用域切换为黑白，不改动首页滚动主题或科小文主题。
- 涉及文件：`app/proposalpilot/page.tsx`、`components/ProposalWorkbench.tsx`、`components/InteractiveGridBackground.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 桌面 `1440x900` 与移动端 `412x915` 实测均无横向溢出、控制台错误或页面错误；三个困境标签与五个流程阶段数量正确，桌面悬停切换生效，滚动后工作流可推进到第三阶段。生产构建仅因沙箱无法联网获取 `next/font` 的 Inter 字体而中止，网络提权请求又被本地审批服务 `503` 拒绝，未发现页面代码编译错误。
- 顶部颜色修复：智小申路由同时覆盖共享 `.site-nav` 外层和 `.site-nav + main` 的旧浅蓝背景，使导航胶囊外侧与 Hero 统一为暖白 `#f6f6f3`；首页和科小文不受影响。
- 首页覆盖动效复用：智小申直接挂载共享 `HomePageCoverMotion`，困境、工作流、证据核查、人工门控与底部 CTA 统一使用首页 `home-color-page--cover` 契约；黑白区块按层级依次缩放、上推并覆盖前一区块，导航随当前黑页或白页切换主题。
- 白纸叠层恢复：重新接入 G1–G6 六张人工门控纸片和原有 sticky 叠层逻辑，保留每张纸的阶段、决策边界、确认角色、阶段交付及概念界面；蓝色旧皮肤改为黑白纸张、灰色网格与荧光黄门控编号。覆盖容器只在入场运动期间裁切，落位后恢复 `overflow: visible`，避免阻断纸张 sticky。
- 追加验证：Playwright 检测到 5 个覆盖页，入场中间态产生 `matrix(0.9266, 0, 0, 0.9266, 0, 95.19)`；G1–G6 共 6 张，滚动中段前三张分别固定在 `96 / 102 / 108px`，确认叠层生效。桌面与移动端继续无横向溢出和运行时错误。
- 最后一张纸吸附修复：浏览器不会把 `gate-stack` 的 `padding-bottom` 计入最后一张 sticky 纸片的有效内容高度，导致 G6 到位后立即随父容器离开。改为在纸堆末尾增加真实的视口高度内容间隔；桌面实测 G6 到达 `126px` 后继续滚动 `420px` 仍保持 `126px`，下一节保持在视口下方。移动端隐藏该间隔，维持连续阅读。

### 首页 Hero 蓝白重构
- 参考来源：用户提供的 Modal 首页录屏 `E:/视频/2026-08-07 09-57-50.mp4` 与 `https://modal.com/?ref=lapaninja`。
- 借鉴范围：居中宣言式标题、分层入场、稀疏空间元素漂移、主视觉周期性聚合与放大的节奏；未复制 Modal 的荧光绿、立方体素材、文案或导航结构。
- 删除首页 Hero 右侧 `SourceField` 和分栏布局，将品牌定位、说明和 CTA 改为居中构图；次 CTA 改为锚点跳转“探索产品”。
- 新增 `components/HomeHeroField.tsx`，用纯 CSS 实现蓝色源模块、扩散波和漂移节点，全部只动画 `transform` / `opacity`，并支持 `prefers-reduced-motion`。
- 首页使用局部蓝白主题，不改变智小申与科小文产品页；导航和页脚仅在首页通过作用域切换为浅色蓝白外观。
- 涉及文件：`app/page.tsx`、`app/globals.css`、`components/HomeHeroField.tsx`、`components/Nav.tsx`。
- 验证：桌面 `1440x900` 与窄屏 `390x844` 截图检查；`npm.cmd run check` 通过，Next 静态构建成功，设计令牌检查 `9/9 passed`。

### 首页页尾收束区
- 参考来源：用户提供的 Dayos 页脚录屏 `E:/视频/2026-08-07 10-26-16.mp4`。
- 借鉴范围：深色页尾作为页面收束、左侧品牌与直接联系方式、右侧稀疏分组导航；未复制其品牌、黄色强调色、Slack CTA、栏目名称或文案。
- 重写共享 `Footer`：以溥源品牌、`hello@puyuan.tech`、已存在的产品与公司链接组成页尾；新增 `#site-footer` 锚点。
- 使用深海蓝底和蓝色邮箱强调，首页蓝白主视觉在页尾切换为深色结束区，形成明确的浏览终点。
- 在支持 CSS scroll-driven animation 的浏览器中，页尾各列进入视口时依次上移显现；`prefers-reduced-motion` 回退为完整静态状态。
- 涉及文件：`components/Footer.tsx`、`app/globals.css`。验证：`npm.cmd run check` 通过，Next 静态构建成功，设计令牌检查 `9/9 passed`。

## 2026-08-07 · Codex

### 导航产品菜单动效
- 参考来源：用户提供的 `E:/视频/2026-08-07 10-14-47.mp4`。借鉴其导航项 hover 后浮层贴合展开、触发项高亮和菜单项依次进入的交互节奏；未复制其品牌、文案或绿色配色体系。
- 桌面端“产品”由点击式下拉改为 hover 与键盘 focus 均可触发的产品浮层。浮层以淡入、上移回弹和轻微缩放进入，两个产品项按顺序出现；滑入产品项后出现局部底色和箭头位移。
- 产品浮层保留智小申 ProposalPilot Agent 与科小文 ScholarPilot Agent，并增加各自已确认的简要场景说明。移动端继续使用点击式折叠菜单。
- 同步适配首页浅色蓝色主题与产品页深色主题，避免首页出现青绿色菜单高亮。
- 涉及文件：`components/Nav.tsx`、`app/globals.css`。
- 验证：`npx.cmd tsc --noEmit` 通过；Playwright 验证桌面 hover、跨间隙持续悬停、键盘 focus、移动端菜单均正常，桌面与移动端无横向溢出，浏览器无运行时错误。

### 首页“为什么选择溥源”互动图谱重构
- 参考来源：用户提供的 `E:/视频/2026-08-07 10-19-28.mp4` 与 `C:/Users/黑石/Desktop/3.PNG`。借鉴左侧关系网络与右侧纵向选择项联动、当前项突出显示的布局与交互原则；未复制参考品牌、文案或视觉素材。
- 删除首页原有四列静态“Why PuYuan”文字区，改为深色全宽互动区：左侧以“源”为核心连接图谱，右侧四项能力以折叠列表呈现；选择、点击或键盘聚焦任一项时，图谱标签和连接状态同步切换。
- 保留并原样使用已确认的四项事实文案：懂专业场景、从流程到交付、证据可以追溯、数据边界清楚。深色区域采用首页蓝色主题的低饱和强调色，避免受浅色首页工具类覆盖。
- 组件支持窄屏上下堆叠、可见焦点状态和 `prefers-reduced-motion`，动效仅用于连接线、轨道和切换入场。
- 涉及文件：`app/page.tsx`、`components/HomeSystemGraph.tsx`、`app/globals.css`。
- 验证：`npx.cmd tsc --noEmit` 通过；首页 HTTP 返回 `200`；浏览器自动检查桌面 `1440x900` 与移动端 `390x844` 均可切换到“证据可以追溯”状态、无运行时错误且无横向溢出；移动端按 `prefers-reduced-motion: reduce` 检查完成。

### 导航产品菜单微调
- 删除桌面端“产品”后的下拉箭头，使导航文字保持简洁。
- 产品浮层与触发项之间保留 `16px` 可见空隙，并用同高的透明 hover 桥接区保证鼠标移入浮层时不会意外关闭；新增从上到下的 `clip-path` 展开动效，浮层淡入和位移过渡调整为 320ms / 460ms，子项进入调整为 260ms / 340ms，第二项延迟调整为 80ms，使展开节奏更慢。
- 涉及文件：`app/globals.css`。验证：`npx.cmd tsc --noEmit` 通过；浏览器检查确认菜单与导航栏实际间距约 `17px`（含边框），展开后 `clip-path` 完全打开，且无运行时错误。

### 首页产品矩阵选择模式
- 参考来源：用户提供的 `E:/视频/2026-08-07 10-50-39.mp4` 与 `C:/Users/黑石/Desktop/5.PNG`。借鉴横向选择器的当前项前置、邻项退让、标签切换和左右控制交互；未复制参考站品牌、颜色或内容。
- 将首页产品矩阵拆为客户端选择组件 `ProductFamilyCarousel`：产品标签、左右箭头均可切换当前产品，当前卡片放大并增强边框/阴影，未选卡片降低透明度和饱和度；移动端改为单卡横向轨道。
- 保留智小申与科小文已有文案、路由和能力点；新增按钮均提供可见焦点状态，并支持 `prefers-reduced-motion`。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/page.tsx`、`app/globals.css`。
- 验证：`npx.cmd tsc --noEmit` 通过；浏览器桌面 `1440x900` 与移动端 `390x844` 均可切换至科小文、无运行时错误、无横向溢出，并完成人工截图检查。

### 首页产品矩阵扇形拖拽
- 根据用户对参考视频的进一步说明，将选择模式从点击切换升级为可拖拽扇形布局。
- 产品卡片以底部为旋转原点，拖动时跟随指针产生横向位移和轻微旋转；松手超过阈值后切换当前产品并吸附到中心，另一张卡片退到左右侧保持可见。点击标签、箭头和详情链接仍可使用。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。

### 扇形队列速度切换
- 拖拽释放时同时读取位移距离与最近指针速度：慢拖通常移动一格，快速甩动可连续跨越多个队列位置，最多连续切换三格。
- 队列一次性更新到目标顺序，利用卡片的 transform 过渡完成平滑吸附，避免逐格跳动。
- 涉及文件：`components/ProductFamilyCarousel.tsx`。验证：`npx.cmd tsc --noEmit` 通过。

### 修复产品扇形拖拽失效
- 增加 `lostpointercapture` 状态复位，避免浏览器丢失指针捕获后拖拽状态一直锁定。
- 拖拽区域改为 `touch-action: none` 并在按下时阻止原生手势抢占，鼠标和触控拖拽统一交给组件处理。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。验证：浏览器复现拖拽可移动，TypeScript 检查通过。
- 验证：`npx.cmd tsc --noEmit` 通过；浏览器检查确认三个位点拖动时同步更新，向左拖动后队列状态切换、空白补位数量为 1，且无运行时错误。
- 验证：`npx.cmd tsc --noEmit` 通过；桌面 `1440x900` 与移动端 `390x844` 拖拽切换均成功，当前产品状态从智小申切换为科小文，无运行时错误和横向溢出。

### 限定卡片拖拽范围
- 将拖拽事件从产品区外层移到每一张产品卡片和空白补位块；空白区域不再触发拖动，触屏纵向滚动保持正常。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。验证：TypeScript 与浏览器拖动检查通过。

### 使用全局指针监听修复拖拽兼容性
- 卡片按下后改由 `window` 监听 `pointermove` / `pointerup`，不再依赖变换元素的 Pointer Capture；鼠标移出卡片后拖动仍持续。
- 拖动后的详情链接点击会被拦截，正常点击仍可进入产品页。
- 涉及文件：`components/ProductFamilyCarousel.tsx`。验证：浏览器检查确认鼠标移出卡片后 transform 仍持续变化，无运行时错误，TypeScript 检查通过。

### 修复快速拖动回到原位
- 三位置循环队列原先允许快速甩动移动三格，正好完整循环回到原卡片，视觉上表现为切换失败。
- 将单次拖动最大有效步数限制为两格；慢拖切换一格，快速拖动切换两格，任何有效拖动都会改变当前卡片。
- 涉及文件：`components/ProductFamilyCarousel.tsx`。验证：浏览器快速向左拖动后当前卡片从智小申切换为科小文，状态发生变化且无运行时错误；`npx.cmd tsc --noEmit` 通过。

### 扇形队列连续过程动画
- 多格快速拖动改为逐格连续播放：每一格完成约 `520ms` 后再进入下一格，避免一次性重排造成卡片瞬移。
- 卡片位置、旋转、透明度和队列补位都沿用统一的 `760ms` 缓动过渡；播放期间暂时忽略新的拖动，完成后恢复交互。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。验证：`npx.cmd tsc --noEmit` 通过；浏览器在 `100ms`、`560ms`、`1100ms` 采样确认 transform 持续变化并最终切换到科小文，无运行时错误。

### 产品卡片弧线轨道
- 将三个位点改为连续弧线参数：卡片横向坐标根据拖动实时计算纵向弧高、旋转角和缩放比例，拖动时不再沿直线平移。
- 松手后的队列换位继续使用 transform 过渡，卡片沿弧线回到新的左、中、右位置。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。验证：`npx.cmd tsc --noEmit` 通过；浏览器采样确认三张卡片同时产生横向位移、纵向弧高和旋转角变化，无运行时错误。

### 提高快速拖动触发门槛
- 将速度跨位阈值提高到 `2.2 / 3.8`，普通切换距离提高到 `110px`，速度触发提高到 `1.6`，减少轻微拖动误触发多格动画。
- 涉及文件：`components/ProductFamilyCarousel.tsx`。验证：`npx.cmd tsc --noEmit` 通过。

### 取消快速拖动跨位动画
- 删除一次拖动跨越多个队列位置的逻辑与逐格播放，仅保留单次单步弧线切换。
- 触发门槛进一步提高为位移 `180px` 或速度 `2.8`，降低误触发频率。
- 涉及文件：`components/ProductFamilyCarousel.tsx`。验证：`npx.cmd tsc --noEmit` 通过。

### 产品切换连续衔接
- 三张卡片改为固定身份并共享同一条闭合弧线轨道，拖拽与松手吸附都通过连续相位计算位置；卡片不再在动画结束时重排 DOM，越过边缘的卡片会从轨道后方连续绕到另一侧。
- 松手后的 `760ms` 动画逐帧计算横向位置、弧高、旋转、缩放与前后层级，保留 settling 锁、空白补位、单次一位切换和较高的快速拖动门槛。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。验证：`npx.cmd tsc --noEmit` 通过；浏览器在拖拽 `55 / 110 / 165 / 220px` 及松手后 `80 / 260 / 520 / 780ms` 连续采样，三张卡片 DOM 身份始终不变且坐标沿闭合弧线连续变化，最终从智小申切换到科小文；桌面 `1440x900` 与移动端 `390x844` 均无横向溢出和运行时错误。

### 删除产品空白补位
- 删除产品选择区的“待扩展”空白卡片及其样式，只保留智小申与科小文两个真实产品。
- 三位闭合轨道改为双端弧线：智小申居中时科小文在右侧待命，向左拖动切换到科小文，反向拖动返回；标签和左右箭头均只在两个产品之间切换。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。验证：`npx.cmd tsc --noEmit` 通过；浏览器确认产品卡 DOM 数量在桌面与移动端均为 `2`，向左拖动后从智小申切换到科小文、向右拖动后返回智小申；桌面 `1440x900` 与移动端 `390x844` 均无横向溢出和运行时错误。

### 产品卡片统一场景连续切换
- 移除卡片进入中心时从浅色切换为深色的整套“换皮”效果；智小申与科小文从侧边待命、拖拽移动到居中以及离开中心的全过程都保持同一套深蓝材质、文字颜色和内部图形。
- 当前选择只通过轨道位置、缩放、前后层级及轻微透明度差表达，避免用户将状态切换误解为新卡片重新渲染；移动端同时提高侧边卡片可见度。
- 涉及文件：`app/globals.css`。验证：`npx.cmd tsc --noEmit` 通过；浏览器记录切换前后智小申始终为 `card-0`、科小文始终为 `card-1`，两张卡的外层背景均稳定为 `rgb(13, 27, 49)`、视觉画布均稳定为 `rgb(17, 31, 56)`，仅 transform、层级和活动状态变化；桌面 `1440x900` 与移动端 `390x844` 截图检查完成，无横向溢出和运行时错误。

### 增加连续切换测试空白页
- 按用户测试需求临时增加 3 张编号空白测试卡，与智小申、科小文组成 5 个固定 DOM 位置；空白卡只显示测试编号，不增加产品事实或链接。
- 拖拽和左右箭头改为在 5 个位置间逐张前后移动，轨道两端停止；所有卡片继续沿同一弧线移动，不做内容替换或 DOM 重排。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。验证：`npx.cmd tsc --noEmit` 通过；浏览器依次切换并确认智小申、科小文、空白测试页 1、2、3，再反向返回智小申，`card-0` 至 `card-4` 的 DOM 身份全程不变；桌面 `1440x900` 与移动端 `390x844` 均显示 5 张固定卡片，无横向溢出和运行时错误。

### 产品卡片切换性能优化
- 将松手后的吸附动画从 JavaScript `requestAnimationFrame` 每帧更新 5 张卡片的 25 个 CSS 变量，改为一次性生成 transform 关键帧并交给浏览器 WAAPI 合成线程播放；拖拽阶段每张卡只写入一个 transform 和必要的层级。
- 每卡关键帧由 31 个降为 16 个，只为中心及邻近卡片创建合成动画，远端卡片保留固定 DOM 但停止渲染；透明度直接随轨道距离连续变化，不再在动画中段切换活动 class。
- React 状态延后到动画结束同步，避免运动首帧整组卡片重渲染；拖拽期间暂停 spotlight 光影的指针重绘，并移除卡片 filter 动画。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。验证：`npx.cmd tsc --noEmit` 通过；同一无头 Chromium 环境下，1 秒采样的平均帧间隔由优化前 `45.45ms` 降至 `18.48ms`，最大帧间隔由 `216.7ms` 降至 `33.4ms`，约从 `22 FPS` 提升到 `54 FPS`；箭头依次切换科小文及空白测试页 1-3 后，向右拖拽可从测试页 3 返回测试页 2，5 张卡 DOM 身份不变；移动端无横向溢出和运行时错误。

### 移除产品拖拽提示文字
- 删除扇形产品区底部的“拖动选择产品”辅助文字，保留拖拽、标签和箭头控制。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。

### 产品卡片视觉美化
- 参考来源：用户提供的三张产品卡片截图，借鉴其大面积视觉封面、卡片内嵌产品图形、深色选中态与扇形叠放关系；未复制参考站品牌、配色或文案。
- 智小申卡片新增“源点—证据节点”图形，科小文卡片新增“稿件叠层”图形；当前选中卡片切换深蓝高对比主题，未选卡片保持浅色退让，增强选择层级。
- 保留现有产品事实、能力点、拖拽切换和详情链接；桌面与移动端均使用响应式视觉模块。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。
- 验证：`npx.cmd tsc --noEmit` 通过；桌面 `1440x900` 与移动端 `390x844` 截图检查完成，无运行时错误。

### 撤回横向产品轨道方案
- 按用户要求完整撤回本轮“深色横向惯性滚动轨道”实验，恢复上一版深蓝产品封面与扇形拖拽交互。
- 删除本轮新增的横向轨道组件逻辑、全部专用 CSS、扩展占位卡和临时验证文件；导航及其他页面改动不受影响。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。
- 验证：`npx.cmd tsc --noEmit` 通过；确认 `product-showcase` / `product-rail` 无残留样式。

### 产品扇形队列补位
- 根据用户新的交互要求，在扇形布局中加入一个空白方块作为队列补位；三个位点按左、中、右循环排列。
- 拖动时三个位点整体平滑跟手，松手超过阈值后循环换位，离开一侧的产品从另一侧排队出现；保留产品标签和左右箭头控制。
- 涉及文件：`components/ProductFamilyCarousel.tsx`、`app/globals.css`。

### 首页 Hero 主方块显示与拖动修复
- 根据用户截图修复首页蓝色主方块被首屏下边界裁掉的问题：方块改为完整显示，并重新压缩标题、说明、CTA 与互动模块之间的垂直留白。
- `HomeHeroField` 改为客户端交互组件，支持鼠标和触屏 Pointer Capture 拖动；拖动位置限制在 Hero 可视区内，松开后保持位置，指针取消或丢失捕获时会正确结束拖动。
- 增加键盘操作：方向键移动、`Shift + 方向键` 加速、`Enter` / `Home` 复位；键盘移动同样遵守边界。补充抓取光标、悬停反馈、焦点状态和防选中文字处理。
- 涉及文件：`components/HomeHeroField.tsx`、`app/globals.css`。验证：桌面 `1440x900` 截图确认方块完整且不遮挡文案；浏览器实际拖动从 `(570.5, 564.5)` 移至 `(720.5, 494.5)`，位移与指针输入 `+150 / -70` 一致，松开后状态清除且方块未越界；`npm.cmd run check` 完成，设计令牌检查 `9/9 passed`。

### 首页 Hero 玻璃立方体动效
- 参考来源：用户提供的 `E:/视频/2026-08-07 10-14-47.mp4`。通过 MediaKit 本地探测与关键帧提取分析视频中的方块，确认参考特征为独立顶面/侧面、玻璃高光、内部发光能量团、周期性扫光与缓慢俯仰；未上传视频，也未复制参考站的荧光绿色。
- 将原平面渐变方块重构为六面 CSS 3D 立方体，使用蓝白品牌色区分亮顶部、深侧面和半透明正面；内部能量团在不同方向移动并改变形态，表面高光周期扫过，整体持续轻微漂浮和旋转。
- 保留上一轮的鼠标/触屏拖动、边界限制、键盘移动与复位逻辑；在 `prefers-reduced-motion` 下关闭立方体旋转、内部流光、扫光和漂浮动画。
- 涉及文件：`components/HomeHeroField.tsx`、`app/globals.css`。验证：桌面 `1440x900` 的两个动画时刻与窄屏 `390x844` 截图检查完成；`npm.cmd run check` 通过，Next 静态构建成功，设计令牌检查 `9/9 passed`。

### 首页立方体无缝合拢、点击散开与视角交互
- 根据用户反馈收紧立方体各面的圆角与边框，合拢状态只保留连续发光棱线，不再出现面与面之间的可见缝隙。
- 增加点击展开/再次点击合拢：六个面沿各自法线向上、下、左、右及前后方向分离，并显露内部发光核心；展开时整体轻微退远和上移，避免散开的面板被 Hero 边界裁切或遮挡 CTA。
- 交互按状态区分：合拢时拖动仍移动立方体位置；展开后拖动改为限制范围内的俯仰和水平观察角度。使用 5px 移动阈值区分点击与拖动，防止松手误触合拢；键盘 `Enter` / 空格切换展开，方向键在展开状态调整视角，`Home` 完整复位。
- 新增围绕立方体的 12 个蓝色发光粒子；默认缓慢漂浮，展开时沿不同方向外移并增强亮度。`prefers-reduced-motion` 下关闭粒子和拆分相关动画。
- 涉及文件：`components/HomeHeroField.tsx`、`app/globals.css`。验证：浏览器自动化确认点击后 `aria-expanded=true`，展开后拖动产生 `viewX=5`、`viewY=10.8`，松手后保持展开且拖动状态清除；合拢与展开截图均完成视觉检查；`npm.cmd run check` 通过，Next 静态构建成功，设计令牌检查 `9/9 passed`。

### 首页长按体素化立方体
- 参考来源：用户提供的 `E:/视频/2026-08-07 11-18-02.mp4`，MediaKit 本地探测为 `13.27s / 1280x720 / 60fps`。关键帧显示目标是完整荧光立方体长按后转化为大小不一、带顶面和侧面的多个小立方体，散布到主视觉空间；不再使用整面分离。
- 删除“点击拆六面”的交互，改为 Pointer 长按约 `440ms` 触发体素化；中心保留缩小的主立方体锚点，30 个独立小方块按不同延迟、深度、尺寸和空间坐标分批散开，松手后回聚。
- 合拢状态保留原有位置拖动；长按散开后拖动改为限制范围内的观察角度调整。超过 `6px` 的移动会取消未触发的长按，避免普通拖动误触；键盘 Enter/空格按住进入散开，松开回聚，Home 复位。
- 荧光材质改为深蓝透明表面、白热青蓝核心、锐利窄扫光和局部蓝色光晕；小方块具有独立正面、顶部、侧面高光，周围环境粒子继续作为背景层。
- 涉及文件：`components/HomeHeroField.tsx`、`app/globals.css`。验证：桌面合拢、长按散开和窄屏截图完成；浏览器自动化确认散开状态包含 `30` 个体素并能维持；`npm.cmd run check` 通过，Next 静态构建成功，设计令牌检查 `9/9 passed`。

后续追加时使用日期和 agent 名称，至少说明：

1. 改了什么（页面、组件、配置或内容）。
2. 为什么改（用户需求、事实修正、视觉问题或构建问题）。
3. 如何验证（命令、截图或人工检查结果）。

## 2026-08-07 · Codex

### Hero 方块分解全屏散射与旋转
- 参考来源：用户提供视频 `E:/视频/2026-08-07 11-47-11.mp4`（1280×720、60fps）。
- 改动内容：更新 `components/HomeHeroField.tsx` 的 30 个 voxel 坐标，将散射范围扩展至桌面 Hero 近满宽；更新 `app/globals.css`，分解后主立方体缩放至约 30% 并保留为中心锚点，小方块增加独立 X/Y/Z 旋转、亮边和蓝白荧光层次，移动端使用缩放后的散射舞台。
- 改动原因：使长按分解后的状态符合最新参考视频：原始大方块消退、中心保留缩小版、小方块旋转飞散并覆盖主要视野，同时保持现有蓝白首页主题。
- 验证结果：`npm.cmd run check` 通过（TypeScript、Next 静态构建、设计令牌校验）；需在浏览器中继续进行长按/拖动视觉验收。

### Hero 分解触发与小方块 3D 材质修正
- 改动内容：移除原有约 `440ms` 的长按等待，指针按下后立即开始分解；短点击保留分解状态，再次点击聚合，分解状态拖动继续控制观察角度。小方块从三块斜面升级为六个真实 3D 面，并让自转与 `760ms` 飞散位移同步开始。
- 改动原因：解决点击无反应、长按存在中间停顿、旋转晚于飞行以及小方块立体感不足的问题；增加不同面的冷暖明暗、亮顶面、深色背面和蓝白荧光呼吸，使材质更接近用户参考视频。
- 验证结果：`npm.cmd run check` 通过；首页 HTTP 返回 `200`，服务端输出已包含新的点击交互说明，`prefers-reduced-motion` 同步关闭新增的材质动画。

### Hero 核心净空与持续自转修正
- 改动内容：将误落在核心旁边的中心 voxel 移到外围，并将分解状态下的小方块自转改为明确的 `2.6s linear infinite`，补充透视深度。
- 改动原因：截图中出现了核心立方体与散射方块重叠的视觉误读，且小方块到达目标位置后旋转不够明显。
- 验证结果：TypeScript 检查通过，首页保持 `200` 响应。

### 分解状态移除中心核心
- 改动内容：分解状态下隐藏 `.home-hero-field__core`，聚合状态恢复显示；飞散 voxel 与外围光场继续保留。
- 改动原因：用户确认截图中的中心缩小核心不需要保留，分解后视觉焦点应完全交给飞散小方块。
- 验证结果：CSS-only 状态调整，未改变路由或交互结构。

### 小方块持续旋转强制修正
- 改动内容：分解状态下将小方块内部 3D 容器的旋转动画显式设为持续运行，并加入 `will-change` 与 3D 保留；减少动效模式下仍明确关闭该动画。
- 改动原因：修复小方块飞散到目标位置后视觉上停止旋转的问题。
- 验证结果：TypeScript 检查通过，首页响应保持 `200`。

### 小方块抵达后保持最终朝向
- 改动内容：将分解状态下小方块的旋转从无限循环改为一次性 `760ms` 动画，并与飞散位移同步；动画结束后使用 `both` 保留最终朝向。
- 改动原因：按用户最新反馈，旋转只发生在飞出过程中，到达目标位置后停止。
- 验证结果：CSS-only 动效调整，未改变交互触发与拖动视角逻辑。

### 飞行与旋转时序校正
- 改动内容：飞散位移保持 `760ms`，旋转改为从同一时刻开始的 `600ms linear` 单次动画，确保旋转在方块抵达目标位置前完成。
- 改动原因：修复位移缓动先产生“到位”观感、随后仍能看到尾段旋转的问题。
- 验证结果：CSS-only 时序调整，最终朝向与减少动效回退保持不变。

### 分解动画整体降速
- 改动内容：小方块飞散位移由 `760ms` 延长至 `1600ms`，同步旋转由 `600ms` 延长至 `1300ms`。
- 改动原因：用户希望明显放慢分解过程，以便看清飞行中的 3D 旋转；旋转仍在抵达前结束。
- 验证结果：CSS-only 时长调整，交互状态与最终位置不变。

### 飞散位移延长至 3200ms
- 改动内容：小方块飞散位移从 `1600ms` 调整为 `3200ms`，旋转仍为 `1300ms` 单次动画。
- 改动原因：按用户指定进一步放慢飞散过程，让长距离位移更从容。
- 验证结果：CSS-only 时长调整，交互和最终状态保持不变。

### 小方块差异化旋转幅度
- 改动内容：为每个 voxel 增加独立的 X/Y/Z 中段与终点角度参数，旋转时长和飞散时序保持一致。
- 改动原因：避免所有小方块以相同角度翻转，增强散射场的自然层次与 3D 动态感。
- 验证结果：TypeScript 检查与首页响应验证通过。

### 首页窄桌面与短视口自适应
- 改动内容：新增 `640–1023px` 窄桌面断点，缩小标题、说明、交互方块与散射舞台，并为 Hero 设置 `700px` 的可滚动最小高度；移动端短视口设置 `650px` 最小高度。
- 改动原因：修复分屏/窄浏览器中标题字号过大、底部方块被裁切以及内容与视口高度竞争的问题。
- 验证结果：CSS 断点调整，保持首页结构与动效逻辑不变。

### Hero 方块回收交互与归核动画
- 改动内容：分解状态下点击 Hero 空白区域、中心交互区或键盘 Enter/空格可触发回收；新增 `is-returning` 状态，小方块在 `1150ms` 内反向翻转并归拢，在中心淡出后主方块延迟恢复。
- 改动原因：原回收触发区在小方块飞散后难以命中，且缺少独立的收束过渡。
- 验证结果：TypeScript、首页 HTTP 响应检查通过后交付。

### Hero 短桌面视口排版收束
- 改动内容：新增 `>=1024px` 且高度不超过 `800px` 的断点，收紧 Hero 上下内边距和文案底部保留，缩小标题、说明、核心方块和光场尺寸。
- 改动原因：修复短桌面窗口滚动后标题离开视野、仅剩 CTA 与散射方块的失衡构图。
- 验证结果：CSS 响应式规则调整，不改变页面内容、路由或方块交互状态。

### Hero 文案与方块构图解耦
- 改动内容：移除 Hero 文案的固定 `18rem` 底部占位；桌面端改为上部固定文案构图，方块以视口高度为基准固定在下部安全区，并同步修正窄桌面和短视口的方块底部定位。
- 改动原因：原有 flex 居中加大底部留白的组合会随窗口高度漂移，导致文案过低、方块贴底裁切，与参考首屏的清晰上下层级不一致。
- 验证结果：TypeScript 与首页 HTTP 响应检查通过后交付。

### Hero 体素聚合入场与大方块合成
- 参考来源：用户提供视频 `E:/视频/2026-08-07 14-15-56.mp4`，以 `8fps` 抽帧生成 `video-frames/entry-reference/contact-8fps.png` 进行时序分析。
- 改动内容：首次进入 Hero 时新增三段入场序列：散落 voxel 呈现（520ms）→ 30 个 voxel 收束为统一尺寸的中心网格（约 1560ms）→ 网格淡出并由大方块以发光尺度脉冲完成合成（840ms）。入场完成后恢复现有点击分散/回收交互。
- 改动原因：用户要求补充由分散到聚合的入场动画，以及小方块合成为大方块的明确过渡。
- 验证结果：TypeScript、Next 构建和首页 HTTP 响应检查通过后交付。

### Hero 合成动画渲染优化
- 改动内容：体素收束不再过渡 `width/height`，改为每个 voxel 的 GPU transform 缩放；增加局部渲染隔离，并降低入场阶段六面体的多重光晕开销。
- 改动原因：30 个六面 voxel 同时做尺寸变化和强阴影重绘会造成合成阶段掉帧。
- 验证结果：TypeScript 与首页 HTTP 响应检查通过后交付。

### 合成完成切回常态的跳变修正
- 改动内容：移除核心容器在合成结束时与常态漂浮关键帧之间的 transform 接管冲突；合成关键帧只负责透明度、垂直回弹和缩放，旋转继续由立方体自身控制。
- 改动原因：合成动画结束切换到正常方块时，父容器和子方块同时更换 transform，产生明显抽动。
- 验证结果：CSS-only 过渡修正，保留现有合成时序与交互。

### 分散动画 3D 绘制回归修复
- 改动内容：移除 voxel 和散射舞台上的 `contain: paint/style` 绘制隔离，仅保留 `will-change: transform, opacity`。
- 改动原因：paint containment 会与 `preserve-3d` 的飞散面叠加产生裁切/压平，导致点击分散动画异常。
- 验证结果：TypeScript 与首页 HTTP 响应检查通过。

### 交互式裂解与体素合成双向动画
- 参考来源：用户提供视频 `E:/视频/2026-08-07 14-52-42.mp4`，以 `12fps` 抽帧生成 `video-frames/interaction-reference/contact-12fps.png`，逐阶段分析裂解、飞散、聚合与合成节奏。
- 改动内容：点击正常大方块时先执行 `fracture`，大方块淡出并显露中心体素大方块，随后进入 `launch` 向外围飞散；点击回收时先进入 `gather` 聚成中心体素大方块，再进入 `synthesize` 发光合成为正常大方块。动画期间锁定重复输入，完成后恢复拖动视角与再次切换。
- 改动原因：原交互在大方块与散射 voxel 之间直接切换，缺少参考视频中清晰可见的中间拼合形态。
- 验证结果：TypeScript、完整构建与首页响应检查通过后交付。

### 裂解阶段大方块与体素重叠修正
- 改动内容：大方块先在 `480ms` 内完全淡出，中心体素延迟至 `520ms` 后出现并完成拼合，`1080ms` 后才进入外围飞散；同步延后交互解锁时点。
- 改动原因：修复大方块尚未消失时小方块已经出现的状态重叠。
- 验证结果：TypeScript 与首页 HTTP 响应检查通过后交付。

### 大方块与体素之间的能量桥接
- 改动内容：在裂解的 `920ms` 中加入中心蓝白能量脉冲，大方块收缩后由光晕遮罩形态切换，小方块从同一光源中心展开；减少动效模式下关闭该桥接效果。
- 改动原因：解决严格先后播放造成的大方块消失、小方块出现之间的视觉硬切，同时避免重新引入实体重叠。
- 验证结果：CSS-only 过渡增强，原有裂解与飞散时序不变。

### 主方块正面字样更新
- 改动内容：中心字样由“源”改为“溥源科技”，桌面字号调整为 `19px`、移动端为 `14px`，并保持单行居中。
- 改动原因：按用户要求强化公司品牌识别，同时避免四字名称溢出方块正面。
- 验证结果：TypeScript 与首页 HTTP 响应检查通过。
## 2026-08-07 · Codex

### 首页系统图谱背景统一
- 改动内容：将系统图谱外层从独立炭黑区块恢复为首页统一的 `#f5f8fe` 背景，保留左侧深色关系图作为内容画布。
- 改动原因：修复系统区与首页产品区之间的黑色横向断层，统一浅色首页的页面节奏与文字对比度。
- 验证结果：通过 `npm.cmd run check`，并确认 `/` 路由正常返回 `200`。

### 系统图谱画布颜色统一
- 改动内容：将左侧关系图画布、源点、节点、连线和底部标识同步切换到首页浅蓝白与品牌蓝色体系。
- 改动原因：消除关系图内部深色画布与其他首页区块的第二处色调断层，同时保留关系图的层级和动效辨识度。
- 验证结果：`npm.cmd run check` 通过，Next.js 静态构建和设计令牌检查均完成。

### 系统图谱连线持续流动
- 改动内容：在每条稳定关系线之上增加蓝色短光段，连线首次绘制完成后沿路径持续移动；切换右侧能力时重新播放完整连接过程。
- 改动原因：让关系图在静止状态下仍能表达信息持续流转，同时不干扰节点和拓扑识别。
- 可访问性：`prefers-reduced-motion` 下隐藏流动光段，只保留完整静态连线。

### 系统图谱源点到节点的序列入场
- 改动内容：将所有入场主线统一为从中心源点射向目标节点，按顺序逐条绘制；每条线完成后才释放对应节点。
- 视觉调整：节点由方块加文字改为发光圆点、内核、脉冲环和无容器文字标注，左右两侧自动镜像排布。
- 可访问性：保留静态关系图降级，减少动画模式下隐藏脉冲环和流动光段。

### 系统图谱统一坐标系重构
- 问题根因：旧图谱在组件挂载时即播放，用户滚动到区块时动画已经结束；节点使用 HTML 百分比定位而连线使用 SVG 坐标，在不同画布比例下会产生端点偏移。
- 改动内容：将源点、连线、节点和标签全部重构到同一个 `600×410` SVG 坐标系；连线从源点圆周精确计算起点并连接节点圆心。
- 动画顺序：图谱进入视口后才启动，严格执行“源点出现 → 线条绘制 → 节点确认 → 下一条线”，初始状态不显示任何连线。
- 视觉调整：中心改为同心源核与涟漪环，节点使用三层圆形状态标记、柔和光晕和无容器标签，移除方块节点。
- 验证结果：`npm.cmd run check` 通过，TypeScript、Next.js 静态构建与设计令牌检查 `9/9` 全部完成。

### 系统图谱方向箭头
- 改动内容：每条关系线绘制时增加沿路径移动的箭头，箭头自动旋转指向目标节点，到达后淡出。
- 时序约束：箭头与对应线条使用相同开始时间和持续时间，初始状态不显示，节点出现前完成方向提示。
- 可访问性：`prefers-reduced-motion` 下隐藏移动箭头。

### 方向箭头与线头同步修正
- 问题根因：关系线使用贝塞尔缓动，箭头使用线性运动，导致动画中段位置不同步。
- 改动内容：箭头改用与关系线一致的 `.65 0 .35 1` 速度曲线，尺寸扩大约一倍，并调整几何中心让箭头尖端贴合线条生长端点。

### 方向箭头统一动画时钟
- 问题根因：关系线由 CSS 动画驱动，箭头由 SVG SMIL 动画驱动，两套动画时钟在浏览器渲染中仍可能产生一帧以上的偏差。
- 改动内容：箭头改为 CSS `offset-path` 路径运动，与线条共用同一个延迟、`720ms` 时长和贝塞尔缓动，确保箭头始终贴在线条生长端。

### 方向箭头跨尺寸同步修正
- 问题根因：CSS `offset-path` 使用页面坐标，而图谱使用可缩放 SVG `viewBox` 坐标，因此只有特定画布尺寸能够视觉重合。
- 改动内容：关系线绘制与箭头移动统一改为 SVG 动画，并通过同一次 `beginElement()` 调用同步启动；两者共享路径、时长和缓动，在任意屏幕尺寸下保持同一坐标。

### 首页系统图谱配色与联动动效重做
- 参考来源：用户提供的 `E:/视频/2026-08-07 11-33-38.mp4` 与 `C:/Users/黑石/Desktop/6.PNG`。借鉴其左侧关系图随右侧内容切换、节点先出现再连接的叙事节奏，未复制参考站品牌、图标或文案。
- 改动内容：重做 `components/HomeSystemGraph.tsx` 的四套能力拓扑；右侧点击或键盘聚焦时，左侧对应节点依次出现、连线随后绘制并进入轻微呼吸状态；未操作时自动轮播，用户主动选择后停止自动切换。
- 视觉调整：该区由灰蓝黑改为更纯净的炭黑、深海蓝与电光蓝白，中心源点改为克制的方形系统核心，右侧活动项新增垂直状态线并强化文字层级。
- 可访问性：保留按钮语义、`aria-expanded` 与键盘操作；`prefers-reduced-motion` 下关闭自动轮播和所有入场动画，直接显示完整关系图。
- 涉及文件：`components/HomeSystemGraph.tsx`、`app/globals.css`。

### 产品卡片任意位置连续拖动
- 改动内容：移除卡片拖动的单卡位移限制，拖动阶段按 `350px/卡` 连续映射完整弧线轨道；松手时结合当前相位与速度投影，允许从第一张直接落到任意后续卡片。跨多卡动画按距离增加时长与关键帧，并让中间经过的卡片保持可见、层级随轨道动态变化。
- 改动原因：用户需要从第一张直接拖到任意卡片，切换过程必须连续经过中间卡片，而不是每次只能移动一位或刷新出现。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 实测桌面长拖、桌面快速划动、390×844 移动端快速划动均从第一张到第五张成功，5 个卡片 DOM 身份稳定、无控制台错误、无横向溢出；拖动帧间隔平均 `18.36ms`、最大 `33.4ms`。
- 涉及文件：`components/ProductFamilyCarousel.tsx`。

### 智小申视觉重构第一阶段
- 改动内容：将 `/proposalpilot` 首屏重组为产品承诺、权威事实与申报运行演示的非对称布局；问题区由三等分卡片改为诊断列表；五步工作流改为统一轨道，并用连续流线表达任务推进。
- 信息整理：恢复“问题 → 工作流 → 证据核查 → 人工门控 → 数据主权”的阅读顺序，删除重复的证据核查与数据主权展示；六类核查名称仍由 `auditTypes` 权威数组驱动，并传入交互图谱统一展示。
- 文案校正：移除控制台中的虚构运行编号与同步时间，将状态改为“示例场景”“证据同步核验”和“G4 定稿人工确认”。metadata、FAQ、Schema、canonical 与产品事实未修改。
- 动效约束：工作流流光仅用于表达流程方向，只动画 `transform/opacity`，在 `prefers-reduced-motion` 下关闭。
- 验证结果：TypeScript 通过；Playwright 在 `1440×900` 与 `390×844` 下无横向溢出和控制台错误，Hero 标题均为两行，CTA 在首屏可见；桌面 Hero 高 `836px`，移动端 CTA 底部位于 `432px`。`npm.cmd run check` 的 TypeScript 和编译阶段通过，但 Next.js 静态页面 worker 在当前 Windows 环境因内存错误退出，令牌检查因此为 `8/9`（缺少生产构建 CSS 产物）。
- 涉及文件：`app/proposalpilot/page.tsx`、`components/ProposalSections.tsx`、`components/EvidenceGraph.tsx`、`components/ProposalConsole.tsx`、`app/globals.css`。

### 产品页统一官网蓝白色调
- 改动内容：智小申与科小文页面统一使用官网浅蓝白背景、深蓝主文字和 `#1d5bff` 强调色；主按钮改为蓝底白字，次按钮改为白底蓝色描边状态。
- 面板层级：智小申申报控制台、科小文学术文档工作面保留深海蓝高对比度画布，内部连线、节点、扫描线和状态文字全部由绿色切换为蓝色体系。
- 组件统一：证据图谱、人工门控、数据主权切换、科小文能力卡片及 hover 状态同步改为蓝白配色；Proposal AmbientField 的粒子和流线改用官网蓝色。
- 语义修正：移除产品页内部重复的 `<main>`，保留全局布局提供的唯一主内容区域。
- 验证结果：TypeScript 通过；Playwright 在 `/proposalpilot`、`/scholarpilot` 的 `1440×900` 与 `390×844` 视口确认背景为 `#f5f8fe`、标题为 `#10203a`、强调色为 `#1d5bff`，无横向溢出、无控制台错误；科小文能力卡 hover 为蓝底白字。
- 涉及文件：`app/proposalpilot/page.tsx`、`app/scholarpilot/page.tsx`、`components/AmbientField.tsx`、`components/ScholarCapabilityGrid.tsx`、`app/globals.css`。

### 全站蓝白视觉一致性收束
- 问题定位：产品页主体已切换蓝白，但共享导航、产品二级菜单、科小文写作工作面与 Footer 仍保留大面积深色，导致首屏呈现多套明度系统。
- 改动内容：导航壳、桌面二级菜单、移动菜单和 Footer 统一改为白色/浅蓝表面，导航 CTA 改为官网蓝；导航周围增加浅蓝底，消除透明区域露出全局黑底的问题。
- 产品面板：科小文写作面板与文档层改为白底、浅蓝网格和深蓝文字；智小申可视化控件的浅色映射保持完整，避免恢复深色时产生主题断层。
- 品牌细节：Logo 色相滤镜重新校准为蓝色，不再出现偏紫效果；阴影统一为浅蓝投影。
- 验证结果：Playwright 在首页、`/proposalpilot`、`/scholarpilot` 的 `1440×900` 与 `390×844` 视口确认导航为浅色、菜单为浅色、科小文面板为浅色、Footer 为 `#edf3fb`；全部路由无横向溢出、无控制台错误。TypeScript 通过。
- 涉及文件：`app/globals.css`。
## 2026-08-07 · Codex

### 智小申五步流程自动横向流转
- 改动内容：将 `/proposalpilot` 五步工作流从手动横向滚动改为双组卡片无缝循环，轨道以 `28s` 匀速持续向左流转，首尾衔接不跳帧。
- 交互与可访问性：鼠标移入、键盘聚焦时临时暂停；按用户反馈移除右上角暂停/继续按钮；减少动效模式关闭自动流转并恢复手动横向滚动。
- 内容约束：发现机会、诊断资格、编制材料、质检定稿、申报陪伴五步内容与视觉卡片均保持不变。

## 2026-08-07 · Codex

### 首页产品矩阵替换扇形轮播
- 改动内容：新增 `ProductMatrix` 双列产品矩阵，在首页并排展示智小申与科小文，完整保留产品名称、定位、能力点、状态与详情入口；移动端改为纵向排列。
- 交互调整：删除矩阵卡片顶部装饰图并降低卡片高度；卡片在鼠标移入、键盘聚焦或触屏聚焦时沿 Y 轴翻转，背面展示产品完整流程、能力边界或上线状态。
- 细节优化：翻转时长由 `760ms` 放慢至 `1200ms`；智小申背面增加 `200+` 页材料、`6 类 14 项`核查与 `G1-G6` 人工门控，科小文背面增加个人、机构适用对象与内测状态。
- 保留策略：原 `ProductFamilyCarousel` 扇形轮播组件及其交互样式保持不变，后续产品数量和内容丰富后可直接重新调用。
- 改动原因：当前公开产品内容较少，双列矩阵能同时展示完整信息，避免轮播留白与次要卡片被遮挡。

### 智小申底部证据流 CTA 动效
- 参考来源：用户提供视频 `E:/视频/2026-08-07 16-51-57.mp4` 与截图 `C:/Users/黑石/Desktop/捕获.PNG`，吸收固定内容层、缓慢流动色带与两侧十字节点阵列的动效关系。
- 改动内容：将 `/proposalpilot` 底部“把申报工作放回可核查、可协作的轨道”重做为动态 CTA；标题和按钮保持稳定，背景品牌蓝色带缓慢迁移，两侧证据节点阵列相向漂移，并增加少量信号点穿行。
- 响应式与可访问性：移动端收敛节点密度并隐藏第二信号点；`prefers-reduced-motion` 下关闭所有循环动画，保留静态背景与内容。

### 智小申首屏内容收束
- 改动内容：移除 `/proposalpilot` 首屏右侧的申报流程演示面板，将产品名称、主标题、说明、CTA 与核心事实数据统一改为居中布局。
- 改动原因：按用户截图反馈减少首屏视觉负担，让产品主张成为唯一阅读焦点。
- 验证结果：TypeScript 与页面响应检查通过，桌面和移动端首屏均保持稳定居中。

### 公司介绍页与常见问题页
- 改动内容：重写 `/about` 为编辑式公司叙事页，补充公司定位、命名含义、agent 构建方法、产品家族、产品原则与长期方向；新增 `/faq` 独立问答页，按公司与方法、智小申、科小文与联系分组，并输出服务端可抓取内容与 `FAQPage` JSON-LD。
- 改动原因：将首页下方的公司介绍与常见问题拆为职责清晰的独立页面，减少首页重复内容，同时增强品牌叙事、用户查找效率与 GEO 实体信息完整度。
- 全站连接：首页原 FAQ 改为 `/about` 与 `/faq` 的双入口知识区；桌面和移动导航、Footer、sitemap 均增加常见问题入口；公司 `Organization` 描述同步校正为已确认事实。
- 视觉与响应式：两个页面沿用首页浅蓝白背景、品牌蓝与深海军蓝页尾体系；公司页采用方法轨道与产品目录，FAQ 页采用桌面粘性索引和移动端单栏折叠问答。
- 验证结果：`npx.cmd tsc --noEmit` 与 `npm.cmd run check` 通过，Next.js 生产构建静态生成 `/about`、`/faq` 与 `/sitemap.xml`；设计令牌检查 `9/9` 通过；上述路由返回 `200`，canonical 与 JSON-LD 均存在；Playwright 完成桌面及 Pixel 7 全页截图检查，无横向溢出。
## 2026-08-07 · Codex

### 导航与页面顶部黑线修复
- 问题根因：悬浮导航实际高度为 `78px`，全局主内容顶部占位为 `80px`，两者之间露出了 `2px` 的旧深色 `body` 背景，形成横跨视口的黑线。
- 改动内容：为导航后的共享主内容层补充官网蓝白背景 `#f5f8fe`，消除顶部接缝，不改变导航尺寸、阴影、页面结构或产品页内容。
- 验证结果：Playwright 在 `1003×319` 桌面视口与 `390×844` 移动视口确认接缝区域计算背景均为 `#f5f8fe`，截图中无黑线；`npx.cmd tsc --noEmit` 通过。
- 涉及文件：`app/globals.css`。
## 2026-08-07 · Codex

### 科小文首屏示意图移除
- 改动内容：删除 `/scholarpilot` Hero 右侧的写作流程示意图及 `ProductField` 引用，将标题、说明和 CTA 收拢为居中单列布局。
- 改动原因：按用户截图反馈移除该视觉面板，同时避免原双栏结构留下空白区域。
- 验证结果：Playwright 在 `1440×900` 与 `390×844` 视口确认示意图节点为 0、Hero 居中、CTA 对齐正常，无横向溢出和控制台错误；`npx.cmd tsc --noEmit` 通过。
- 涉及文件：`app/scholarpilot/page.tsx`、`app/globals.css`。

## 2026-08-07 · Codex

### 全站标题与排版层级统一
- 改动内容：新增共享 Hero 排版规则，页面 H1 统一为桌面 `64px`、平板 `52px`、移动 `40px`；标准区块 H2 统一为 `36px / 30px`，品牌叙事型展示 H2 统一为 `48px / 38px`。同步统一 Hero 眉题为 `11px`，说明文字为桌面 `17px`、移动 `16px`。
- 改动原因：不同路由原先 H1 在桌面为 `64–92px`、移动为 `36–50px`，H2 也在 `28–64px` 间漂移，导致页面像不同设计系统；同时检查并修正字号调整后的首屏高度、换行和内容宽度。
- 涉及文件：`app/page.tsx`、`app/proposalpilot/page.tsx`、`app/scholarpilot/page.tsx`、`app/about/page.tsx`、`app/faq/page.tsx`、`components/ProductFamilyCarousel.tsx`、`components/HomeSystemGraph.tsx`、`components/ProposalSections.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过，静态生成 13 个页面/资源，设计令牌检查 `9/9` 通过；Playwright 覆盖 `1440×900`、`768×900`、`390×844` 五个路由，H1/H2 计算字号符合统一阶梯，三种视口 `scrollWidth` 均等于视口宽度，无控制台错误；完成五页桌面截图复核。

## 2026-08-07 · Codex

### 首页内容架构重排
- 改动内容：将首页调整为“Hero 公司身份 → 关于溥源 → 产品家族 → 工作方法 → 能力边界与数据安全 → FAQ → CTA”的理解路径。Hero 增加公司定义并将主 CTA 改为“了解溥源”；关于溥源前置到产品轮播之前；原关于/FAQ 混合区拆分为独立的公司介绍、能力边界和 FAQ 区块。
- 改动原因：原首页先展示动效、产品和系统图谱，用户难以判断溥源科技是什么公司、agent 解决哪类工作；新的顺序先建立公司认知，再解释产品和方法。
- 涉及文件：`app/page.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过，设计令牌检查 `9/9` 通过；Playwright 在 `1440×900` 与 `390×844` 检查首页区块顺序、首屏高度、横向宽度和控制台错误，`scrollWidth` 均等于视口宽度；完成首页全页桌面与移动截图复核。

## 2026-08-07 · Codex

### 全站 Hero 标题蓝色渐变统一
- 改动内容：将全局 `text-accent-gradient` 收口为深蓝 `#174ebf`、品牌蓝 `#246bfe`、亮蓝 `#43a2ff` 的单一渐变规则；首页“驱动专业 agent”、公司页“真实工作流程”、FAQ 页“清楚回答。”统一使用该规则，智小申和科小文已有的 Hero 强调语句同步继承。
- 改动原因：按用户确认的字体效果图把纯蓝强调升级为克制的蓝色渐变，并避免各页面维护不同渐变值造成品牌色漂移。
- 涉及页面：`/`、`/proposalpilot`、`/scholarpilot`、`/about`、`/faq`。
- 验证结果：`npm.cmd run check` 通过，Next.js 静态生成 13 个页面/资源，设计令牌检查 `9/9` 通过；桌面 `1440×900` 与移动端 `390×844` 检查覆盖五个页面，渐变渲染、标题换行和 CTA 均正常，移动端五页 `scrollWidth` 均为 `390px`，无横向溢出。
## 2026-08-07 · Codex

### 参考 StatsPAI 补充科小文内容
- 参考来源：`https://www.statspai.com/zh`。借鉴其“真实科研负担 → 连续任务流程 → 研究者保留判断 → 适用对象与入口”的内容组织方式，不复制品牌文案、产品数据或统计分析能力。
- 改动内容：在 `/scholarpilot` 三项已确认能力之后新增“协作方式”和“内测对象”区块，明确研究者与科小文的责任分工，并为科研个人、实验室与科研团队提供清晰的内测语境。
- 事实边界：页面仍只公开选题与文献综述、初稿生成、润色与投稿准备三类能力；FAQ 明确统计分析、代码复现等尚未作为科小文公开功能，不增加未经 PRD 确认的数据、格式支持或效果承诺。
- GEO：FAQ 从 2 条补充到 5 条，服务端可见内容与 `FAQPage` JSON-LD 同步更新。
- 验证结果：Playwright 在 `1440×900` 与 `390×844` 视口确认新增协作条目 3 条、受众条目 2 条、FAQ 与 Schema 均为 5 条，无横向溢出和控制台错误；`npm.cmd run check` 通过，Next.js 静态构建完成，设计令牌检查 `9/9`。
- 涉及文件：`app/scholarpilot/page.tsx`、`app/globals.css`。

## 2026-08-07 · Codex

### 科小文“研究稿件旅程”视觉与动效重构
- 参考来源：Lapa Ninja 收录的 Dovetail、Slite、Reflect 与 Dropbox Paper 页面。吸收研究任务分段叙事、编辑式大标题、单屏视觉焦点和文档协作隐喻，不复制原站配色、品牌文案或产品界面。
- 改动内容：将三张等权能力卡重构为桌面端“左侧阶段叙事 + 右侧粘性稿纸”的连续滚动体验；稿纸在同一画面内依次呈现研究问题、章节结构和投稿检查，不换页、不重新弹出卡片。
- 协作与受众：协作方式改为带活动节点和进度线的责任交接轨道，增加“判断留给研究者，重复整理交给科小文”的核心视觉焦点；内测对象改为编辑式横向条目与方向反馈。
- 动效与响应式：滚动阶段由 `IntersectionObserver` 驱动，只动画 `transform`、`opacity` 和进度线；移动端取消粘性稿纸并改为可点选的连续步骤；`prefers-reduced-motion` 下关闭循环与过渡动画。
- 事实与 GEO：继续只公开选题与文献综述、初稿生成、润色与投稿准备三项已确认能力；metadata、canonical、5 条 FAQ、`SoftwareApplication` 和 `FAQPage` Schema 均未修改。
- 可撤回性：改版前的完整 `app/scholarpilot/page.tsx` 已保存至 `backups/scholarpilot-before-manuscript-journey-2026-08-07/page.tsx`；旧 `ScholarCapabilityGrid` 保持原样，新版使用独立组件和 CSS Module，未修改全局样式。
- 验证结果：TypeScript 通过；Playwright 验证三个滚动阶段均能依次激活且稿纸内容同步变化，移动端 `390px` 视口 `scrollWidth` 等于 `390px`、稿纸为非 sticky、FAQ 仍为 5 条、JSON-LD 仍为 2 组，控制台无错误；完成桌面和移动端全页截图检查。
- 涉及文件：`app/scholarpilot/page.tsx`、`components/ScholarJourney.tsx`、`components/ScholarJourney.module.css`、`scripts/qa-scholar-journey.cjs`、`CHANGELOG.md`、`backups/scholarpilot-before-manuscript-journey-2026-08-07/`。

### 科小文写作流程标题校正
- 改动内容：将“不是三件分开的工具，而是一段连续的写作过程”改为“从选题到投稿，一步步推进研究写作”，并用文献整理、初稿组织和投稿前表达检查说明具体流程。
- 改动原因：原表述依赖用户先理解“三件工具”指什么，脱离上下文时语义不明确；新文案直接说明阶段、主体和任务。
- 事实边界：仍只描述已确认的三项能力，没有增加统计分析、代码复现或效果承诺。
- 涉及文件：`components/ScholarJourney.tsx`、`CHANGELOG.md`。

### 导航产品菜单跨页自动收起
- 改动内容：将桌面端产品二级菜单从纯 CSS 悬停改为显式开关状态；点击产品子项或路由变化时立即关闭，并要求鼠标移出产品区域后才能再次悬停打开。移动端主菜单也会在路由变化时自动收起。
- 改动原因：客户端跨页跳转不会改变鼠标位置，旧的 `:hover` 条件在新页面仍然成立，导致二级菜单持续显示。
- 验证结果：Playwright 确认桌面端从首页二级菜单进入 `/scholarpilot` 后 `data-open` 由 `true` 变为 `false`、菜单计算样式为 `visibility: hidden`；移动端从 `/scholarpilot` 进入 `/proposalpilot` 后 `details` 的 `open` 属性被移除；控制台无错误。`npm.cmd run check` 通过，13 个静态页面/资源生成成功，设计令牌检查 `9/9`。
- 涉及文件：`components/Nav.tsx`、`app/globals.css`、`scripts/qa-nav-route-close.cjs`、`CHANGELOG.md`。

### 删除首页 FAQ 预览区
- 改动内容：移除首页“Common questions / 先回答几个关键问题”预览区及其专属样式，保留独立 `/faq` 页面、导航入口和 FAQ 结构化数据。
- 改动原因：按用户截图反馈，首页该区块不再需要，减少首页内容重复与视觉占位。
- 涉及文件：`app/page.tsx`、`app/globals.css`、`CHANGELOG.md`。

### 删除产品卡片悬停提示文字
- 改动内容：移除首页产品翻转卡片底部的“移入查看完整信息”提示文字和循环箭头，仅保留卡片原有悬停翻转、键盘聚焦和详情内容。
- 改动原因：按用户截图反馈，删除卡片底部多余的小字提示，减少视觉噪音。
- 涉及文件：`components/ProductMatrix.tsx`、`app/globals.css`、`CHANGELOG.md`。

### 首页产品卡片视觉重构
- 改动内容：将产品卡片从深色双面翻转改为蓝白单面展示；增加产品专属视觉预览、清晰的信息层级、能力列表和详情入口，并加入轻微上浮、边框高亮与鼠标跟随高光。
- 改动原因：原翻转结构信息隐藏在背面，视觉重量较大且不利于快速理解产品；新结构让关键信息在同一视图内完成扫描。
- 动效约束：高光仅通过 CSS 变量定位，`prefers-reduced-motion` 下关闭过渡；不改变产品事实、路由和详情内容。
- 涉及文件：`components/ProductMatrix.tsx`、`app/globals.css`、`CHANGELOG.md`。

### 产品卡片鼠标跟随倾斜
- 改动内容：根据指针在卡片内的位置计算小幅 `rotateX / rotateY`，让卡片产生连续的鼠标跟随晃动，同时保留上浮和高光效果；倾斜幅度限制在约 3.2°。
- 改动原因：按用户视频参考增加卡片的空间反馈，让悬停状态更有动感，但不影响阅读和点击。
- 无障碍：`prefers-reduced-motion` 下禁用倾斜和过渡。
- 涉及文件：`components/ProductMatrix.tsx`、`app/globals.css`、`CHANGELOG.md`。

### 产品卡片 CTA 文案调整
- 改动内容：将首页产品卡片底部 CTA 从“了解详情”改为“立即体验”，保留原有产品页跳转地址和箭头动效。
- 改动原因：让卡片主操作更直接，突出进入产品体验的意图。
- 涉及文件：`components/ProductMatrix.tsx`、`CHANGELOG.md`。

### 产品卡片 CTA 按钮与交互动效
- 改动内容：将首页产品卡片底部的“立即体验”从文字链接升级为蓝色按钮，加入悬停滑光、箭头推进、上浮阴影、按压反馈和键盘焦点样式。
- 改动原因：明确卡片的主操作层级，并为进入产品页提供可感知但克制的交互反馈。
- 无障碍：`prefers-reduced-motion` 下关闭按钮过渡与位移，保留静态可点击状态。
- 涉及文件：`components/ProductMatrix.tsx`、`app/globals.css`、`CHANGELOG.md`。

### 移除产品卡片 CTA 箭头
- 改动内容：移除“立即体验”按钮中的箭头图标及箭头位移动效。
- 改动原因：按页面调整要求让按钮视觉更简洁，保留滑光、上浮和按压反馈。
- 涉及文件：`components/ProductMatrix.tsx`、`app/globals.css`、`CHANGELOG.md`。

### 取消产品卡片整体上浮
- 改动内容：移除产品卡片悬停和聚焦时的 `translateY(-6px)` 上浮位移。
- 改动原因：让卡片保持稳定位置，仅保留鼠标倾斜、边框高亮和跟随高光反馈。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。

### 删除首页底部 CTA 区块
- 改动内容：删除首页底部“Start from the source / 从一个真实问题，开始一段可靠的协作”及其“免费开始”“联系团队”按钮区块。
- 改动原因：按页面调整要求移除截图所示区块，保留首页其他内容与 Hero 操作入口。
- 涉及文件：`app/page.tsx`、`CHANGELOG.md`。

### Footer 背景分区调整
- 改动内容：将底部 Footer 背景调整为独立的浅蓝色 `#e5eefb`，并增强顶部边界与内侧高光。
- 改动原因：让 Footer 与首页主体背景形成清晰的底部层级，同时保持蓝白品牌风格和文字对比度。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。

### Footer 高对比深色方案
- 改动内容：将 Footer 从浅蓝色改为深海军蓝 `#07172d`，并同步调整标题、正文链接、邮箱、辅助文字和分隔线颜色。
- 改动原因：强化页面内容结束后的明暗分区，让底部区域具有更明确的视觉落点，同时保持蓝色品牌体系。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。

### 导航二级菜单精简
- 改动内容：移除产品二级菜单两个子项右侧的箭头，将菜单宽度从 `316px` 缩窄至 `276px`，并收紧子项布局。
- 改动原因：降低二级菜单的横向占用，让产品入口更简洁紧凑。
- 涉及文件：`components/Nav.tsx`、`app/globals.css`、`CHANGELOG.md`。

### 首页公司信息与问题区重构
- 改动内容：首页公司信息区改为“品牌定义 + 源点系统图 + 紧凑事实栏”的非对称结构。新增独立客户端组件展示专业知识、智能工作流、企业申报与学术写作之间的源点关系，包含鼠标跟随高光、节点呼吸和沿线流动效果。首页问题区改为连续问题轨道，为信息分散、流程断裂、责任不清分别配置状态图形、悬停反馈与进入动画。
- 改动原因：原有两个区块均为标题加等分栏，视觉节奏重复且大面积留白没有信息焦点。新版分别承担“溥源是谁”和“为什么从工作现场开始”的叙事任务。
- 事实边界：保留公司全称、AI-native 定位、企业申报与学术写作方向，以及三项真实工作问题的原有表述；未新增产品能力或业务数据。
- 响应式与无障碍：桌面端为非对称图文与横向轨道，平板和移动端自动切换为单列；`prefers-reduced-motion` 下关闭节点呼吸、流光、轨道动画和交互过渡。
- 涉及文件：`app/page.tsx`、`components/CompanySourceVisual.tsx`、`app/globals.css`、`CHANGELOG.md`。

### 导航栏圆角调整
- 改动内容：将顶部导航外壳圆角调整为 `18px`。
- 改动原因：让浮层导航的轮廓更柔和，与页面中的圆角视觉语言更协调。
- 涉及文件：`components/Nav.tsx`、`CHANGELOG.md`。

### 首页参考视频叙事节奏重构
- 参考来源：用户提供视频 `E:\视频\2026-08-10 11-00-03.mp4`。借鉴其浅色首屏、右侧实体视觉入场、深色宣言区、方法信号带与左右分栏的节奏，不复制原视频的品牌、文案或彩色 3D 资产。
- 改动内容：首页首屏改为左侧品牌定位与 CTA、右侧现有可拖拽源点立方体；公司信息区切换为深海军蓝宣言区，新增“源点 / 流程 / 判断”方法信号带，并保留动态源点系统图与公司事实栏。宣言标题调整为“不是替你做决定，而是把专业工作推进到结果”。
- 文案调整：保留已确认定位句“从知识源头，驱动专业 agent”和现有公司事实，仅增加方法层级表达，不新增产品数据或未经确认的能力。
- 响应式与无障碍：平板和移动端恢复单列叙事，保留立方体交互；减少动态效果设置下关闭源点和入场动画。
- 涉及文件：`app/page.tsx`、`app/globals.css`、`CHANGELOG.md`。

### 深色宣言区上推过渡
- 改动内容：让深色宣言区向上重叠浅色首屏 `72px`，增加 `36px` 上圆角，并在支持滚动驱动动画的浏览器中使用视口进入动画从下方推入。
- 改动原因：还原参考视频中整块深色页面从下方连续上推的转场感，而不是普通区块硬切换。
- 响应式与无障碍：移动端重叠调整为 `42px`；`prefers-reduced-motion` 下取消位移动画但保留结构和圆角。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
## 2026-08-07 · Codex

### `/about` 公司介绍页美化
- 改动内容：压缩首屏空白，将 Hero 标题调整为「从知识源头，进入真实工作流程」，新增右侧“源点到交付”四步流程轨道；底部事实条增加编号；在 Hero 后新增“我们关注什么样的工作”三项聚焦内容（高信息量、复杂多步骤、专业判断）；原有公司起源、方法、产品家族、原则和未来方向按 02–05 重新编号。
- 改动原因：参考用户提供的页面截图，原页面首屏留白过大、公司定位与信息层级不够明确，需要让访客先理解溥源的工作对象与工作方式，再进入品牌细节。
- 涉及文件：`app/about/page.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过；Playwright 检查桌面 `1440x900` 与移动端 `390x844` 均无横向溢出和控制台错误。桌面 Hero 高 `563px`、H1 `64px`，移动端 Hero 高 `807px`、H1 `40px`；四个流程项与三个聚焦项均正常渲染。
## 2026-08-07 · Codex

### 智小申流程区改为横向步骤画廊
- 改动内容：将 `/proposalpilot#capabilities` 从五列文字轨道改为五张连续的横向工作流卡片；每张卡包含阶段编号、视觉预览占位、阶段说明和“输入 → 判断 → 交付”交接提示；桌面端横向排列并支持悬停抬升，移动端保持横向滑动浏览。
- 改动原因：参考用户提供的视频，流程应以连续的横向工作画面推进，而不是一次性平铺的静态文字列表；图片素材先留出稳定插槽，后续可以逐张替换，不再改动布局结构。
- 图片约定：计划使用 `public/images/proposal-workflow-evidence.png` 作为流程视觉素材；本次 `gpt-image-2` 调用因当前 API key 返回 `401 invalid_api_key` 未生成文件，因此页面使用 CSS 视觉占位，不引用失效路径。
- 涉及文件：`app/proposalpilot/page.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过；`/proposalpilot` 返回 `200`，流程区五个 `PHASE` 节点均存在；移动端横向画廊可滚动且不改变页面整体宽度。
## 2026-08-07 · Codex

### 删除流程卡片辅助小字
- 改动内容：移除智小申流程卡片底部的“输入 → 判断 → 交付”辅助文字，并清理对应 CSS。
- 改动原因：用户反馈该小字干扰卡片主体信息，流程卡片保留阶段编号、视觉区域、标题和说明即可。
- 涉及文件：`app/proposalpilot/page.tsx`、`app/globals.css`。
- 验证结果：页面结构保持不变，横向流程卡片和响应式布局继续使用。
## 2026-08-10 · Codex

### `/about` 公司介绍页信息架构展开
- 参考来源：Lapa Ninja 中已抓取的 Dovetail、Slite、Dropbox Paper、Reflect 页面截图。吸收其公司定位、方法叙事、真实工作场景和交替内容节奏，不复制品牌视觉、文案或产品界面。
- 改动内容：在 Hero 后新增“公司基本信息”区，明确公司全称、英文名、AI-native 定位和工作方式；新增“为什么从工作现场开始”问题区，说明信息分散、流程断裂、责任不清三个真实问题；原有关注领域、公司命名、agent 方法、产品家族、产品原则和长期方向顺延为 03–07。
- 改动原因：原页面虽有公司叙事，但基本信息和用户痛点隐藏在段落中，访客无法快速判断溥源是谁、为什么做专业 agent。新增区块让公司身份先于方法与产品被理解。
- 涉及文件：`app/about/page.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过；`/about` 返回 `200`，服务端 HTML 包含新增公司身份区和问题区；沿用现有蓝白令牌、响应式断点与无障碍链接结构。
## 2026-08-10 · Codex

### 首页前置公司基本信息
- 改动内容：将公司身份区和真实问题区同步放入首页 `About Puyuan` 后方，首页直接展示公司全称、英文名、AI-native 定位、工作方式，以及信息分散、流程断裂、责任不清三个工作现场问题；产品矩阵继续位于其后。
- 改动原因：用户明确要求公司具体信息应在首页展示，而不是依赖跳转 `/about` 才能理解。
- 涉及文件：`app/page.tsx`、`app/globals.css`。
- 验证结果：保留首页 `#about` 锚点、产品矩阵和 `/about` 独立页；沿用已有蓝白响应式样式。

## 2026-08-10 · Codex

### 公司基本信息页视觉与文案落地
- 改动内容：将首页与 `/about` 的公司身份区收束为“公司是谁 / 具体做什么 / 如何工作 / 当前方向”的四列事实信息；补充企业申报、学术写作等明确工作场景，以及“把分散知识变成可执行步骤、把过程推进到可继续使用成果”的具体表述。问题区标题改为“专业工作真正难的，是把过程推到结果”。
- 改动原因：根据页面一效果图，将抽象的品牌介绍改成可快速扫描的公司事实与工作对象，让访客在进入产品前理解溥源的业务范围和方法。
- 响应式处理：桌面四列、平板两列、移动单列，保留分隔线层级并修正移动端内边距。
- 涉及文件：`app/page.tsx`、`app/about/page.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过，Next.js 静态生成 13 个页面/资源，设计令牌检查 `9/9`；首页返回 `200`，服务端 HTML 包含公司事实区与三项问题区。Playwright 未安装，未执行截图脚本。

### 首页公司介绍区结构重排
- 改动内容：删除 Hero 后重复的 `About Puyuan` 双栏简介，将公司基本信息提升为首页第一段主叙事；改为英文眉题、左侧大标题、右侧具体定义、下方四列事实的完整结构，并增加浅蓝背景层次。
- 改动原因：前一版只改变文案和列数，视觉结构几乎没有变化，且两段公司介绍连续出现造成重复。本次按已确认的页面一效果图直接重排信息层级。
- 响应式处理：`821px` 以上保持四列事实，平板两列、移动单列；公司介绍正文在窄屏转为单列。
- 涉及文件：`app/page.tsx`、`app/globals.css`。
- 验证结果：开发服务器首页返回 `200`，HTML 已不包含旧 `home-intro`，并包含新 `COMPANY PROFILE` 与“当前方向”；`npm.cmd run check` 通过，13 个页面/资源完成静态生成，设计令牌检查 `9/9`。

## 2026-08-10 · Codex

### 智小申问题区悬停解法卡片
- 改动内容：将 `/proposalpilot` 的问题区从左右分栏文字列表改为居中标题 + 三张问题卡；默认展示“不知道能报什么 / 资格判断不清楚 / 材料证据会打架”，鼠标悬停或键盘聚焦时切换为智小申对应解决方案。
- 事实边界：解决方案仅使用已确认的机会发现、资格诊断、证据核查引擎、跨 200 页材料核查、6 大类 14 项检查、无据标红和不承诺“包过”等 PRD 信息。
- 交互与响应式：桌面端悬停/聚焦切换，触屏端直接同时显示问题和方案，避免移动端无法触发 hover；标题居中。
- 涉及文件：`app/proposalpilot/page.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过，13 个页面/资源完成静态生成，设计令牌检查 `9/9`；`/proposalpilot` 返回 `200`，服务端 HTML 包含三张问题卡、三组解决方案和证据核查数据。

### 移除问题卡片操作提示小字
- 改动内容：删除三张问题卡片底部的“移入查看智小申解决方案”提示文字，并清理对应样式。
- 改动原因：用户明确不希望页面使用小字号文案解释悬停、点击或动画操作；后续交互应由视觉反馈自身表达。
- 协作记录：该约束已同步写入 `AGENTS.md`，供后续 agent 继续遵守。
- 涉及文件：`app/proposalpilot/page.tsx`、`app/globals.css`、`AGENTS.md`。

## 2026-08-10 · Codex

### 导航栏圆角增强
- 改动内容：共享导航栏外壳圆角从 `18px` 增加到 `28px`，保留原有半透明背景、模糊和阴影。
- 改动原因：让悬浮导航与蓝白页面之间的边界更柔和，提升独立悬浮感。
- 涉及文件：`components/Nav.tsx`。

### 科小文内测区动态背景纹路
- 改动内容：为 `/scholarpilot` 底部内测 CTA 增加低亮度网格与同心弧线背景；指针经过时，鼠标附近的蓝色纹路局部亮起并随指针移动，背景纹路同时缓慢漂移。
- 交互约束：不增加操作提示小字；触屏设备使用静态局部高亮，`prefers-reduced-motion` 下关闭纹路漂移动画。
- 涉及文件：`components/ScholarEarlyAccess.tsx`、`app/scholarpilot/page.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过，13 个页面/资源完成静态生成，设计令牌检查 `9/9`；`/scholarpilot` 返回 `200`，HTML 包含动态纹路组件和内测 CTA。

### 科小文内测区网格点击散开
- 改动内容：移除内测 CTA 背景中的同心弧线，只保留网格；点击空白背景时，网格分裂为十二个片段向外散开并淡出，随后恢复为完整网格。CTA 按钮点击不触发背景动画。
- 动效边界：鼠标局部高亮继续保留；触屏与 `prefers-reduced-motion` 环境关闭散开动画并保留静态网格。
- 涉及文件：`components/ScholarEarlyAccess.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过，13 个页面/资源完成静态生成，设计令牌检查 `9/9`。

### 科小文网格点击动效校正为链路光线
- 改动内容：撤销网格碎片向外飞散的表现；点击空白背景后，以点击位置为源点，四条蓝色光线沿水平与垂直网格链路向外展开，短暂停留后沿原路径收回。
- 改动原因：用户期望“光线随链路散开再回来”，网格本身不应碎裂或位移。
- 涉及文件：`components/ScholarEarlyAccess.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过，13 个页面/资源完成静态生成，设计令牌检查 `9/9`；开发服务器 `/scholarpilot` 返回 `200`，页面包含四条链路光线且已移除旧碎片结构。

### 科小文背景改为真实交互网格
- 改动内容：移除 CSS 遮罩高亮与独立十字光线，改用 Canvas 绘制完整背景网格。鼠标经过时，指针附近的真实横纵网格线段按距离发亮；点击后，亮度波峰按网格曼哈顿距离沿连接向外传播，再沿同一网格路径收回。
- 改动原因：用户强调交互对象应是“背景网格本身”，而不是叠加在网格上方的独立光线。
- 性能处理：Canvas 像素比上限为 `2`，仅绘制可见网格线段，尺寸通过 `ResizeObserver` 同步。
- 涉及文件：`components/ScholarEarlyAccess.tsx`、`app/globals.css`。
- 验证结果：停止开发服务器后重新执行 `npm.cmd run check`，13 个页面/资源静态生成成功，设计令牌检查 `9/9`；重新启动前端后 `/scholarpilot` 返回 `200`，HTML 包含 Canvas 网格且不再包含旧独立链路结构。

### 智小申复用真实交互网格
- 改动内容：将 Canvas 网格背景抽为共享 `InteractiveGridBackground`，应用到 `/proposalpilot` 底部 CTA；移除智小申原有静态十字纹路、漂移色块和信号点。
- 改动原因：用户要求把科小文确认的“鼠标影响真实网格、点击后沿网格传播再回收”交互应用到智小申页面。
- 涉及文件：`components/InteractiveGridBackground.tsx`、`components/ScholarEarlyAccess.tsx`、`app/proposalpilot/page.tsx`、`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过；13 个页面/资源完成静态生成，设计令牌检查 `9/9`；`/proposalpilot` 返回 `200`，HTML 包含 Canvas 网格背景。
## 2026-08-10 · Codex

### 科小文协作时间轴动效美化
- 改动内容：为 `/scholarpilot` 的“协作方式”时间轴增加低亮度基础轨道、随轨道向下循环的蓝色流光、阶段进度亮线、完成/当前/待推进状态层级，以及当前节点的扩散脉冲和文字同步位移反馈；移动端沿用单列布局。
- 改动原因：原时间轴信息层级偏弱，节点与阶段缺少连续的视觉反馈，需要让研究者、科小文与共同完成三个交接阶段更容易被理解和关注。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1440x900` 与 `412x915` 检查无横向溢出、无控制台错误；`prefers-reduced-motion: reduce` 下流光与节点动画关闭。
- 涉及文件：`components/ScholarJourney.tsx`、`components/ScholarJourney.module.css`、`scripts/qa-scholar-handoff.cjs`。
## 2026-08-10 · Codex

### 科小文“协作方式”区改为文字加效果画面
- 改动内容：仅重构 `/scholarpilot` 的“协作方式”区域，将原竖向时间轴改为左侧三步文字选择器与右侧概念效果画面；研究者决策、科小文整理草稿、共同修改确认三种画面自动轮播，并支持鼠标、点击与键盘切换。
- 改动原因：按用户截图确认，目标是协作区而非上方科研写作流程；新的图文结构比单纯节点时间轴更容易展示每个交接阶段对应的实际工作画面。
- 素材说明：当前会话无内置 `image_gen` 且未配置 `OPENAI_API_KEY`，因此先使用可替换的代码原生概念效果画面，没有伪称为 `gpt-image-2` 产物；后续正式产品截图或生成图可直接替换右侧画面。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1440x900` 与 `412x915` 验证每次仅显示一张效果画面、无横向溢出、无控制台错误，减少动效模式下关闭切换过渡和光标动画。
- 涉及文件：`components/ScholarJourney.tsx`、`components/ScholarJourney.module.css`、`scripts/qa-scholar-handoff.cjs`。
## 2026-08-10 · Codex

### 智小申 G1-G6 门控区改为纸片叠放图文页面
- 参考来源：用户提供视频 `E:/视频/2026-08-07 17-51-13.mp4`。吸收其“左侧文字 + 右侧产品画面 + 多张内容页滚动吸顶覆盖”的交互节奏，不复制参考站品牌、文案或产品界面。
- 改动内容：将 `/proposalpilot` 原六格门控横向轨道重构为六张全宽图文纸片；桌面端滚动时依次吸顶并覆盖前一张，顶部保留纸边层次，移动端降级为连续图文。G4 使用更明确的蓝色边框与顶部标记突出“不可省略”。
- 文案校正：按 `_prd_zhishen.md` 权威定义重新展开 G1 路线图确认、G2 诊断与 Go/No-Go、G3 关键事实确认、G5 一键修订授权、G4 材料定稿、G6 退回修复；展示顺序按实际工作流采用 `G1 → G2 → G3 → G5 → G4 → G6`，不沿用旧版不准确的简化名称。
- 图片说明：为六张纸片建立固定图片路径并生成可替换的本地概念效果图。当前会话无内置 `image_gen` 且未配置 `OPENAI_API_KEY`，因此这些图片不是 `gpt-image-2` 产物；后续可直接覆盖同名文件为正式生成图或真实产品截图。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1440x900` 验证多张纸片同时保持 `sticky` 顶部层级并逐张覆盖，在 `412x915` 验证六张纸片均正常渲染、无横向溢出、移动端使用连续布局；减少动效模式无纸片动画，控制台无错误。
- 涉及文件：`components/ProposalSections.tsx`、`app/globals.css`、`public/images/proposal-gate-*-placeholder.png`、`scripts/generate-proposal-gate-placeholders.cjs`、`scripts/qa-proposal-gate-stack.cjs`。
## 2026-08-10 · Codex

### 智小申门控纸片矮屏提前覆盖修复
- 改动内容：将门控纸片之间的停留距离从短间隔扩展为独立滚动区，并增加末张 G6 的底部停留空间；新增桌面矮屏紧凑模式，在视口高度不超过 `700px` 时压缩纸片排版、收起辅助三点清单，同时保留标题、说明、确认角色、阶段交付和效果图。
- 改动原因：原纸片内容高度在矮屏下超过可视区域，下一张纸片会在当前纸片底边出现前开始覆盖，G6 到达后也会过早进入下一章节。
- 验证结果：Playwright 在 `1000x443` 视口确认 G4 位于 `92px–433px`、下一张 G6 顶部仍在 `690px`；G6 吸顶后位于 `96px–437px`，整张纸片底边在视口内，门控区底部仍保留 `319px` 滚动空间。`1440x900` 叠放效果与 `412x915` 连续移动布局保持正常，无横向溢出和控制台错误。
- 涉及文件：`app/globals.css`、`scripts/qa-proposal-gate-stack.cjs`。
## 2026-08-10 · Codex

### 移除 G4 纸片顶部蓝线
- 改动内容：删除 G4 重点纸片顶部的蓝色内嵌描边，仅保留浅蓝边框和阴影层级。
- 改动原因：顶部描边在纸片叠放时呈现为突兀的横向蓝线，与纸张视觉不一致。
- 涉及文件：`app/globals.css`。
## 2026-08-10 · Codex

### 调整门控纸片覆盖节奏
- 改动内容：将纸片之间的滚动间距从 `58vh` 收回到 `24vh`。当前纸片仍会完整显示，下一张随后从视口底部进入并直接覆盖，不再出现两张纸片上下分离的长空档。
- 改动原因：用户要求页面 6 应覆盖页面 5 后再继续向下滚动，而不是先看到页面 5 底边、空白间隔和独立的页面 6。
- 涉及文件：`app/globals.css`。
## 2026-08-10 · Codex

### 压缩智小申常见问题卡片空白
- 改动内容：桌面端问题卡片高度由 `320px` 降为 `270px`，内边距同步压缩；正面标题改为紧随编号区显示，背面解决方案重新分配标题、正文与底部流程提示的位置。
- 改动原因：原卡片使用上下两端对齐，短文案被推到卡片底部，形成大面积无意义空白。
- 涉及文件：`app/globals.css`。
## 2026-08-10 · Codex

### 短视口门控文字显示修正
- 改动内容：短屏模式下 G1-G6 纸片改为顶部对齐，避免下一张纸片进入时标题仍处于垂直居中位置而被遮挡。
- 改动原因：用户截图显示 G3/G5 标题在低高度视口中出现下半段裁切，视觉上像文字异常。
- 验证结果：待运行类型检查与短屏截图验证。

### G6 最后一页吸顶时序修正
- 改动内容：移除 G4 与 G6 之间额外的 `24vh` 纸片间距，使 G6 进入后直接覆盖 G4 并到达吸顶位置。
- 改动原因：用户截图显示 G6 已进入视口但停留在页面中段，最后一页未及时完成覆盖。
- 验证结果：待运行类型检查与桌面视口验证。

### 门控编号与流程序号歧义修正
- 改动内容：删除 G1-G6 圆形门控编号旁重复的 `01-06` 流程序号，只保留“路线图生成后”“质检提出修订后”等触发时点。
- 改动原因：实际流程为 `G1 → G2 → G3 → G5 → G4 → G6`，原来的 `G5 + 04`、`G4 + 05` 容易被理解为文字编号错误。
- 验证结果：待运行类型检查。

### 首页深色宣言区强化页面上推动效
- 改动内容：将首页深色宣言区桌面端顶部圆角由 `36px` 增至 `56px`，移动端由 `24px` 增至 `34px`；滚动进入时增加从 `94%` 到完整尺寸的连续放大、`156px` 上推位移和阴影变化，让深色页面产生从下方推入并覆盖浅色首屏的重量感。
- 文字动效：公司标签、方法信号、宣言文案、源点视觉与事实栏按内容进入视口的顺序分别淡入并上移，避免整区内容同时弹出。
- 无障碍：在 `prefers-reduced-motion` 环境下关闭上推、缩放和分段淡入，内容保持完整可见。
- 涉及文件：`app/globals.css`。
- 验证结果：`npm.cmd run check` 通过；Next.js 完成 13 个静态页面/资源生成，TypeScript 检查与设计令牌检查 `9/9` 通过。
## 2026-08-10 · Codex

### 首页深色页面放大幅度增强
- 改动内容：将首页深色宣言区上推动画的初始缩放由 `94%` 调整为 `64%`，中段缩放由 `98.5%` 调整为 `91%`，最终保持 `100%`，形成更明显且连续的由小变大过程。
- 涉及文件：`app/globals.css`。
- 验证结果：停止 Turbopack 开发服务后执行 `npm.cmd run check` 通过，13 个页面/资源完成静态生成，设计令牌检查 `9/9`；使用标准 `next dev` 重启后 `/proposalpilot` 与 `/scholarpilot` 均返回 `200`，两页 HTML 均包含共享 Canvas 网格组件。
## 2026-08-10 · Codex

### 全站 Lenis 惯性滚动与首页 GSAP 推入时间轴
- 改动内容：桌面端全站接入 Lenis，使用 `lerp: 0.075`、`wheelMultiplier: 0.85` 和 `smoothWheel` 平滑滚轮输入，并通过 GSAP ticker 驱动 Lenis、同步 `ScrollTrigger.update()`。
- 首页动效：深色宣言区改为 GSAP ScrollTrigger `scrub: 0.85` 时间轴，保持从下方上推和 `64%` 到 `100%` 的连续放大，并让标签、方法信号、宣言文案、源点视觉和事实栏依次淡入。
- 兼容策略：`768px` 以下设备保留原生滚动；`prefers-reduced-motion` 下不初始化 Lenis 和 GSAP 宣言区时间轴；桌面端启用 GSAP 时关闭原 CSS `view()` 动画，避免 transform 竞争。
- 涉及文件：`components/SmoothScroll.tsx`、`components/HomeManifestoMotion.tsx`、`app/layout.tsx`、`app/page.tsx`、`app/globals.css`、`package.json`、`package-lock.json`。
- 验证结果：`npm.cmd run check` 通过；13 个静态页面/资源生成，TypeScript、Next.js 构建和设计令牌检查 `9/9` 通过；开发服务首页返回 `200`。
## 2026-08-10 · Codex

### 首页深色区初始缩放调整为 84%
- 改动内容：将深色页面从下方上推时的初始缩放统一调整为 `84%`，中段缩放调整为 `95%`，最终平滑过渡到 `100%`。
- 同步范围：GSAP ScrollTrigger 时间轴与 CSS 回退关键帧保持一致。
- 涉及文件：`app/globals.css`、`components/HomeManifestoMotion.tsx`。
- 验证结果：`npx.cmd tsc --noEmit` 通过，首页开发服务返回 `200`。

## 2026-08-10 · Codex

### 首页第一阶段黑白荧光黄改版
- 参考来源：用户指定 `https://www.dayos.com/` 的滚动节奏与产品展示方式，以及 `https://reflect.app/` 的字体、黑白色彩和细描边风格。只吸收设计规律，不复制品牌、文案或产品界面。
- 改动范围：按用户确认，本阶段只重构首页 Hero 与公司能力区。后续工作流、产品矩阵、FAQ 和底部 CTA 暂时保留现状；全站令牌替换推迟到第一阶段视觉验收后。
- Hero：改为占满首屏的居中文案与横向源点网络，主 CTA 为“选择产品”；荧光黄版 logo 形状作为动画源点，连接“企业项目申报 / 智小申 ProposalPilot Agent”和“学术写作 / 科小文 ScholarPilot Agent”。场景节点点击后定位并短暂高亮对应产品卡，随后把焦点移到详情入口。
- 公司能力区：改为近黑背景与三项等权能力模块，分别说明“理解专业工作 / 贯穿完整流程 / 人保留关键决策”，并明确“以智小申为例”，避免把智小申的成熟能力泛化到科小文。
- 视觉与动效：页面第一阶段使用 `#F5F5F2`、`#0A0A0A`、`#D7FF00`，圆角限制为 `4px`；源点轻微自动呼吸，桌面节点随鼠标偏移 `6-10px`，区块进入和黑白背景变化由 CSS scroll-driven animation 控制；`prefers-reduced-motion` 下关闭自动、鼠标和滚动动画。
- 导航：保持原结构和链接，首页 CTA 临时改为“选择产品”，首页导航切换到黑白荧光黄外观；导航和页脚使用的原青绿色 logo 文件未修改。
- 图标：用户原确认使用 Lucide，但两次安装请求均被审批服务 `503` 拦截；经用户确认，本阶段临时使用项目已有 Phosphor 图标，后续审批恢复后再统一替换，不改变布局。
- 涉及文件：`app/page.tsx`、`app/globals.css`、`components/HomeSourceNetwork.tsx`、`components/HomeProductJump.tsx`、`components/ProductMatrix.tsx`、`components/Button.tsx`、`components/Nav.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；开发服务器 `http://localhost:3000` 首页返回 `200`，服务端 HTML 包含新 Hero、源点网络、能力区和产品矩阵焦点目标。`npm.cmd run check` 的 TypeScript 阶段通过，但生产构建因沙箱无法访问 Google Fonts 失败；联网重跑和 Edge 截图请求均被审批服务 `503` 拦截，未完成生产构建与自动截图验收。

### 黑白页面覆盖切换保留
- 改动内容：将此前首页深色页面从下方推入并覆盖上一页的动效抽为可复用 `home-color-page--cover` 规则，当前应用于白色 Hero 进入黑色公司能力区。黑页从下方进入时逐步放大并覆盖白页，桌面重叠 `72px`，移动端重叠 `48px`。
- 后续约定：工作流、产品矩阵、FAQ 和底部 CTA 完成黑白交替改版时，每个相邻页面区块沿用同一覆盖规则，不为不同区块重新发明转场。
- 无障碍：`prefers-reduced-motion` 下取消覆盖位移、缩放和负边距，恢复普通连续页面。
- 涉及文件：`app/page.tsx`、`app/globals.css`、`CHANGELOG.md`。

### 覆盖页推进感与文字淡入增强
- 改动内容：覆盖页桌面顶部圆角调整为 `36px`，移动端为 `28px`；进入视口时从 `84%` 缩放和约两倍重叠距离开始，经过 `95%` 中间状态放大到 `100%`，同步增强上推阴影。
- 文字动效：公司能力标题先淡入，三项能力随后按顺序由下向上淡入，避免页面和全部文字同时出现。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；开发服务器 `http://localhost:3000/` 返回 `200`。

### 覆盖页滚动动效兼容修复
- 问题原因：内置浏览器未实际执行 CSS `animation-timeline: view()`，导致覆盖页直接保持最终状态，滚动时看不到变化。
- 改动内容：新增 `HomePageCoverMotion`，使用 `requestAnimationFrame` 根据覆盖页进入视口的实际进度更新 CSS 变量；缩放、上推、圆角、阴影与文字依次淡入都由同一进度驱动，不再依赖 CSS 滚动时间轴支持。
- 无障碍：检测到 `prefers-reduced-motion` 时不启用滚动计算，页面保持完整静态状态。
- 涉及文件：`components/HomePageCoverMotion.tsx`、`app/page.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；热更新后的首页返回 `200`，服务端输出已包含新的滚动驱动组件。

### 公司能力区移除底部小字标签
- 改动内容：删除三项公司能力模块底部的荧光黄等宽小字，包括场景、流程和责任边界标签，不再以小字号补充说明重复正文信息。
- 改动原因：用户反馈该形式割裂版面、视觉效果不够高级；能力信息改由标题和正文完整表达。
- 涉及文件：`app/page.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，服务端输出中已不存在三组被删除的小字内容。

### 公司能力文字淡入触发位置修复
- 问题原因：标题和能力内容原先共用覆盖页整体进度，文字进入视口前透明度已经接近完成，因此实际滚动时难以察觉淡入。
- 改动内容：标题和能力列改为根据各自元素进入视口的位置计算透明度与纵向位移；标题独立淡入，三项能力按滚动进度错峰出现，并提高初始位移以增强可见反馈。
- 涉及文件：`components/HomePageCoverMotion.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；热更新后的首页返回 `200`。

### 公司能力区标题桌面端单行修复
- 问题原因：标题容器最大宽度仅 `760px`，导致正常 PC 宽度下“专业能力，从真实工作开始。”被挤到两行。
- 改动内容：桌面标题容器扩展到 `1180px` 并保持单行；`821px-1100px` 使用 `52px` 固定字号，手机端恢复自然换行，避免文字溢出。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`。
## 2026-08-10 - Homepage cover-motion refinement

### Hero to company-capability transition
- Scope: homepage `/` Hero and company capability section only. No product routes, metadata, Schema, shared navigation, or legacy manifesto component were changed.
- Change: `HomePageCoverMotion` now uses an explicit three-stage cover scale: `84%` at entry, `95%` at the midpoint, and `100%` once the dark capability page settles. The same requestAnimationFrame progress continues to drive the upward push, rounded top edge, shadow, and staggered heading/item fades.
- Accessibility and stability: a live `prefers-reduced-motion` change now restores the normal static layout immediately; the animated cover is paint-contained while it is moving to reduce redraw work.
- Verification: `npx.cmd tsc --noEmit` passed; the running development server returned `200` for `/`, and its server-rendered HTML contains both the confirmed Hero tagline and company-capability heading.

## 2026-08-10 · Codex

### 首页第二阶段：工作流、产品矩阵、FAQ 与联系区
- 设计延续：继续借鉴 Dayos 的滚动节奏和产品呈现方式，以及 Reflect 的黑白排版、细描边与克制色彩；不复制参考站的品牌、界面或内容。首页形成白色 Hero → 黑色公司能力 → 白色工作流 → 黑色产品矩阵 → 白色 FAQ → 黑色联系区的明确滚动节奏。
- 工作流：移除与公司能力重复的旧“信息分散 / 流程断裂 / 责任不清”区块，新增公司级通用流程“源点 → 发现 → 判断 → 执行 → 交付”，并用企业项目申报和学术写作说明同一方法如何进入不同场景，不伪造产品界面。
- 产品矩阵：智小申与科小文改为同面积、同视觉权重模块；保留产品中英文名、定位、介绍、三项能力与“了解详情”入口；移除“主推 / 即将上线”状态标签和“立即体验”文案。两张卡继续保留 `product-proposal`、`product-scholar` 与 `.home-product-matrix-card__cta` 契约，首屏源点节点可滚动定位、高亮并聚焦详情入口。
- FAQ：新增默认收起且允许多项同时展开的手风琴，内容围绕“溥源是谁、agent 如何工作、有哪些产品、与通用聊天工具的区别”；首页同步加入 `FAQPage` JSON-LD。
- 联系区：首页底部新增黑色邮箱联系区，展示 `hello@puyuan.tech`，不提供未准备好的试用或申请入口。
- 共享入口：导航在首页与产品页统一显示“选择产品”，链接到首页产品矩阵；页脚移除“免费试用”，联系栏只保留企业邮箱。真实试用入口准备好后再统一恢复。
- 动效与响应式：四个新区块复用 `home-color-page--cover` 覆盖转场；`HomePageCoverMotion` 新增通用内容淡入进度。桌面工作流横向展开，移动端转为纵向；产品矩阵在窄屏改单列；`prefers-reduced-motion` 下关闭自动流线与覆盖内容位移。
- 涉及文件：`app/page.tsx`、`app/globals.css`、`components/HomePageCoverMotion.tsx`、`components/HomeWorkflow.tsx`、`components/HomeFaq.tsx`、`components/ProductMatrix.tsx`、`components/Nav.tsx`、`components/Footer.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；`/`、`/proposalpilot`、`/scholarpilot` 均返回 `200`。首页服务端 HTML 包含工作流、两项产品目标、两处“了解详情”、四项 FAQ、`FAQPage` Schema、选择产品入口和联系邮箱，不再包含旧问题区、旧边界区、“主推”“立即体验”或“免费试用”。`verify:tokens` 静态检查通过 `8/9`，唯一未通过项是开发模式没有 `.next/static/css` 构建产物；无头 Edge 截图因本机自动审批服务返回 `503` 未能执行。

### 黑白双向覆盖层级修复
- 问题原因：所有覆盖页虽然共用缩放与上推动效，但只有首个黑色能力区设置了显式 `z-index`，后续区块依赖浏览器自动堆叠；在连续 transform stacking context 下，白页进入可见，而黑页进入时没有稳定压住上一段白色内容。
- 改动内容：为 Hero、公司能力、工作流、产品矩阵、FAQ、联系区设置 `1-6` 的递增层级，确保每一个后出现的页面都覆盖前一个页面，不再依赖颜色或默认绘制顺序。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，四个第二阶段区块均存在于服务端输出。

### 导航栏随页面背景自动切换
- 改动内容：Hero、公司能力、工作流、产品矩阵、FAQ 和联系区分别声明浅色或深色主题；`HomePageCoverMotion` 在滚动时检测实际覆盖到导航位置且层级最高的页面，并把主题同步到根节点。
- 视觉反馈：白色页面使用浅色半透明导航；黑色页面切换为近黑半透明导航、白色导航文字与深色下拉菜单。荧光黄 CTA 和原青绿色 logo 保持不变，主题切换使用 `320ms` 过渡。
- 改动原因：导航应与当前页面背景保持足够对比度，并跟随覆盖边缘切换，不能始终停留在浅色状态。
- 涉及文件：`app/page.tsx`、`components/HomeWorkflow.tsx`、`components/ProductMatrix.tsx`、`components/HomeFaq.tsx`、`components/HomePageCoverMotion.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，服务端输出包含 3 个浅色主题区块和 3 个深色主题区块；`git diff --check` 无补丁格式错误。

### 工作流区 PC 端标题排版修复
- 问题原因：工作流标题与说明采用左右分栏，标题列宽不足，正常 PC 端出现“交付。”单独落到第二行，说明文字同时被推到页面右侧，视觉重心分裂。
- 改动内容：PC 端改为居中单列标题，标题保持单行，说明置于标题下方并限制到 `760px`；流程轨道与标题的距离从 `104px` 收紧到 `82px`。`960px` 以下恢复自然换行。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，工作流标题正常存在于服务端输出。

### 产品矩阵差异化动态叙事重构
- 设计依据：按 `grill-me` 访谈确认，产品矩阵以“产品差异感”为主，保持同源共享框架与同等面积。智小申表现严谨的流程、证据和人工门控；科小文表现开放的知识关联与文章结构。
- 智小申动态图：材料证据节点从不同位置低速汇聚，连接到中心证据枢纽，随后经过荧光黄“人工确认”门控并形成交付文档；悬停卡片时动画加速。
- 科小文动态图：文献节点持续建立关联，连接线汇入文章结构，标题、章节与段落骨架按节奏生成；不展示具体论文内容或伪造产品界面，悬停时同步加速。
- 信息层级：视觉区域约占产品模块 `55%`，文字约占 `45%`；中文名为主标题，英文名改为正常字号副标题，不使用荧光黄小标签；取消三项能力列表，改为一段完整能力说明。
- 交互：整张产品模块可点击并支持键盘 `Enter`；拖选文字时不跳转；“了解详情”继续作为明确焦点入口，保留荧光黄描边和悬停填充反馈。首屏源点定位、高亮和聚焦契约保持不变。
- 响应式与无障碍：移动端压缩动态图比例和节点尺寸，内容保持完整可见；`prefers-reduced-motion` 下停止两套循环动画并显示静态结构。
- 涉及文件：`components/ProductMatrix.tsx`、`app/globals.css`、`CHANGELOG.md`。

### FAQ 问题行侧向入场
- 改动内容：FAQ 四个问题行根据各自进入视口的位置，从右侧约 `52px` 逐步移动到原位并淡入；不同问题按顺序错峰进入，答案展开交互不受影响。
- 改动原因：用户希望“问题”的入场动效从侧面进入，让 FAQ 区域的滚动叙事与其他区块区分开。
- 无障碍：`prefers-reduced-motion` 下问题行保持静态完整可见。
- 涉及文件：`components/HomePageCoverMotion.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，服务端输出包含两套产品动态图、人工确认节点、文章结构视觉和“了解详情”入口。

### 移除全站 Footer
- 改动内容：从共享 `app/layout.tsx` 中移除 Footer，不再在首页、智小申页和科小文页渲染截图中的旧深色页脚；首页第二阶段黑色联系区继续保留企业邮箱。
- 改动原因：用户明确要求删除该页脚区域，避免旧版蓝色页脚与当前黑白荧光黄视觉系统并存。
- 涉及文件：`app/layout.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；`/`、`/proposalpilot`、`/scholarpilot` 均返回 `200`，旧 Footer 文案不再出现在三条路由的服务端 HTML 中。

### 产品详情入口去按钮化
- 问题原因：新产品矩阵继续复用了旧 `.home-product-matrix-card__cta` 类，旧蓝色填充、圆角和阴影仍作用在“了解详情”上，形成与黑白荧光黄系统冲突的蓝色按钮。
- 改动内容：显式清除旧背景、阴影、圆角和扫光效果；详情入口改为荧光黄文字与细下划线，悬停时切换白色并让箭头向右上方移动。整张产品卡仍可点击，因此该入口只承担方向提示和键盘焦点作用。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，服务端输出保留两处“了解详情”入口。

### 产品动态图节点与连线秩序化
- 问题原因：两套动态图使用多组随机散布节点和斜向连线，视觉焦点分散，用户难以快速理解信息流向。
- 改动内容：智小申收束为三组证据输入沿左侧纵向排列，通过三条水平线连接到统一证据主轴，再进入中心枢纽、人工门控和交付文档；科小文同样使用三组文献输入、水平关联线和统一知识主轴，再通过单一桥接线生成文章结构。
- 视觉结果：节点数量减少，取消随机斜线，所有流程统一按“左侧输入 → 中央汇聚 → 右侧结果”阅读，保留默认低速与悬停加速。
- 涉及文件：`components/ProductMatrix.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，两套主轴和科小文桥接线均存在于服务端输出。

### 导航主题缓慢过渡与圆角统一
- 改动内容：导航浅色与深色主题的背景、边框和阴影切换时长调整为 `780ms`，文字与导航图标颜色使用 `620ms` 过渡，统一采用平缓的 `cubic-bezier(.16,1,.3,1)`。
- 圆角规则：导航外壳桌面端改为 `36px`，与页面覆盖层顶部圆角一致；移动端使用 `28px`，与移动端覆盖页一致。内部按钮和下拉菜单继续保持原有紧凑圆角。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`。

### 产品矩阵标题 PC 端排版修复
- 问题原因：产品矩阵标题与说明仍使用左右分栏，标题贴近左侧、说明悬在右侧，和刚统一的工作流标题结构不一致，形成明显的视觉断层。
- 改动内容：产品矩阵标题改为居中单列，说明放到标题下方并限制为 `760px`；常规 PC 宽度保持标题单行，`960px` 以下允许自然换行，`961px-1200px` 使用 `46px` 固定字号。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，产品矩阵标题存在于服务端输出。

### 首页导航预留区背景统一
- 问题原因：旧版全站蓝白样式仍为 `.site-nav + main` 设置 `#F5F8FE`，首页 Hero 已使用 `#F5F5F2`，导航下方的 `pt-20` 预留区因此形成一条浅蓝色块。
- 改动内容：首页存在 `.home-phase-one` 时，将导航后的 `main` 背景覆盖为 `var(--phase-white)`，与 Hero 和页面浅色区统一；不影响两个产品页现有背景。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`。

### 导航“选择产品”按钮自适应配色
- 改动内容：浅色页面下使用近黑背景、荧光黄文字和清晰边界；深色页面下自动切换为荧光黄背景、近黑文字。背景、边框和文字使用 `620ms` 过渡，与导航主题缓慢变色保持一致。
- 交互反馈：浅色状态悬停增加荧光黄细边缘反馈，深色状态悬停使用略深的荧光黄与轻微光晕；不使用旧蓝色阴影。
- 改动原因：`#D7FF00` 与 `#F5F5F2` 明度接近，原黄色按钮在白色导航中边界不清晰，自适应反转可同时保证两种背景下的对比度。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，选择产品入口正常存在。

### 首页内容页大标题格式与字数统一
- 文案规则：公司能力、工作流、产品矩阵、FAQ 和联系区统一为“4 字主题 + 6 字说明”的双句式，每个标题均为 10 个汉字，不计标点。品牌 Hero 的已确认定位句“从知识源头，驱动专业 agent”保持不变。
- 标题文案：`专业能力，始于真实工作。`、`统一流程，贯穿完整交付。`、`产品矩阵，进入专业流程。`、`常见问题，理解溥源方法。`、`联系溥源，开始专业协作。`。
- 排版规则：桌面端统一居中、`54px`、`1.12` 行高和 `590` 字重，副标题统一位于标题下方且最大宽度 `760px`；`1200px` 以下统一 `46px`，移动端统一 `38px` 并允许自然换行。
- FAQ 调整：从左右分栏改为与其他内容页一致的居中标题结构，问题列表在标题下方以 `960px` 宽度展开。
- 涉及文件：`app/page.tsx`、`components/HomeWorkflow.tsx`、`components/ProductMatrix.tsx`、`components/HomeFaq.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，五个统一标题均存在于服务端输出。

### 首页内容页大标题移除结尾句号
- 改动内容：删除公司能力、工作流、产品矩阵、FAQ 和联系区五个大标题末尾的中文句号，保留中间逗号与统一文案结构。
- 涉及文件：`app/page.tsx`、`components/HomeWorkflow.tsx`、`components/ProductMatrix.tsx`、`components/HomeFaq.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，五个旧带句号标题均不再存在。

### 产品卡边线近距荧光反馈
- 改动内容：产品卡实时计算鼠标到四条边线的最近距离，仅在距离边线约 `96px` 内逐步点亮鼠标附近的荧光黄描边；越靠近边线亮度越高，离开范围后淡出。
- 视觉调整：原整块径向泛光降低到 `4.5%` 强度，主要反馈集中在边线本身；发光范围约 `150px`，并增加轻微黄色光晕，不改变卡片尺寸和布局。
- 无障碍与设备：无悬停设备不会触发；`prefers-reduced-motion` 下关闭跟随边线发光。
- 涉及文件：`components/ProductMatrix.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，两张产品卡正常输出。

### Hero 标题 agent 渐变强调
- 改动内容：仅为品牌定位句末尾的 `agent` 增加从近黑 `#171717` 到黄绿色 `#A9C900` 的文字渐变，并保留轻微深色描边以保证白色背景上的字形清晰度；其余标题文字保持纯黑。
- 涉及文件：`app/page.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；首页返回 `200`，渐变文字节点正常输出。
## 2026-08-10 · Codex

### 科小文完整产品页深色场景化重构
- 改动内容：将 `/scholarpilot` 从浅色占位页升级为深色完整产品介绍页，新增全屏产品工作台 Hero、四段滚动覆盖场景、三项已确认能力、科研工作台规划能力、个人与机构双场景、FAQ 和三类转化入口。
- 动效设计：使用 GSAP ScrollTrigger 将研究工作台、文献综述、证据关联初稿、检查与投稿准备四个场景绑定滚动进度；上一场景随下一场景进入缩小、压暗并退入景深，移动端和减少动效模式回退为连续阅读。
- 内容边界：保留选题与文献综述、初稿生成、润色与投稿准备三项已确认能力；论文检索、PDF 阅读、引用管理、私有化部署和批量席位均明确标注为规划方向。
- 视觉资产：内置生图工具已生成四张 ScholarPilot 深色产品界面预览，但当前环境未返回可复制的本地文件路径；页面先使用可运行的高保真产品界面场景，后续真实截图可直接替换对应视觉区。
- 验证结果：`npx.cmd tsc --noEmit` 通过；短屏规则已增加顶部对齐、隐藏冗余描述和压缩列表间距；截图脚本因项目未安装 `playwright` 无法运行；生产构建受 Google Fonts 网络访问限制未完成。

### ScholarPilot v2 短屏与按钮主题修复
- 改动内容：在 `1000x443` 等短桌面视口中让滚动场景内容顶部对齐，隐藏冗余说明并压缩标题、列表和界面高度；在全局样式末尾提高深色页选择器优先级，确保 CTA 使用 ScholarPilot 青绿色而不是旧版蓝白主题。
- 改动原因：短屏场景文案被垂直居中裁切，旧版 `.scholar-page` 规则覆盖了新产品页按钮颜色。
- 涉及文件：`components/ScholarJourney.module.css`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；生产构建因 Google Fonts 网络访问被环境拦截未完成，Playwright 截图脚本因依赖未安装未运行。

### ScholarJourney 恢复纸片叠加原版并统一首页色调
- 改动内容：恢复三阶段左侧叙事与右侧固定稿纸的原版 ScholarJourney 结构，保留研究者确认、初稿组织和投稿准备三个状态切换；将页面强调色从青绿色改为首页使用的黑白与 `#d7ff00` 荧光黄体系。
- 改动原因：用户要求撤回新版四场景叠加方式，并指出科小文色调与首页不一致。
- 涉及文件：`components/ScholarJourney.tsx`、`components/ScholarJourney.module.css`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；`/scholarpilot` 返回 `200`。

### ScholarJourney 稿纸阶段同步修复
- 改动内容：将三阶段稿纸同步从 `IntersectionObserver` 改为基于视口中心距离的滚动位置计算，并使用 `requestAnimationFrame` 合并更新；兼容全站 Lenis 平滑滚动。
- 改动原因：旧监听在当前滚动环境中持续停留于 `01 / 03`，稿纸内容没有随第二、第三阶段向下滚动而切换。
- 涉及文件：`components/ScholarJourney.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；`/scholarpilot` 返回 `200`。

### ScholarJourney 稿纸跟随滚动修复
- 改动内容：移除科小文页面根容器的 `overflow: clip`，恢复右侧稿纸 `position: sticky` 所需的正常页面滚动上下文；Hero 仍保留自身局部裁切，不产生横向溢出。
- 改动原因：根容器裁切导致纸片无法在三段流程范围内随页面下移并保持吸顶。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；`/scholarpilot` 返回 `200`。
## 2026-08-10 - Homepage workflow scroll narrative

### Pinned five-stage workflow and business tabs
- Scope: redesigned the homepage workflow section only, while preserving the confirmed black/white/acid-yellow system, existing routes, GEO content, and product boundaries.
- Desktop motion: the workflow now occupies approximately `350vh`. Its presentation is fixed while the user scrolls through `源点 → 发现 → 判断 → 执行 → 交付`, then releases at the end of the section. Completed nodes and connectors stay highlighted; the current node emits restrained acid-yellow rings and a light beam travels toward the next stage.
- Business modes: added accessible `企业项目申报` and `学术写作` tabs. Switching modes fades the five descriptions out and back in without resetting the current scroll stage. Proposal copy follows the ProposalPilot facts; ScholarPilot copy remains limited to the confirmed topic/literature, draft, polishing, and submission-preparation scope.
- Mobile: below `901px`, the fixed desktop presentation becomes a vertical five-stage flow. Scroll position progressively highlights nodes without creating horizontal overflow or scroll locking.
- Compatibility: the shared page-cover driver now releases its transform containing block after a cover page finishes landing, allowing fixed workflow content to attach to the viewport. `prefers-reduced-motion` restores a static, fully visible workflow.
- Verification: `npx.cmd tsc --noEmit` passed. Browser automation at `1440x900` confirmed stage `2`, three accumulated active nodes, fixed positioning, tab copy replacement, no console errors, and no horizontal overflow. At `412x915`, stage `3` was active in the vertical layout with static positioning, no console errors, and no horizontal overflow. Screenshots: `qa-home-workflow-desktop.png` and `qa-home-workflow-mobile.png`.
## 2026-08-10 - Workflow content transition refinement

### Scroll now changes the story, not only the lighting
- Change: added a stage narrative panel below the workflow rail. Each scroll stage now replaces the active stage explanation and its concrete output, while the five-node rail remains as the orientation layer.
- Motion: the narrative panel fades and lifts into place on stage changes; the active stage title and mobile stage copy also fade in. Business tab changes continue to fade old copy out before replacing it.
- Content boundary: ProposalPilot copy stays within confirmed government-project申报 facts; ScholarPilot remains limited to topic/literature, draft, polishing, and submission preparation.
- Verification: TypeScript passed. Browser automation confirmed desktop stage `2` with fixed positioning, story phase `判断`, dynamic detail text, output `初稿结构`, no console errors, and no horizontal overflow; mobile stage `3` remained vertical with no horizontal overflow.

## 2026-08-10 - Workflow rail alignment and richer stage content

### The rail now aligns to node centers and every stage changes three content layers
- Alignment: replaced the hard-coded desktop rail height with a shared calculation based on node top padding and marker diameter. The connector now passes through the marker center at normal and low-height desktop breakpoints, while the existing mobile vertical alignment remains unchanged.
- Content: expanded each ProposalPilot and ScholarPilot stage from one explanation and one result into `输入依据 / Agent 动作 / 阶段成果`. All five stages now replace their source material, processing action, and concrete result as scroll position changes.
- Motion: the four narrative columns enter with restrained staggered fade-and-lift motion on stage changes. Business-tab changes retain the existing fade-out and fade-in behavior, and reduced-motion mode still disables animation.
- Product boundaries: ProposalPilot copy uses confirmed government-project申报 facts, including the six主管部门 scope, hard-gate judgment, cross-200-page evidence checking, 6 categories / 14 checks, and G4 material-finalization gate. ScholarPilot remains limited to topic/literature work, draft generation, polishing, and submission preparation.
- Verification: `npx.cmd tsc --noEmit` passed and the homepage returned HTTP `200`. The browser QA script was updated to report rail-center delta and the new input/action fields, but could not run because the local project currently has no `playwright` module installed.

## 2026-08-11 - ScholarJourney single paper follow

### One manuscript component follows the journey
- Change: kept one manuscript component and added a scroll-progress translation inside the right-side container; no duplicate manuscript pages are rendered.
- Binding: the single manuscript continuously moves downward through the journey while its research-note content remains linked to the active stage.
- Reason: the requested effect is visible downward movement of one component, not a stationary sticky card or three copied pages.
- Visual alignment: raised the desktop paper stack so its top edge reads clearly as the component head instead of appearing suspended below the frame header.
- Position refinement: lifted the complete desktop manuscript component by `72px` while preserving its scroll-progress movement; mobile remains in normal flow.
- Verification: `npx.cmd tsc --noEmit` and the ScholarPilot HTTP response are checked after the correction.
## 2026-08-11 - Hero gradient glyph clipping fix

- Change: added right-side breathing room to the gradient `agent` word, removed the text stroke, and allowed the hero heading overflow to remain visible.
- Reason: the final glyph appeared visually clipped after the gradient treatment was introduced.
- Verification: `npx.cmd tsc --noEmit` passed; the local homepage server was unavailable during the HTTP check.

## 2026-08-11 - 全站语义文字淡入

### 各页面文字随视口进入淡入
- 改动内容：新增共享 `SiteTextReveal` 客户端组件，并在根布局中统一启用；对 `main` 内的标题、正文、列表、定义项、引文与图注使用 `IntersectionObserver` 做一次性透明度淡入，同一区块按 `45ms` 轻微错峰，动画时长为 `720ms`。
- 兼容策略：淡入只改变透明度，不叠加位移或缩放，避免干扰首页黑白覆盖转场、智小申白纸堆叠、工作台、Canvas 与 3D 动效；已有独立文字时间轴的区域明确排除。路由切换后重新扫描当前页面，`prefers-reduced-motion` 下直接显示全部文字。
- 改动原因：让不同页面的文字出现节奏一致，同时维持现有复杂滚动和产品界面动效的稳定性。
- 涉及文件：`components/SiteTextReveal.tsx`、`app/layout.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `/`、`/proposalpilot`、`/scholarpilot`、`/about`、`/faq` 验证首屏标题可见、滚动后下方文字由 pending 进入 visible、控制台无错误；`1440x900` 与 `390x844` 均无横向溢出；减少动效模式下 pending 与隐藏文字数量均为 `0`。

## 2026-08-11 - 智小申规划流程手动切换与色调统一

### 自动轮播保留，并允许直接选择任意环节
- 改动内容：八环节规划流程的每张卡片支持鼠标点击与键盘 Enter/空格切换；被选卡片平滑移动到视口中央，并从当前环节重新开始 `3200ms` 自动播放计时。
- 视觉调整：移除偏蓝灰的背景、边框和正文色，统一为中性近黑、暖白、灰色描边与 `#D7FF00` 荧光黄；补充悬停、键盘焦点和按下反馈。
- 改动原因：用户需要在保留自动演示的同时主动查看指定步骤，并让该模块与智小申页面已经确认的黑白荧光黄体系一致。
- 涉及文件：`components/ProposalConceptFlow.tsx`、`components/ProposalConceptFlow.module.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `/proposalpilot` 实测点击后激活环节由 `0` 切换为 `1`，等待自动播放周期后继续切换为 `2`；控制台无错误且页面无横向溢出。

## 2026-08-11 - 科小文能力卡片基线对齐

- 改动内容：将能力卡片状态标签从自动撑开定位改为固定上间距，并为标题统一预留两行高度；移动端恢复自然内容高度以避免无意义留白。
- 改动原因：不同标题行数会改变自动剩余空间，导致状态标签、标题和正文纵向错位。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1440x900` 下确认三张卡片的状态标签、标题和正文顶部坐标分别完全一致，页面无横向溢出。

## 2026-08-11 - 三页问答样式统一

- 改动内容：抽出共享 `FaqAccordion`，首页、智小申与科小文均使用同一套原生折叠问答结构、分隔线、加号旋转和答案下落动画；智小申仅使用深色对比版本以适配所在页面。
- 交互调整：首页问答展开后不再将问题文字或图标切换为绿色，也不再填充绿色背景；状态变化只保留标题行的轻微位移、加号旋转和答案展开动画。
- 改动原因：消除三个产品/公司页面在问答结构和反馈方式上的差异，并按首页的简洁阅读节奏统一。
- 涉及文件：`components/FaqAccordion.tsx`、`components/HomeFaq.tsx`、`app/proposalpilot/page.tsx`、`app/scholarpilot/page.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `/`、`/proposalpilot`、`/scholarpilot` 的桌面与 `390px` 移动端确认首项可展开、加号旋转、展开前后文字颜色一致、无控制台错误和横向溢出。

## 2026-08-11 - 首页工作流连接线对齐

- 改动内容：工作流连接线的左右端点和垂直位置改为由运行时读取首尾节点圆心后计算，不再依赖固定百分比偏移。
- 改动原因：网格列宽、响应式尺寸或字体变化会让固定 `10%` 起止位置与实际节点圆心产生偏差，造成线段看起来断开或未对齐。
- 涉及文件：`components/HomeWorkflow.tsx`、`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1440x900` 下实测连接线相对首尾节点圆心的水平误差均为 `1px`、垂直误差为 `1.5px`，无控制台错误和横向溢出。

## 2026-08-11 - 首页产品矩阵短窗口适配

- 改动内容：`721px-960px` 的 PC 窄窗口继续保持双列产品卡，不再提前堆叠为单列；高度不超过 `700px` 时压缩区块留白、标题间距、产品视觉和内容区，并将产品描述限制为三行，详情入口保持可见。
- 改动原因：分屏使用时浏览器宽度约 `870px`，旧 `960px` 断点会让两张完整产品卡上下堆叠，导致区块远超 PC 可视范围。
- 设备边界：手机端 `720px` 及以下继续使用单列完整阅读布局，正常高度桌面不使用紧凑尺寸。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `870x500` 下确认产品区为 `416px + 416px` 双列、两卡同一行、区块高度约 `395px` 且无横向溢出；`390x844` 下仍为单列且无横向溢出，控制台无错误。

## 2026-08-11 - Hero title width refinement

- Change: reduced the Hero title scale at medium desktop widths and added stable horizontal breathing room to the full heading.
- Reason: the single-line title was too close to the viewport edge, making the final gradient glyph appear swallowed.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - Hero parent clipping removal

- Change: removed the Hero section's `overflow: hidden` clipping and kept clipping scoped to the source-network visual.
- Reason: the heading span could remain visually cut by its parent even when the heading itself allowed visible overflow.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - Hero agent glyph readability fix

- Change: replaced the transparent gradient text treatment on `agent` with a solid olive accent and retained a small right-side buffer.
- Reason: transparent background-clipped text produced apparent missing portions in the rendered glyphs on the current desktop font stack.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - Content rebase planning baseline

- Change: added `CONTENT_REBASE_PLAN.md`, consolidating updated external content for ScholarPilot, ProposalPilot, About, and FAQ without changing live pages.
- Reason: the new drafts materially change product maturity, CTAs, capability scope, brand wording, and domain direction; direct implementation before resolving conflicts with the existing SSOT would publish contradictory facts.
- Verification: source drafts were reviewed and each conflict with `AGENTS.md` is explicitly recorded for confirmation.

## 2026-08-11 - Brand naming decision

- Decision: the new website will use `溥源智能 / Pryos AI` as its sole public brand; `杭州溥源智能科技有限公司` remains the legal entity name.
- Scope: the confirmed replacement covers visible copy, metadata, Schema, footer, and wordmark work in the forthcoming content rebase.

## 2026-08-11 - Domain architecture decision

- Decision: use `pryos.cn` for the company site, `scholar.pryos.cn` for ScholarPilot, and `project.pryos.cn` for ProposalPilot.
- Implementation implication: the shared Next.js project must add host-aware routing and domain-specific canonical, Schema, sitemap, robots, navigation, and cross-site links.

## 2026-08-11 - Product maturity decision

- Decision: ScholarPilot is the current flagship product, in beta with a demonstrable prototype; ProposalPilot is in research and co-creation with no public prototype.
- Content implication: ProposalPilot's mature workflow, evidence-audit metrics, deployment claims, and simulated product UI must be removed or rewritten as planning; ScholarPilot becomes the complete product site.

## 2026-08-11 - Brand statement decision

- Decision: replace the global tagline with `让智能体，走进真实的专业世界` and use `AI Agents for the Professional World.` as its English companion.
- Content implication: `从知识源头，驱动专业 agent` is retained only as a possible About-page method expression, not the primary brand statement.

## 2026-08-11 - Wordmark decision

- Decision: use `PRYOS AI` as the formal brand wordmark; retain the source-and-node graphic as a visual asset rather than the official logo.
- Content and asset implication: navigation, footer, metadata, and future brand assets must use the wordmark while product scenes may continue using the source motif.

## 2026-08-11 - ScholarPilot capability status decision

- Decision: present ScholarPilot capabilities in three explicit states: beta-demonstrable, in development, and planned.
- Content implication: the page may explain the complete research workflow, but every capability must carry an honest availability state.

## 2026-08-11 - ScholarPilot capability matrix confirmed

- Decision: beta-demonstrable capabilities are topic selection/research design/literature review, literature retrieval/reading/evidence organization/citation, and paper drafting/revision.
- Decision: in-development capabilities are submission/revision/reviewer-response support, Chinese database retrieval strategy, domestic research workflow adaptation, and local-first operation.
- Decision: planned capabilities are data analysis/reproducible code, research project and grant assistance, clinical and other specialized disciplinary tasks, the 200+ skill system, and bring-your-own API.
- Content implication: every displayed capability must retain its state label; planned capabilities must not be presented as currently available.

## 2026-08-11 - Hero agent gradient restoration

- Change: restored the `agent` gradient as five progressively colored solid glyphs instead of transparent background-clipped text.
- Reason: preserve the requested black-to-yellow-green transition without reintroducing glyph rendering gaps.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - Hero agent entrance refinement

- Change: shifted `agent` to an olive-to-fluorescent-yellow-green color sequence and added a short staggered fade-and-rise entrance for its five letters.
- Reason: the original near-black-to-acid gradient felt abrupt; the new sequence keeps the accent present without weakening readability.
- Accessibility: `prefers-reduced-motion` renders all letters immediately without animation.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - Hero agent entrance visibility increase

- Change: delayed the `agent` entrance until the Hero settles, increased the letter stagger, and added blur, rise, scale, and restrained overshoot states.
- Reason: the previous short fade-and-rise completed too quickly to register clearly during page load.
- Accessibility: reduced-motion mode also removes the blur and renders the final word immediately.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - ProposalPilot co-creation content rebase

- Change: rebuilt `/proposalpilot` as a research and co-creation page with no public-prototype claim; removed the mature product workbench, autonomous full-workflow language, 200-page audit metrics, deployment promises, and free-trial actions.
- Content: added the confirmed eight-part planned workflow, co-creation audience and process, product boundaries, stage-specific FAQ, and co-creation CTAs. All planned capabilities are explicitly labeled as concepts rather than available functions.
- Visual: restored the established monochrome page-cover motion across six alternating black and white sections. Added an automatically advancing horizontal planning workflow while preserving clear concept labels.
- Color correction: removed all acid-yellow small text from the ProposalPilot page. Acid yellow remains only on active flow nodes and larger icon feedback.
- Verification: `npx.cmd tsc --noEmit` passed. Browser QA at `1440x900` confirmed six active cover transforms in dark/light alternation and zero yellow small-text elements. At `390x844`, document width matched viewport width, all six cover pages were present, two JSON-LD schemas rendered, and no console errors occurred. Screenshot: `qa-proposal-rebase-cover.png`.

## 2026-08-11 - Pryos brand and domain fact layer

- Change: updated shared metadata, Organization/SoftwareApplication authorship, navigation wordmark, footer wordmark, sitemap product URLs, homepage facts, About, FAQ, and `llms.txt` to the confirmed `溥源智能 / Pryos AI` brand and `pryos.cn` domain family.
- Product status: ScholarPilot is represented as the current beta product with a demonstrable prototype; ProposalPilot is represented as research and co-creation with no public prototype.
- Handoff boundary: further ScholarPilot visual and interaction work is owned by another active task. This task will not continue modifying that page to avoid cross-task overwrites.
- Verification: TypeScript passed. Production build reached Next.js compilation but could not fetch Inter because outbound font access was denied; the approval service was unavailable. Token verification passed 8/9 checks, with only built-CSS inspection unavailable because the production build did not complete.

## 2026-08-11 - ProposalPilot micro-label removal

- Change: removed the small status badge and product eyebrow shown above the ProposalPilot hero title, plus matching English section kickers, decorative list numbers, boundary numbers, CTA kicker, and the planning-flow header strip.
- Reason: these micro-labels duplicated information already expressed by headings and body copy and added unnecessary visual noise.
- Preserved: workflow step numbers remain because they communicate sequence; navigation CTAs remain because they are interactive controls rather than decorative labels.
- Verification: `npx.cmd tsc --noEmit` passed. Browser QA found none of the removed label strings, retained all six monochrome cover sections, showed no horizontal overflow at `390px`, and reported no console errors. Screenshot: `qa-proposal-hero-clean.png`.

## 2026-08-11 - Proposal workflow card label cleanup

- Change: removed the `01 / 08` style card counters and the bottom `规划产物` metadata rows from all eight ProposalPilot planning-flow cards.
- Preserved: the animated node, directional arrow, active-card border, title, description, and automatic horizontal progression remain intact.
- Verification: TypeScript passed; browser QA retained all six monochrome cover transforms, showed no horizontal overflow at `390px`, and reported no console errors. Screenshot: `qa-proposal-rebase-cover.png`.

## 2026-08-11 - Homepage workflow compact desktop layout

- Change: moved the pinned homepage workflow below the fixed navigation on desktop viewports up to `960px` high and compacted the heading, tabs, five-node rail, and story panel for the available height.
- Reason: the previous fixed layer started at viewport `top: 0`, so the navigation covered the title and the lower workflow content fell below the visible PC frame.
- Responsive behavior: the compact rule is desktop-only; mobile keeps the existing vertical workflow layout.
- Verification: browser QA at `1440x900` reported `stickyTop: 80px`, an `820px` usable frame, rail center delta `1px`, no horizontal overflow, and no console errors. Mobile at `412x915` remained static with no horizontal overflow. Screenshot: `qa-home-workflow-desktop.png`.

## 2026-08-11 - Homepage FAQ interaction refinement

- Change: expanded FAQ answers now grow with a fade-and-rise transition; question rows add an acid-yellow wash, text shift, and plus-icon rotation on hover, focus, and open states.
- Reason: the previous accordion gave too little feedback before and after selection.
- Accessibility: keyboard focus receives a visible outline; reduced-motion mode disables the new transitions.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - FAQ downward answer entrance

- Change: opening a FAQ now runs a dedicated downward answer entrance, moving the revealed content from above the answer area into its final position with a small settle.
- Reason: make the requested downward expansion direction visible beyond the accordion height transition.
- Accessibility: reduced-motion mode disables the entrance animation.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - FAQ expansion timing simplification

- Change: shortened the accordion height transition, reduced the answer travel distance, and removed the settling overshoot.
- Reason: the slow container expansion and end rebound felt excessive for a compact FAQ interaction.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - FAQ closed-state hover cleanup

- Change: removed the full-row hover background from closed FAQ items while retaining the question text and plus-icon hover feedback.
- Reason: a pale open-state-like background remained under the pointer after closing, making the exit state appear unfinished.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - ScholarPilot content rebase and editorial cover system

- Content: rebuilt `/scholarpilot` around the confirmed content baseline: flagship beta status, project-based research workspace, three demonstrable capability groups, four in-development directions, five planned directions, individual and institutional use, and explicit data and research-responsibility boundaries.
- Visual reference: adapted the warm-gray editorial canvas, oversized typography, flat white and black surfaces, large-radius section openings, and restrained yellow-green accents from `D:/xz/DESIGN.md`; no shadows or large chromatic surfaces were introduced in the new content sections.
- Motion: added the isolated `ScholarPageCoverMotion` driver so each black or light section slides over the previous section with rounded leading corners and navigation theme synchronization.
- Existing components: `ScholarJourney`, `ScholarJourney.module.css`, and `ScholarEarlyAccess` were not modified; their internal scroll, manuscript, and background animations remain intact.
- Responsive behavior: added single-column capability, roadmap, audience, and FAQ layouts for tablet and mobile, with reduced-motion fallbacks that remove cover transforms.
- Files: `app/scholarpilot/page.tsx`, `components/ScholarPageCoverMotion.tsx`, `app/globals.css`, and `CHANGELOG.md`.
- Verification: `npx.cmd tsc --noEmit` passed; `http://localhost:3001/scholarpilot` returned `200`; server HTML contained the new Hero, capability, roadmap, FAQ, and beta CTA copy; existing motion component files had no diff.

## 2026-08-11 - ScholarPilot legacy blue hover isolation

- Change: added ScholarPilot rebase-specific hover and focus rules for the demonstrable capability cards, retaining white and warm-gray surfaces with black text and no shadow.
- Reason: a legacy `.scholar-page` selector still turned the first capability card bright blue on hover, breaking the new monochrome editorial system.
- Verification: `npx.cmd tsc --noEmit` passed and `/scholarpilot` returned `200`.

## 2026-08-11 - Homepage FAQ closed-state color reset

- Change: closed FAQ items now retain their default black question text and plus icon under hover or focus; acid-yellow-green remains reserved for the genuinely open state.
- Reason: the prior hover color still looked like an unfinished exit state after an answer was closed.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - Homepage cover-layer side-entry restoration

- Change: restored the alternating horizontal fade-in for each homepage cover layer. The workflow, product matrix, FAQ, and contact content now enter in sync with their respective page cover; FAQ questions retain a staggered left-to-right reading sequence.
- Fix: cover, content, and FAQ-item progress now derive from the cover section's transform-independent layout position. The previous implementation read transformed child bounds, which could feed an element's own translation back into its next scroll calculation and suppress the visible entrance on some viewports.
- Verification: `npx.cmd tsc --noEmit` passed. Playwright at `1440x900` confirmed the FAQ enters from the left and lands at `opacity: 1` with no transform; the same landing state and no horizontal overflow were confirmed at `390x844`, with no browser console errors.

## 2026-08-11 - Homepage workflow progress visibility

- Change: removed clipping from the workflow track, moved the per-stage real-time progress bar 10px above the lower edge, and increased the shared connector from 1px to 2px.
- Visual: the full source marker now uses the same fluorescent yellow-green as the active workflow signal, with a black inner source point for contrast.
- Layout follow-up: added a dedicated low-height desktop layout so the fixed workflow frame remains fully inside the viewport at high display scaling; stage text now reserves room above its progress bar.
- Local preview: disabled the Next.js development indicator because its bottom-left `N` badge covered the live stage panel during workflow review.
- Layering correction: raised the horizontal light-progress track above node cards and dividers while keeping markers above the track; changed the source core itself from black to fluorescent yellow-green.
- Follow-up: removed the node stacking context so marker icons can sit above the raised track without the light line crossing their faces.
- Final-stage dwell: the light track now completes during the first 82% of the pinned workflow, reserving the remaining scroll distance for the fifth stage. Pin release is delayed until the following product cover has entered most of the viewport, preventing the workflow from disappearing before it is covered.

## 2026-08-11 - FAQ question-only side entry

- Change: the FAQ heading now arrives with its cover layer; only the individual question rows slide and fade in from the side with their staggered sequence.
- Reason: the requested entrance animation applies to the questions, not the section title.
- Follow-up: each row now calculates its entrance from its own stable layout position rather than from the FAQ cover's progress. This keeps the animation active while the row is actually entering the viewport, with a more legible 112px side offset.
- Final trigger: replaced the scroll-progress dependency with one-shot `IntersectionObserver` entrances. Each question now begins only when it enters the reading viewport, so fast wheel scrolling and anchor jumps cannot skip the fade-in.

## 2026-08-11 - Yellow microcopy cleanup

- Change: removed the decorative yellow English/numbered eyebrows from ScholarPilot's Hero, capability, roadmap, and FAQ sections; changed retained research-journey and roadmap state labels to neutral gray.
- Scope: yellow remains only for high-salience controls, status backgrounds, and abstract visual nodes, not small descriptive text.
- Constraint: existing ScholarJourney behavior remains unchanged; only its microcopy color was made neutral.
- Verification: `npx.cmd tsc --noEmit` passed; `/scholarpilot` returned `200`; the removed Hero and section eyebrow strings were absent from server HTML.

## 2026-08-11 - Non-home navigation theme unification

- Change: unified ProposalPilot, ScholarPilot, About, and FAQ navigation shells, product triggers, dropdowns, mobile menus, and primary actions under the black-white system.
- Fix: removed the legacy blue open and hover state visible on the ProposalPilot product trigger and dropdown.
- Theme behavior: ProposalPilot and ScholarPilot retain their scroll-aware dark navigation states, using white text and the acid-yellow action only over dark sections.
- Verification: `npx.cmd tsc --noEmit` passed; `/`, `/proposalpilot`, `/scholarpilot`, and `/about` returned `200`. The first `/faq` development compile was delayed by the existing Google Fonts network retry.

## 2026-08-11 - Final navigation blue-tone cleanup

- Change: added a final navigation palette layer for the homepage, both product pages, About, and FAQ. It resets desktop links, product dropdowns, mobile menus, focus states, and the product-selection action from the legacy blue palette to neutral black, white, and fluorescent yellow-green.
- Theme behavior: light sections use a warm-white shell, black navigation text, neutral hover states, and a black action with fluorescent yellow-green text; the existing dark-section drivers retain white text with fluorescent yellow-green active and action states.
- Reason: earlier blue navigation rules remained active at similar specificity, so the previous overrides did not consistently apply across all page and mobile states.
- Verification: `npx.cmd tsc --noEmit` passed; `/`, `/proposalpilot`, `/scholarpilot`, and `/about` returned `200`; no legacy blue values were found in the final navigation rule layer. `/faq` local development compilation remains delayed by the existing Google Fonts network retry.

## 2026-08-11 - Homepage workflow component refinement

- Reference direction: translated the interaction language of modern stepper and segmented-control components into the existing homepage workflow without copying an external component or changing the confirmed content model.
- Visual change: replaced the table-like workflow presentation with a unified rounded stepper surface, a sliding business-line selector, clearer complete/current/pending stage states, and a focused active-stage summary.
- Content hierarchy: separated the stage report from the rail and strengthened the reading order across stage, input, Agent action, and result; the result column now carries the acid-yellow semantic emphasis.
- Motion and responsive behavior: retained the existing `350vh` stage driver, beam, accumulated completion state, tab copy transition, vertical mobile flow, and reduced-motion fallback.
- Files: `app/globals.css`, `CHANGELOG.md`.

## 2026-08-11 - Homepage workflow report crossfade

- Change: replaced the report panel's keyed remount animation with overlapping outgoing and incoming story layers. The previous stage remains visible while fading upward, while the next stage enters from the same layout position.
- Reason: stage changes previously cleared the report before mounting the new content, which created a visible flash rather than a continuous transition.
- Scope: the scroll stage state, business-line copy, mobile vertical workflow, and product-fact boundaries are unchanged.
- Files: `components/HomeWorkflow.tsx`, `app/globals.css`, `CHANGELOG.md`.

## 2026-08-11 - Shared product Hero pipeline

- Design source: the user approved borrowing the Hero composition, spacious rhythm, rounded process container, and data-pipeline visual from `D:/xz/DESIGN (2).md`, while retaining PuYuan's black, white, and acid-yellow visual system.
- Shared architecture: ProposalPilot and ScholarPilot now use one `ProductHero` component with product name, trust-led statement, CTA pair, process panel, and factual footer strip.
- Product differentiation: ProposalPilot loops from enterprise materials through policy matching, qualification review, human confirmation, and project delivery. ScholarPilot loops from research question through literature structure, draft structure, polishing, and submission preparation.
- Motion: the active pipeline step advances every 1.9 seconds, moves a single signal through the following connector, updates the current output, and pauses for inspection on pointer or keyboard focus. Reduced-motion mode is static.
- Files: `components/ProductHero.tsx`, `app/proposalpilot/page.tsx`, `app/scholarpilot/page.tsx`, `app/globals.css`, `CHANGELOG.md`.

## 2026-08-11 - Product Hero topology correction

- Correction: replaced the sequential five-step rail with the topology shown in the approved reference image and the imagery section of `D:/xz/DESIGN (2).md`.
- Structure: five source capsules now converge through curved hairline paths on a four-part processing hub, then branch into four product-specific outputs. ProposalPilot maps enterprise materials to申报成果; ScholarPilot maps research materials to写作成果。
- Motion: one input and one corresponding output path carry a restrained moving acid-yellow signal while the center core rotates and its semantic glow breathes. Pointer or keyboard focus pauses the cycle; reduced-motion mode is static.
- Responsive behavior: desktop preserves the wide left-to-center-to-right topology. Mobile converts it to input group, center hub, and output group without horizontal overflow.
- Files: `components/ProductHero.tsx`, `app/globals.css`, `CHANGELOG.md`.

## 2026-08-11 - Product Hero homepage palette alignment

- Change: recalibrated both product Hero canvases and topology visuals to the homepage palette: `#f5f5f2` canvas, `#171717` ink, `#d8d8d2` hairlines, and `#d7ff00` as the single signal accent.
- Removed: legacy warm-gray, mint, and olive-green fills from the active nodes, animated paths, hub petals, and central glow.
- Preserved: the approved source-to-hub-to-output geometry, five-to-four routing behavior, pause interaction, responsive collapse, and reduced-motion fallback.
- Verification: `/proposalpilot` and `/scholarpilot` returned `200`. `npx.cmd tsc --noEmit` remains blocked by the pre-existing undefined `coverIndex` reference in `components/HomePageCoverMotion.tsx`, outside this color-only change.

## 2026-08-11 - Product Hero center processing feedback

- Change: connected the center hub to the active input cycle. Each incoming route now lights its matching processing petal, sends a visible acid-yellow pulse through the core, and keeps the core orbit and icon rotation active before the related output branch lights.
- Preserved: the black, white, and acid-yellow homepage palette, topology geometry, pause-on-inspection behavior, and reduced-motion static fallback.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` and `/scholarpilot` both returned `200`; patch whitespace validation passed.

## 2026-08-11 - Product Hero material-to-result payload animation

- Change: each active source now launches its contextual Phosphor icon toward the center hub. A verification mark appears during processing, then the matching result icon departs the hub toward its active output node.
- Motion: the payload animation uses transform and opacity only, restarts with the existing 1.9-second topology cycle, and is disabled for reduced-motion users.
- Visual system: source, verification, and result states retain the shared monochrome lines and acid-yellow signal treatment.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` and `/scholarpilot` both returned `200`; patch whitespace validation passed.

## 2026-08-11 - Product Hero slower payload rhythm and branded core

- Change: extended the topology cycle from 1.9 to 3.8 seconds and lengthened the incoming and outgoing payload phases so the material, core processing, and result conversion can be read separately.
- Center: replaced the transient verification glyph with a persistent rotating geometric mark plus the current product name. ProposalPilot displays `智小申`; ScholarPilot displays `科小文`.
- Preserved: the contextual input and result icon conversion, monochrome topology, acid-yellow signal treatment, pause behavior, and reduced-motion fallback.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` and `/scholarpilot` both returned `200`; patch whitespace validation passed.

## 2026-08-11 - Product Hero slower cycle without rotation

- Change: extended the material-to-result cycle from 3.8 to 5.2 seconds and slowed both payload travel phases.
- Center: replaced the rotating circular mark with a static Phosphor tree-structure symbol and stopped the outer core orbit. Product-name labels remain visible.
- Preserved: processing pulse, active route, icon conversion, pause behavior, and reduced-motion fallback.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` and `/scholarpilot` both returned `200`; patch whitespace validation passed.

## 2026-08-11 - Product Hero payload path alignment

- Change: replaced straight point-to-point payload interpolation with sampled SVG path geometry. Incoming and outgoing icons now follow the exact active curved connector from source to core and from core to result.
- Implementation: the client animation samples 28 points from each active `SVGPathElement` and animates only transform and opacity, preserving the existing timing and responsive topology.

## 2026-08-11 - Product Hero center mark removal

- Change: removed the center structure icon and kept only the current product name in the core, while retaining the material and result icons that travel along the active paths.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` and `/scholarpilot` both returned `200`; patch whitespace validation passed for the path-following and center-label changes.

## 2026-08-11 - Product Hero multi-payload streams

- Reference: adapted the staggered event-badge density from the user-provided pipeline screenshot without copying its pastel palette or security-domain labels.
- Change: each active route now carries three incoming material icons and three outgoing result icons, launched with short offsets so multiple payloads remain visible on the same curved connector.
- Visual: reduced each payload from 36 to 32 pixels and kept one consistent Phosphor icon family, black outline, and acid-yellow signal fill.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` and `/scholarpilot` both returned `200`; patch whitespace validation passed.

## 2026-08-11 - Product Hero multi-source payload batches

- Change: each three-icon batch now starts from three different material nodes instead of repeating one source icon. Every payload uses its source node's own Phosphor icon and follows that node's individual curve into the core.
- Output: the batch converts into three distinct result icons and branches across three result paths, keeping each route visually traceable.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` and `/scholarpilot` both returned `200`; patch whitespace validation passed.

## 2026-08-11 - Product Hero verified output payload

- Change: after a material icon reaches the product core, every outgoing payload changes into a filled check-circle to communicate successful verification before reaching its named result node.
- Preserved: right-side result nodes retain their own product-specific icons, names, and paths.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` and `/scholarpilot` both returned `200`; patch whitespace validation passed.

## 2026-08-11 - Product Hero topology scale reduction

- Change: reduced the desktop topology canvas to a 1040-pixel maximum width and 365-pixel height, with a proportional reduction to nodes, hub, petals, core, and moving payload badges.
- Responsive: tablet geometry is also compacted; the existing mobile stacked layout remains unchanged.
- Preserved: three-source batches, exact curved-path animation, verified check outputs, and the product-name-only core.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` and `/scholarpilot` both returned `200`; patch whitespace validation passed.

## 2026-08-11 - ProposalPilot problem section center alignment

- Change: centered the problem-section headline and introduction, then converted each problem row from a two-column split into a centered title-and-description stack.
- Responsive: the same reading axis now applies consistently across desktop and mobile without changing the confirmed copy.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` returned `200`; patch whitespace validation passed.

## 2026-08-11 - ProposalPilot problem section title-only alignment

- Change: limited center alignment to the problem-section headline. The introductory paragraph and the original two-column problem rows were restored to their prior reading layout.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` returned `200`; patch whitespace validation passed.

## 2026-08-11 - ProposalPilot workflow title alignment

- Change: centered the `八个计划环节，等待真实流程验证。` heading within its existing 800-pixel measure while leaving the supporting paragraph left-aligned and otherwise unchanged.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` returned `200`; patch whitespace validation passed.

## 2026-08-11 - ProposalPilot problem header reading-axis alignment

- Change: centered and widened the supporting paragraph beneath the centered problem-section heading. The grouped problem rows remain in their existing two-column layout.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` returned `200`; patch whitespace validation passed.

## 2026-08-11 - ProposalPilot section-heading consistency pass

- Change: removed remaining decorative English micro-labels from evidence, data-sovereignty, and human-gates headers. Centered the explanatory headers for evidence, workflow planning, human gates, product boundaries, and FAQ while preserving their data visualizations, lists, cards, and controls.
- Layout: reduced the human-gates header-to-paper gap from 72 to 56 pixels, correcting the oversized blank band before the stacked gate sequence.
- Scope: the split co-creation section and all detailed gate-sheet content retain their left-aligned, task-oriented layout.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` returned `200`; no removed English micro-label remained in the affected source; patch whitespace validation passed.

## 2026-08-11 - ScholarPilot manuscript travel extension

- Change: extended the desktop ScholarJourney manuscript track from `156vh` to `220vh`, lengthening the section so the right-hand paper stack continues moving downward after the third stage becomes active.
- Preserved: stage-selection thresholds, manuscript content transitions, pointer tilt, mobile static layout, and reduced-motion behavior.
- Verification: `npx.cmd tsc --noEmit` passed; `/scholarpilot` returned `200`; patch whitespace validation passed.

## 2026-08-11 - ProposalPilot capability-boundary heading revision

- Change: revised the capability-boundary heading to `产品仍在研发，能力边界先说清楚。` for a more direct and specific connection to the following boundary cards.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` returned `200`; patch whitespace validation passed.

## 2026-08-11 - Cross-page route transition

- Change: added a shared route transition for internal page links. A fluorescent source point expands into a black cover from the click position; after the destination mounts, the cover pushes upward and the new page enters with a restrained fade and rise.
- Scope: applies to navigation, footer, product cards, and other same-origin page links while leaving same-page anchors, email links, external links, modified clicks, and new-tab actions unchanged.
- Compatibility: the transition lives above the existing page-cover systems and does not modify HomePageCoverMotion, ScholarPageCoverMotion, or product-specific motion components. Reduced-motion users navigate without the added animation.
- Verification: `npx.cmd tsc --noEmit` passed; `/`, `/proposalpilot`, `/scholarpilot`, and `/about` returned `200`; `git diff --check` reported no patch whitespace errors.

## 2026-08-11 - Route transition event capture fix

- Fix: moved the shared same-origin link listener to the capture phase so it starts before Next.js `Link` handles and prevents the default navigation event.
- Reason: the prior bubbling listener was bypassed by framework routing, leaving no visible transition.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - Reliable text-reveal registration

- Fix: text reveal now registers text inserted after a route transition begins, and immediately schedules visible viewport text for the next frame after assigning its hidden state.
- Reason: a one-time scan plus asynchronous intersection observation could miss or visually skip first-screen text while the incoming route was mounting beneath the page transition.
- Scope: below-the-fold text retains intersection-triggered reveals; existing excluded motion components remain excluded.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - ScholarPilot top-background alignment

- Fix: aligned the ScholarPilot body and main background with the ProductHero's warm light surface.
- Reason: the fixed navigation exposed an outer gray background above the Hero grid, creating a visible horizontal color break at the top of the page.
- Verification: `npx.cmd tsc --noEmit` passed; the local development server was restarted on port `3001` and `/scholarpilot` returned `200`.

## 2026-08-11 - ScholarPilot journey title wrapping

- Fix: balanced desktop-stage titles and tightened their responsive size and line height.
- Reason: the first stage title could leave its final Chinese character isolated on a third line beside the manuscript panel.
- Constraint: did not modify the existing stage-selection, paper-position, or manuscript-state animations.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - ScholarPilot manuscript viewport fit

- Fix: reduced the desktop manuscript maximum height, made it responsive to the current dynamic viewport height, and tightened its internal vertical spacing so all document content remains visible.
- Motion correction: paper travel now uses the rendered manuscript height instead of the previous hard-coded `690px` value.
- Reason: on common laptop-height desktop viewports, the manuscript frame extended below the visible browser area.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - ScholarPilot manuscript stack depth motion

- Change: added restrained 3D floating motion to the two background manuscript cards, with a shorter cycle while the manuscript is hovered.
- Constraint: the foreground document remains stable for reading; existing stage and document-state transitions are unchanged; reduced-motion mode remains static.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - ScholarPilot manuscript pointer-depth refinement

- Change: added pointer-driven perspective tilt to the manuscript stack and increased the separation and cadence of the two background-card float cycles.
- Reason: the previous automatic motion was too subtle to read reliably; moving over the document now provides clear three-dimensional depth feedback.
- Accessibility: pointer tilt and card motion remain disabled under the reduced-motion preference.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - Homepage source-led workflow progression

- Change: recalibrated the homepage workflow so the fluorescent progress line grows continuously from the source node as the user scrolls. Its light head now reaches each node before that stage becomes active.
- Visual: removed completed nodes' black fill, replaced the travelling automatic segment with a source column and a scroll-driven light head, and retained fluorescent yellow-green only for active progress.
- Motion: current node copy and the lower stage panel now enter from left to right; outgoing panel content exits to the right.
- Verification: `npx.cmd tsc --noEmit` passed; `/` returned `200`; `git diff --check` reported no patch whitespace errors.

## 2026-08-11 - Proposal concept-flow surface alignment

- Fix: recolored the eight-stage ProposalPilot concept-flow component from its legacy black skin to the surrounding warm-white page surface.
- Visual: cards now use white with black text, neutral gray rails and borders, and fluorescent yellow-green only for the active stage.
- Constraint: retained the existing automatic horizontal progression, manual stage selection, and reduced-motion behavior.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` returned `200`.

## 2026-08-11 - Homepage text-reveal transition persistence

- Fix: apply the text-reveal opacity transition before an element changes from pending to visible.
- Reason: first-screen content can be registered and revealed in adjacent frames; defining the transition only on the visible state sometimes caused the browser to paint the final state directly.
- Scope: preserves the opacity-only reveal approach and existing exclusions for complex motion components.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - Dedicated homepage Hero entrance

- Fix: removed the homepage Hero copy from the shared intersection-based text observer and gave it a deterministic mount animation.
- Motion: title, supporting copy, primary action, and source network now fade and rise in sequence; the existing staggered `agent` glyph animation remains nested inside the title entrance.
- Reason: the shared observer and the Hero's own letter choreography could compete for opacity during initial paint, making the overall fade appear absent.
- Verification: `npx.cmd tsc --noEmit` passed.

## 2026-08-11 - 首页 Hero 低高度 PC 完整展示

- 改动内容：为宽度不低于 `721px`、高度不超过 `700px` 的 PC 窗口增加紧凑 Hero；压缩标题、说明、CTA 与源点网络尺寸，源点两侧产品入口继续保持横向布局。
- 改动原因：分屏窗口扣除固定导航后仅剩约 `420px` 高度，原 Hero 的文字区和最小 `270px` 网络区无法同时进入首屏，导致底部产品入口被裁切。
- 设备边界：正常高度桌面和手机端继续使用原 Hero 尺寸与布局。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `870x500` 下确认 Hero 位于 `y=80-500`，两个产品入口底部均在 `y=409`、完整位于视口内且无横向溢出；`1440x900` 下仍保留原 `820px` Hero 和正常节点尺寸，控制台无错误。

## 2026-08-11 - 首页正常屏幕阅读节奏重构

- 改动内容：首页按 `1366×768` 与 `1440×900` 正常桌面视口重新校准区块比例。能力区缩短为约 `602–604px`；工作流的固定滚动距离由 `350vh` 调整为 `280vh`；产品矩阵的视觉区、内容区和上下留白同步收紧；FAQ 与联系区减少无效垂直留白。
- 改动原因：原页面在正常笔记本与桌面屏幕下总高度约 `7,029–7,623px`，工作流和产品卡占用过大，阅读节奏被拉长。调整后总高度为 `5,409–5,925px`，仍保留工作流切换、卡片悬停、文字淡入及页面叠层动效。
- 运行时修复：`HomePageCoverMotion` 不再引用可能在热更新中失效的循环索引变量，改由 `covers.indexOf(cover)` 确定内容入场方向，解决首页出现 `coverIndex is not defined` 的运行时错误。
- 边界：未修改首页文案、路由、Schema、导航主结构或产品事实；`870×500` 紧凑 PC 规则和移动端布局继续独立生效。
- 涉及文件：`app/globals.css`、`components/HomePageCoverMotion.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1366×768`、`1440×900`、`870×500`、`390×844` 下均无控制台错误和横向溢出。Hero 在正常桌面完整展示，`870×500` 下两个产品入口仍完整可见，移动端首屏与产品入口保持可读。

## 2026-08-11 - 首页工作流屏内完整展示

- 改动内容：为高度不超过 `1100px` 的桌面工作流增加独立的紧凑排版，收紧标题区、业务切换、五阶段轨道、节点说明和阶段结果面板的高度。
- 改动原因：高缩放桌面环境会把工作流节点的说明文字撑高，导致最下方阶段结果面板被视口底部截断。
- 边界：保留五阶段信息、当前阶段说明、切换交互、滚动驱动和无障碍结构；不隐藏阶段结果。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1920×900`、`1440×900`、`1366×768`、以及模拟高缩放的 `1097×525` 视口均确认阶段结果面板完整位于视口内，无横向溢出和控制台错误。

## 2026-08-11 - 低高度桌面产品矩阵比例恢复

- 改动内容：撤回低高度 PC 下产品矩阵过度压缩的尺寸，将顶部避让空间恢复至固定导航下方；产品视觉区由 `120px` 恢复至 `220px`，正文区由 `200px` 恢复至 `260px`，完整展示产品介绍；产品锚点增加 `104px` 导航避让。
- 改动原因：此前为短屏强行把整段产品矩阵塞进单一视口，导致产品标题被固定导航覆盖，卡片视觉和正文的阅读比例失衡。
- 边界：保留短屏双列布局、黑白荧光黄视觉、动态产品示意和卡片跳转；产品区允许自然向下滚动，不再以牺牲内容比例换取单屏展示。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1097×525`、`870×500`、`1366×768` 下确认卡片视觉与正文尺寸恢复，锚点无横向溢出和控制台错误。

## 2026-08-11 - 首页产品卡动态示意移除

- 改动内容：删除首页产品矩阵两张卡片上方的动态节点、连线、文档和人工确认示意，只保留产品名称、英文名、阶段定位、介绍与详情入口。
- 改动原因：用户要求移除产品卡上方动图，避免动态视觉继续占用产品介绍区的阅读空间。
- 涉及文件：`components/ProductMatrix.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1366×768` 与 `390×844` 下确认动态示意节点数量为 `0`，卡片无横向溢出和控制台错误。

## 2026-08-11 - 智小申 G1-G6 纸片叠放恢复

- 原因：智小申页面最近重构时仍保留了 `ProposalGateStack` 组件、纸片内容、图片和样式，但 `app/proposalpilot/page.tsx` 不再引用该组件，导致整段未被渲染。
- 改动内容：在“八个计划环节”之后恢复 G1-G6 人工门控纸片序列，并将后续共创、能力边界、FAQ 与 CTA 的页面层级顺延，确保页面叠层和纸片叠层互不覆盖。
- 响应式行为：桌面端继续使用 sticky 纸片逐张叠放；移动端保持六张纸片顺序展开。
- 涉及文件：`app/proposalpilot/page.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1440×900` 与 `390×844` 下确认纸片区渲染一次、包含六张纸片，桌面首张纸片为 `sticky`、移动端为 `relative`，无横向溢出和控制台错误。

## 2026-08-11 - 科小文稿纸跟随与导航避让

- 改动内容：重写科研流程右侧稿纸的桌面滚动跟随计算。稿纸直接在动画帧中更新位移，固定跟随导航下方的安全线，移除原先额外的 `-72px` 偏移和逐帧 React state 重渲染。
- 改动原因：原实现以视口中线计算跟随进度，纸片进入位置过晚；同时额外负偏移会让纸片顶部被固定导航遮挡。
- 边界：不改动阶段切换、稿纸内容、鼠标倾斜、叠纸浮动和移动端顺序展示。
- 涉及文件：`components/ScholarJourney.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1440×900` 三个滚动位置均测得导航底部 `y=78`、稿纸顶部 `y=156`，无控制台错误和横向溢出。

## 2026-08-11 - 科小文研究流程版式平衡

- 改动内容：将研究流程两列从偏向右侧稿纸的 `0.82/1.18` 调整为 `0.94/1.06`，收紧列间距、阶段卡垂直节奏和文字行宽；桌面稿纸最大高度由 `640px` 调整为 `580px`，同步压缩稿内间距与列表节奏。
- 改动原因：原版右侧稿纸过大、左侧阶段信息偏散，正常桌面首屏的阅读重心不平衡。
- 边界：保留稿纸跟随安全线、阶段切换、鼠标倾斜、叠纸效果和移动端顺序展示。
- 涉及文件：`components/ScholarJourney.module.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；Playwright 在 `1440×900`、`1366×768`、`390×844` 下均无横向溢出和控制台错误。桌面稿纸分别在 `y=156–736` 与 `y=138–696` 内完整可读。

## 2026-08-11 - 智小申人工门控顺序修正

- 改动内容：将 G4“最终材料必须由企业批准”移动到 G5“只授权可确定修改的安全子集”之前，纸片顺序统一为 `G1 → G2 → G3 → G4 → G5 → G6`。
- 改动原因：门控数组历史排序将 G5 放在 G4 前，页面因此显示为 `G3 → G5 → G4`，与门控编号和流程逻辑不一致。
- 涉及文件：`components/ProposalSections.tsx`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；服务器 HTML 中 G4 出现在 G5 前，Playwright 读取六张纸片的实际 DOM 顺序为 `G1, G2, G3, G4, G5, G6`。

## 2026-08-11 - 智小申 G5 至 G6 纸片交接修正

- 改动内容：恢复第五张纸片 G5 的底部停留距离；仅最后一张 G6 不保留后续滚动距离。
- 改动原因：G4/G5 调整为正确编号顺序后，遗留的 `nth-child(5)` 零停留规则错误地作用于 G5，G6 会直接越过 G5 并露出 G4。
- 涉及文件：`app/globals.css`、`CHANGELOG.md`。
- 验证结果：`npx.cmd tsc --noEmit` 通过；在 G5→G6 交接位置，Playwright 确认 G5 仍固定于堆叠顶层，G6 从下方进入，无横向溢出和控制台错误。
## 2026-08-11 - ScholarPilot section-heading layout correction

- Change: reduced the desktop section-heading scale, widened the supporting-copy column, and aligned both columns from the top.
- Reason: the previous 94px heading ceiling dominated normal PC viewports and left the supporting paragraph visually detached at the bottom edge.
- Scope: keeps the existing tablet single-column layout, mobile type size, page-cover motion, and section content unchanged.
- Verification: `npx.cmd tsc --noEmit` passed; `http://localhost:3001/scholarpilot` returned `200`.
## 2026-08-11 - ScholarPilot centered section-heading template

- Change: centered each ScholarPilot section title and placed its supporting copy directly below it with a shared readable width.
- Motion: registered the complete heading block with the existing `SiteTextReveal` observer so it uses the same opacity timing and viewport trigger as the rest of the site.
- Scope: keeps page-cover transitions, card motion, FAQ behavior, and all section content unchanged.
- Verification: `npx.cmd tsc --noEmit` passed; `http://localhost:3001/scholarpilot` returned `200`.
## 2026-08-11 - ProposalPilot topology core cleanup

- Change: replaced the overlapping diamond cluster below the ProposalPilot heading with a circular product core and four clearly separated processing steps.
- Motion: active steps now communicate progress through fluorescent color and light only, without scaling into the product name or covering adjacent labels.
- Scope: preserves the input/output topology, route payload animation, automatic stage cycling, hover pause, and mobile stacking behavior.
- Verification: `npx.cmd tsc --noEmit` passed; `http://localhost:3001/proposalpilot` returned `200`.
## 2026-08-11 - Homepage visible-viewport entrances

- Fix: decoupled homepage section content entrances from the page-cover progress and now trigger them once when the actual heading, card group, workflow frame, FAQ header, or contact block enters the viewport.
- Motion: retained the black/white cover transitions and added restrained rise/side entrances with staggered capability and product cards; FAQ questions keep their existing independent side entrance.
- Reason: the previous content opacity reached its final state while the covering page was still below the readable viewport, so the fade-in appeared to be missing.
- Accessibility: reduced-motion mode keeps all content immediately visible and static.
- Verification: `npx.cmd tsc --noEmit` passed; Playwright viewport-entry checks cover `1440x900`, `1060x420`, `390x844`, and the page-bottom contact block.
## 2026-08-11 - Cross-page visible-viewport entrances

- Change: extended the homepage's viewport-triggered fade and motion language to `/proposalpilot`, `/about`, and `/faq` through a shared client-side observer.
- Motion: ProposalPilot section content enters from the alternating page edge; the About page uses quieter rise and staggered rows; FAQ groups and questions enter in readable order.
- Constraint: ScholarPilot remains unchanged because it already owns separate cover and manuscript-scroll motion.
- Accessibility: all new entrances resolve to visible static content when reduced motion is preferred.
- Verification: `npx.cmd tsc --noEmit` passed; `scripts/qa-site-layer-entrance.cjs` covers desktop ProposalPilot and About entrances plus low-height desktop and mobile FAQ entrances.

## 2026-08-11 - ProposalPilot compact gate-stack navigation clearance

- Change: moved the compact-desktop G1-G6 sticky offsets below the fixed navigation safe line (`112px` through `132px`) and reduced the corresponding sheet height to `calc(100vh - 136px)`.
- Reason: at high browser zoom or low desktop viewport heights, G5 could rise into the fixed navigation before G6 took over, making the G5-to-G6 handoff appear to skip or slide out early.
- Scope: normal desktop offsets, mobile sequential cards, gate order, page content, and product facts are unchanged.
- Verification: `npx.cmd tsc --noEmit` passed; `git diff --check` passed. The local browser test harness is unavailable because this workspace does not currently include the `playwright` package.

## 2026-08-11 - ProposalPilot Hero tagline update

- Change: replaced the ProposalPilot Hero headline with "让复杂的项目申报，成为可以看清和推进的过程".
- Layout: retained the existing two-line headline structure, typography, entrance motion, and topology visualization.
- Verification: `npx.cmd tsc --noEmit` passed; `http://localhost:3001/proposalpilot` returned `200`.
## 2026-08-11 - Proposal concept-flow text visibility

- Fix: excluded the automatic horizontal concept-flow component from the cross-page container entrance and restored any stale text-reveal elements left in a pending state during hot updates.
- Reason: combining the container observer with the shared text observer could leave the eight workflow cards' headings and descriptions at `opacity: 0` while their borders and nodes remained visible.
- Constraint: the concept flow keeps its own automatic horizontal progression, active-card movement, and interaction states.
- Verification: `npx.cmd tsc --noEmit` passed; Playwright confirmed all eight stage headings and descriptions render with `opacity: 1` at `1440x900`, with no console errors.

## 2026-08-11 - ProposalPilot compact G6 full-cover handoff

- Change: removed the compact-desktop 4px cascading sticky offsets so G1-G6 now share the same `112px` navigation-safe top line.
- Reason: after G6 reached its sticky position, the staggered offsets still exposed the top of G5, which looked like an unrelated page floating out before the user continued scrolling.
- Scope: the normal-height desktop paper cascade, mobile sequential layout, card order, and trailing scroll space remain unchanged.
- Verification: `npx.cmd tsc --noEmit` passed; `git diff --check` passed; `/proposalpilot` returned `200` from the existing development server.

## 2026-08-11 - ProposalPilot G6 exit sequencing

- Change: removed the full-page cover behavior from the first section following the G1-G6 paper stack.
- Reason: its negative overlap, scale, and upward cover transform began before G6 had released, making the following section appear to float over the final gate card during downward scrolling.
- Scope: the G1-G6 paper stack and all subsequent page-cover transitions remain unchanged; the first post-gate section now follows G6 in normal document flow.
- Verification: verified with a `1097x525` browser viewport that the follow-up section has no cover transform or negative overlap while G6 is sticky.

## 2026-08-11 - ProposalPilot desktop G6 full-cover correction

- Change: removed the default desktop staggered sticky top offsets for G2-G6 while retaining their stacking order.
- Reason: the earlier compact-only correction did not affect normal desktop viewports, where G5 remained visibly exposed above G6 during the final handoff.
- Scope: each incoming gate card now fully covers its predecessor at the shared desktop sticky top line; only the decorative exposed-edge treatment was removed.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed. Browser measurements confirmed G5/G6 share `top: 96px` at `1905x866` and `top: 112px` at `1097x525`, with G6 above G5 and the following section still outside the viewport.

## 2026-08-11 - ProposalPilot G6-to-follow-up synchronization

- Change: removed the residual `96px` bottom padding from the G1-G6 stack section.
- Reason: it held the following section below G6 after the final card began leaving its sticky position, causing a visible gap and an out-of-sync transition.
- Scope: the final card still receives its existing trailing sticky duration; only the handoff from G6 into the immediately following section is made contiguous.
- Verification: `npx.cmd tsc --noEmit`, `git diff --check`, and `/proposalpilot` returned `200`. At the G6 release threshold, automated browser measurements report a `1px` rounded gap at both `1097x525` and `1905x866`.

## 2026-08-11 - ProposalPilot post-G6 cover restoration

- Change: restored the shared full-page cover class on the black section immediately following G6.
- Reason: removing that class eliminated the intended sliding cover transition and changed the handoff into ordinary document scrolling.
- Synchronization: the previously removed `96px` gate-section bottom gap remains removed, so the restored black cover begins directly after the G6 sequence instead of entering late.
- Verification: `npx.cmd tsc --noEmit`, `git diff --check`, and `/proposalpilot` returned `200`. At the video's approximately `244x720` viewport, the black section transitions from `scale(0.84)` with a `46px` top radius to its landed state while moving into a contiguous overlap with G6.
## 2026-08-11 - Proposal concept-flow task dossier cards

- Change: rebuilt the eight independent ProposalPilot planning cards as compact task dossiers with stage label, process signal, source basis, stage result, and a stage footer.
- Visual: active cards now use a near-black surface with fluorescent yellow-green signal treatment; inactive cards retain the light document surface and fine neutral rules.
- Motion: automatic stage travel remains; hovering the rail temporarily pauses progression, and the active card lifts with a synchronized signal line and bounded arrow response.
- Content: the new source and result fields are concise restatements of the existing eight-stage descriptions and do not represent additional product claims.
- Verification: `npx.cmd tsc --noEmit` passed; browser checks cover all eight titles, descriptions and source/result fields, the active black card state, hover pause, and desktop/mobile horizontal overflow.
## 2026-08-11 - ProposalPilot co-creation section rewrite

- Change: rewrote the co-creation section around real workflow interviews, anonymized material analysis, and workflow/prototype review.
- Reason: moved the message from an emphasis on unavailable product access to a clear explanation of how co-creation calibrates product tasks, boundaries, and collaboration.
- Scope: preserves the section's dark cover treatment, three-row structure, icons, and the existing CTA/FAQ boundary disclosures elsewhere on the page.
- Verification: `npx.cmd tsc --noEmit` passed; `http://localhost:3001/proposalpilot` returned `200`.
## 2026-08-11 - Proposal concept-flow active-state restraint

- Refinement: removed the full black active-card inversion and replaced it with a light document surface, fluorescent top signal, restrained dark outline, highlighted process icon, and softer lift.
- Cleanup: merged the competing node treatments so each card now has one readable process signal instead of overlapping circles and glow states.
- Reason: the high-contrast black flash made automatic stage changes visually abrupt and heavier than the surrounding light planning section.
- Verification: `npx.cmd tsc --noEmit` and the route animation QA passed; desktop screenshot review confirmed a continuous light-card rhythm, while the mobile card remains within the viewport without document overflow.
## 2026-08-11 - ProposalPilot gate-stack shadow removal

- Change: removed the ProposalPilot gate-stack cover, paper, folded-corner, key-sheet, and visual-preview shadows.
- Reason: the layered shadows introduced a large gray haze above the section and made the white paper surface appear visually detached from the page.
- Scope: retained the section's borders, cover motion, paper stacking, imagery, and G1-G6 content.
- Verification: `npx.cmd tsc --noEmit` passed; `http://localhost:3001/proposalpilot` returned `200`.
## 2026-08-11 - ScholarPilot cover-radius continuity

- Fix: aligned ScholarPilot's settled cover radius with the final scroll-cover animation value: `30px` on desktop and `18px` on mobile.
- Reason: removing the temporary cover-motion attribute previously reverted each section from the animated radius back to a larger base radius, causing a visible corner jump.
- Scope: preserves the cover's scale, vertical travel, opacity, overlap, and reduced-motion behavior.
- Verification: `npx.cmd tsc --noEmit` passed; `http://localhost:3001/scholarpilot` returned `200`.

## 2026-08-11 - ProposalPilot vertical content entrance

- Change: changed ProposalPilot section-content entrances from left/right slides to a consistent upward rise and fade.
- Correction: removed the central product-name animation added during the initial interpretation; the Hero topology and its `智小申` label remain unchanged.
- Scope: the direction change is limited to `/proposalpilot`, retains the existing reveal timing and IntersectionObserver behavior, and remains disabled under `prefers-reduced-motion`.
- Verification: `npx.cmd tsc --noEmit` passed; `/proposalpilot` returned `200`; patch whitespace validation passed.

## 2026-08-11 - ScholarPilot FAQ heading clarification

- Change: renamed the ScholarPilot FAQ heading to "科小文常见问题".
- Reason: the former "内测、数据与研究责任" phrase described topic categories but did not clearly communicate that this section is an FAQ.
- Scope: retained every FAQ item, accordion behavior, and section layout.
- Verification: `npx.cmd tsc --noEmit` passed; `http://localhost:3001/scholarpilot` returned `200`.
## 2026-08-11 - ScholarJourney value-copy correction

- Copy: replaced the implementation-facing sentence about the manuscript following page scroll with product-facing language about keeping sources, evidence, and drafts in one research project.
- Boundary: the revised copy explicitly keeps research judgment and final responsibility with the researcher.
## 2026-08-11 - Proposal co-creation heading alignment

- Layout: moved the co-creation section title and supporting copy from the left split column into a centered full-width heading block.
- Scope: retained the three co-creation method rows, wording, dark section palette, and existing viewport entrance.
## 2026-08-11 - ProposalPilot G1-G6 synchronized handoff

- Change: removed cover-motion transforms from the G1-G6 sticky stack, moved the inter-sheet rhythm to adjacent `margin-top`, and gave desktop gate sheets one shared viewport-bounded height.
- Reason: the transformed stack and different card heights caused G1-G5 to hit the stack's bottom constraint before G6, so the following black section appeared while G6 was still pinned.
- Scope: desktop/compact sticky choreography only; mobile sheets remain in normal flow with natural height. Existing card content, stacking order, and following cover remain intact.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed. Playwright sampling at `1820x797` and `1097x525` reports equal sheet heights and a `0px` release spread across all six gates; mobile `244x720` remains natural-flow without horizontal overflow.

## 2026-08-11 - About and FAQ homepage-style visual rebase

- Visual system: rebuilt `/about` and `/faq` on the homepage monochrome paper palette using `#f5f5f2`, `#0a0a0a`, neutral rules, and the single fluorescent yellow `#d7ff00` accent; removed the visible legacy blue treatment from both pages.
- About: retained the existing company facts and information architecture while introducing the homepage cover transitions, a restrained source-to-delivery signal animation, alternating light/dark narrative bands, highlighted product rows, and responsive headline line control.
- FAQ: changed the page to a dark hero followed by an upward-covering light FAQ surface, rebuilt the sticky category index as a dark navigation module, and added fluorescent open/focus states with an upward answer reveal.
- Accessibility and scope: preserved `/about` and `/faq`, metadata, canonical URLs, Organization/FAQ schema, product-stage wording, keyboard focus, and reduced-motion fallbacks. Changed the nested FAQ `main` to a semantic section because the shared layout already owns the document `main`.
- QA: added `scripts/qa-about-faq-phase.cjs`; Playwright passed at `1440x900` and `390x844` for both pages with no horizontal overflow or console errors. `/about` and `/faq` returned `200`, `npx.cmd tsc --noEmit` passed, and patch whitespace validation passed.
- Build note: `npm.cmd run check` reached the Next production build but remote `next/font` retrieval failed with sandbox network `EACCES`; the escalation reviewer then returned `503`. The standalone token audit passed `8/9`, with only the built-CSS assertion unavailable because `.next` had been cleaned before the blocked font fetch.
## 2026-08-11 - Proposal problem-section readability

- Copy: replaced the section heading with a direct summary that project application work is difficult because the complete process must be managed continuously, not because one document must be written.
- Readability: increased the supporting and row-description type sizes, changed muted gray body copy to near-white, and widened the text column for faster scanning on the dark surface.
- Verification: `npx.cmd tsc --noEmit` passed; desktop and mobile browser checks confirmed `18px` intro copy, `16px` row copy, near-white text, and no horizontal overflow.
## 2026-08-11 - ScholarPilot roadmap heading wrapping

- Typography: removed the forced line break from "完整的科研工作，分阶段走近" and widened this roadmap heading to the full section measure.
- Responsive: the title remains on one line above `900px` and returns to natural wrapping on tablet and mobile viewports.
- Scope: no roadmap copy, cards, section motion, or capability boundaries changed.
- Verification: added `scripts/qa-scholar-roadmap-heading.cjs`; Playwright confirmed one `76px` line at `1900x1080`, natural two-line wrapping at `390x844`, no horizontal overflow, and no console errors.

## 2026-08-11 - Homepage capability subtitle wrapping

- Typography: widened the company-capability description from `680px` to `1180px`, kept it on one line above `1100px`, and enabled balanced wrapping below that breakpoint.
- Reason: the previous measure left only "推进工作。" on a second line at wide desktop widths.
- Responsive: the heading width remains unchanged; only the supporting sentence gains a wider desktop measure while mobile continues to wrap naturally.
- Verification: Playwright measured a single `29px` line at `1900x1080`; `390x844` retained natural wrapping with no horizontal overflow or console errors.

## 2026-08-11 - Homepage Hero subtitle wrapping

- Typography: widened the Hero subtitle measure from `660px` to `980px` and enabled balanced wrapping.
- Reason: at wide desktop widths the previous narrow measure left only the final character and punctuation on a second line.
- Responsive: wide screens now keep the sentence on one line when space allows; narrower screens wrap into visually balanced lines without horizontal overflow.
- Verification: Playwright measured a single `33px` line at `1900x1080` and `1440x900`; `390x844` retained balanced wrapping with no document overflow or console errors.

## 2026-08-11 - Homepage Hero source-network rhythm

- Layout: removed the light Hero's viewport-height floor and the flexible row that created the large empty band, brought the product source network directly below the CTA, capped its desktop height at `280px`, and enlarged the two product nodes and central source by about eight percent.
- Motion: added a restrained vertical fluorescent signal that travels from the CTA area toward the central source; it uses transform and opacity only and is disabled under `prefers-reduced-motion`.
- Responsive: kept the existing pointer movement and product-card navigation unchanged, while tablet and mobile return to a normal vertical flow with the added desktop signal hidden.
- Intent: the product family now reads as part of the Hero instead of a detached lower block; on wide screens the white surface ends with the content instead of stretching toward the viewport bottom, and the following dark section becomes a substantial next-page cue.
- Verification: added `scripts/qa-home-hero-rhythm.cjs`; Playwright passed at `1900x1080`, `1440x900`, and `390x844`, including CTA/card separation, substantial next-section visibility on wide screens, horizontal overflow, console errors, and reduced-motion behavior. `npx.cmd tsc --noEmit`, `/` status `200`, and patch whitespace validation passed.
## 2026-08-11 - Homepage workflow project-switch choreography

- Motion: replaced the workflow mode's short hide-and-swap with a three-phase transition: outgoing summary leaves, the selected project copy commits, and the incoming summary enters with a longer eased movement.
- Synchronization: the lower stage story keeps its existing outgoing/incoming overlap and now follows the same project-switch phase instead of visually snapping against the track copy.
- Accessibility: the stage panel reports `aria-busy` during the transition and switches immediately without animation when reduced motion is preferred.
- Verification: `npx.cmd tsc --noEmit` passed. Added `scripts/qa-home-workflow-switch.cjs`; Playwright sampled the transition every `40ms` at `1440x900`, `1060x420`, and `390x844`, confirming the leaving/entering/idle sequence, no fully blank frame, no horizontal overflow, no console errors, and an immediate reduced-motion fallback.
- Timing refinement: extended the project switch from roughly `0.84s` to `1.3s`, with a `480ms` exit and `820ms` entrance. The stage summary and lower detail card now share the slower rhythm instead of completing ahead of one another.
- Synchronization refinement: committed the stage copy and detail-card snapshot in the same React update, and made both surfaces leave together before that commit. This removes the perceptible second switch caused by the detail card updating one render after the track copy.
- Crossfade refinement: removed the incoming delay and retained a minimal `0.08` opacity floor at the handoff, preventing a brief fully blank frame while preserving clear outgoing and incoming direction.

## 2026-08-11 - Homepage product-matrix proximity border

- Interaction: changed each product card spotlight from a cursor-centered glow to a nearest-edge projection, so the border segment closest to the pointer remains visibly illuminated while the pointer moves through the card.
- Clarity: strengthened the matrix's resting border, enlarged the local fluorescent edge field, and added a restrained interior pointer glow without changing card content or layout.
- Accessibility: pointer effects remain decorative, keyboard focus retains its solid fluorescent inset outline, and reduced-motion mode continues to remove the animated overlays.
## 2026-08-11 - About page company narrative rebase

- Change: rebuilt `/about` as a static company narrative covering the company definition, working method, suitable work, current product directions, responsibility boundaries, and long-term expansion.
- Content: incorporated the user-provided non-final company reference only where it agrees with `AGENTS.md`; retained the confirmed brand entity `溥源科技 / PuYuan Tech`, the approved tagline, the enterprise government-project boundary for 智小申, and cautious 科小文 wording.
- Visual and motion: removed the page-local cover-scroll component, numbered decorative section headers, blue legacy treatment, and dense table-like information layouts. The page now uses one monochrome paper system with a single fluorescent signal accent and only link hover/focus feedback.
- Scope: changed `app/about/page.tsx` and added scoped `.about-story` rules in `app/globals.css`; navigation, homepage, and product-page behavior remain unchanged.
- Verification: `npx.cmd tsc --noEmit` passed, `git diff --check` passed, and `http://127.0.0.1:3000/about` returned `200`. Browser screenshot QA was not rerun because this workspace has no local Playwright package.
## 2026-08-11 - About page pure monochrome correction

- Change: changed `/about` to a white background with black text, including the navigation state, product rows, section surfaces, logo mark, and link feedback.
- Reason: the user requested a strictly white-and-black visual treatment for the company information page.
- Scope: scoped to `.about-story` and `body:has(.about-story)` selectors in `app/globals.css`; homepage and product pages retain their existing visual systems.
- Verification: `npx.cmd tsc --noEmit` passed, `git diff --check` passed, and `http://127.0.0.1:3000/about` returned `200`.
## 2026-08-11 - Homepage company subject moved forward

- Change: added a static company-introduction block immediately after the homepage Hero, explaining who PuYuan Tech is, what it builds, and how it works before the capability and workflow sections.
- Reason: the homepage previously led with an abstract brand statement and delayed the company identity, so visitors could not identify the main subject quickly.
- Visual: the new block uses a white background, black type, sparse rules, and no automatic motion or table layout.
- Scope: changed `app/page.tsx` and added scoped homepage rules in `app/globals.css`; product routes and the `/about` page remain unchanged.
- Verification: `npx.cmd tsc --noEmit` passed, `git diff --check` passed, `/` returned `200`, and the server-rendered HTML contains the company name and all three company-principle labels.
## 2026-08-12 - ScholarPilot connected capability cards

- Change: replaced the ScholarPilot hero's hub-and-node topology with three connected feature cards for `选题与文献综述`, `初稿生成`, and `润色与投稿准备`.
- Reference: used the supplied Buddy design notes and fanned-card screenshot for the white engineering-card surface, slight card rotation, continuous multicolor process stroke, and restrained elevation.
- Boundary: every card maps to an already confirmed ScholarPilot capability; no new research, analysis, funding-application, or publication claims were added. ProposalPilot keeps its existing topology visual.
- Responsive behavior: desktop cards form a lightly fanned horizontal sequence; mobile changes to an unrotated vertical chain with the connector running down the left side. Reduced-motion removes transforms and transitions.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `http://127.0.0.1:3000/scholarpilot` returned `200` and included the new capability-chain content.

## 2026-08-12 - ScholarPilot feature-chain connector clearance

- Layout: moved the ScholarPilot multicolor process stroke into its own top connector lane above the three capability cards and raised it above the card layer.
- Reason: the earlier center-aligned connector passed behind the white card surfaces and across the content area, making the process sequence appear visually obstructed.
- Responsive: the existing mobile left-side vertical connector remains unchanged.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `http://127.0.0.1:3000/scholarpilot` returned `200` and included the capability-chain content.

## 2026-08-12 - ScholarPilot curved through-card connector

- Correction: replaced the top connector lane with a shallow SVG wave that visibly crosses the surfaces of all three ScholarPilot capability cards.
- Layering: the curve renders above the white cards and ignores pointer input, so it remains continuous without interfering with card hover behavior.
- Responsive: narrow screens keep a separate vertical gradient connector at the left of the stacked cards so the path does not cross mobile copy.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/scholarpilot` returned `200` locally and its rendered markup includes the SVG gradient curve.

## 2026-08-12 - ScholarPilot connector text clearance

- Refinement: lifted the through-card curve and reduced its vertical amplitude so it travels through the upper visual area of each card rather than across the second card's heading and description.
- Intent: retain the requested continuous card-surface path while preserving the content hierarchy and legibility.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/scholarpilot` returned `200` locally with the revised SVG path.

## 2026-08-12 - ScholarPilot connector removal

- Change: removed the multicolor connector from the ScholarPilot capability cards on both desktop and mobile.
- Scope: retained the three cards, their layout, content, rotation, responsive stacking, and hover behavior.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/scholarpilot` returned `200` locally and no connector markup remains.

## 2026-08-12 - ScholarPilot hero summary navigation

- Content: changed the three ScholarPilot hero cards into concise summaries of the page's research workflow, currently demonstrable capabilities, and development/boundary section.
- Navigation: each full card is now a keyboard-accessible anchor linking to its matching section; added missing `#capabilities` and `#roadmap` targets with fixed-navigation scroll clearance, while reusing the existing `#applications` workflow target.
- Interaction: added a restrained directional arrow and preserved the existing card lift; corrected the desktop card rotation selectors after the connector removal so all three cards retain the intended fanned arrangement.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/scholarpilot` returned `200`, and its rendered HTML contains all three card links plus the `#applications`, `#capabilities`, and `#roadmap` targets.

## 2026-08-12 - ScholarPilot summary-card label cleanup

- Change: removed the small `01 / 02 / 03` labels from the ScholarPilot hero summary cards.
- Scope: retained each card's main heading, summary, icon, arrow, and section jump behavior.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/scholarpilot` returned `200` locally with no summary-card number-label markup.

## 2026-08-12 - ScholarPilot summary-card arrow cleanup

- Change: removed the directional arrow and its hover animation from the ScholarPilot hero summary cards.
- Scope: the full cards remain keyboard-accessible links with their existing lift, border, icon, heading, and summary states.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/scholarpilot` returned `200` locally, and the summary-card arrow markup is no longer present.

## 2026-08-12 - ProposalPilot topology core-name emphasis

- Change: increased and emboldened the central `智小申` name in the ProposalPilot topology from `11px / 600` to `18px / 700`, with responsive reductions at narrower widths.
- Reason: the center product identifier was visually undersized against the large processing hub and surrounding nodes.
- Scope: the hub size, connection paths, animation timing, and all other topology labels remain unchanged.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/proposalpilot` returned `200` locally.

## 2026-08-12 - About navigation monochrome correction

- Fix: removed the legacy blue navigation background, border glow, active-state fill, dropdown tint, and blue CTA from `/about`.
- Result: the About navigation now follows the page's confirmed white-and-black palette; hover states use neutral gray, and the CTA uses black with white text plus an inverted hover state.
- Cause: later global blue navigation declarations with `!important` were overriding the earlier About-specific palette.
- Scope: navigation color states only; dimensions, labels, routes, dropdown behavior, and About page content remain unchanged.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/about` returned `200` locally; the final About-specific selectors override the legacy blue shell, interaction, menu, and CTA declarations.

## 2026-08-12 - FAQ closing-layout correction

- Fix: reduced the oversized black closing panel on `/faq` from a `144px` rounded card to an `88px` compact navigation bar with an `8px` radius and a restrained border.
- Rhythm: shortened the gap after the final FAQ group so the About entry follows the last answer without a large empty interruption.
- Responsive: the closing entry remains stacked on mobile with tighter spacing and padding.
- Scope: FAQ content, accordion behavior, routes, and copy remain unchanged.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/faq` returned `200` locally; final desktop and mobile closing-layout rules resolve to the new compact dimensions.

## 2026-08-12 - ProposalPilot hero fact label cleanup

- Change: removed the compact hero fact label `不承诺包过` from the ProposalPilot overview strip.
- Reason: the label was too prominent in the visual summary and was explicitly requested for removal; the detailed capability-boundary copy and FAQ remain as the appropriate place for this qualification.
- Scope: changed only `components/ProductHero.tsx` fact data.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed.

## 2026-08-12 - Homepage company-subject visual alignment

- Change: aligned the company-subject section after the homepage hero with the homepage paper system: `#f5f5f2` surface, shared `1180px` content width, neutral fine rules, and the same near-black/muted text hierarchy.
- Reason: the newly added company introduction read as a separate About-page composition because its title scale, pure white background, and vertical spacing did not match the homepage hero or capability section.
- Scope: retained all company copy, the two-column structure, and the three work-principle items; reduced title, copy, divider, and responsive spacing only. No new cards or automatic motion were added.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed. The local route check could not run because no server was listening on port `3000`; the required dev-server approval failed when the approval service returned `503`.
## 2026-08-12 - Product matrix heading clarity

- Change: revised the homepage product-matrix heading from "产品矩阵，进入专业流程" to "产品矩阵，面向真实专业任务".
- Reason: the previous phrasing was abstract; the new heading states that the agent family is built for concrete professional work.
- Verification: copy-only change; the local development server is running at `http://127.0.0.1:3000/`.
## 2026-08-12 - Homepage company and capability consolidation

- Change: merged the adjacent company-introduction and professional-capability pages into one homepage section. The company definition remains on the light paper surface, while the capability content is now an inset dark panel within the same section.
- Content: removed the duplicated three-item company-principle row and retained the fuller capability explanations once. Revised the capability heading to "把专业知识组织成可推进的工作" to avoid repeating the company statement above it.
- Motion: removed the capability panel from the full-page cover animation while retaining its internal entrance motion and dark navigation theme state.
- Reason: both previous pages explained the same real-scenario, professional-process, human-decision idea, creating repetition and an unnecessary second page transition.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; the local homepage returned `200`.
## 2026-08-12 - Consolidated capability heading alignment

- Change: explicitly centered the consolidated capability panel heading at both container and heading levels.
- Reason: a later responsive homepage title rule could leave the heading visually left-aligned despite the panel's centered subtitle.
- Scope: limited to the capability panel nested inside the homepage company section.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; the local homepage returned `200`.
## 2026-08-12 - Capability heading overflow correction

- Change: replaced the consolidated capability heading's inherited single-line layout with a centered flex column and a balanced `1080px` heading measure.
- Reason: the heading text exceeded its former title container, so it overflowed from the left edge and appeared left-aligned even though `text-align: center` was present.
- Responsive behavior: the heading remains centered on one line when space permits and wraps symmetrically when it does not.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; the local homepage returned `200`.
## 2026-08-12 - ProposalPilot FAQ entrance and open state

- Motion: added each ProposalPilot FAQ question to the route-level layer entrance sequence, with an `85ms` stagger and side-to-center fade matching the homepage FAQ rhythm.
- Interaction: removed background fills from FAQ rows, summaries, answers, hover states, and expanded states. Expansion now communicates state through answer height, divider emphasis, and plus-icon rotation only.
- Scope: changes are limited to the ProposalPilot FAQ through the new `.proposal-faq` class; homepage and ScholarPilot accordions are unchanged.
- Accessibility: reduced-motion users continue to receive the static layout through the existing site entrance fallback.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/proposalpilot` returned `200` locally.
## 2026-08-12 - Cross-product FAQ visual alignment

- Homepage: removed the pale fluorescent fill from expanded FAQ rows; open questions now remain on the page's white surface while retaining divider and icon state changes.
- ProposalPilot: changed expanded answer copy from muted grey to white for stronger contrast on the black FAQ surface.
- ScholarPilot: added staggered side entrance to individual FAQ questions, removed hover and expanded background fills, and changed answer copy from grey to near-black on the white surface.
- Scope: no FAQ wording or structured data changed.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/`, `/proposalpilot`, and `/scholarpilot` each returned `200` locally.
## 2026-08-12 - Homepage FAQ answer contrast

- Change: changed homepage FAQ answer copy from muted grey to the homepage near-black ink color.
- Scope: limited to `.phase-two-faq`; ProposalPilot and ScholarPilot retain their route-specific answer colors.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; the homepage returned `200` locally.
## 2026-08-12 - Homepage FAQ open icon color

- Change: kept the homepage FAQ plus icon's original black color after it rotates into the close state.
- Scope: only the homepage FAQ icon color changed; its rotation and answer animation remain intact.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed.
## 2026-08-12 - ScholarPilot capability card content lift

- Change: removed the visible `01 / 02 / 03` numbering from the three ScholarPilot capability cards.
- Layout: reduced the status label's top margin to move the label, heading, and description upward as one content group.
- Scope: card dimensions, content wording, and three-column layout remain unchanged.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/scholarpilot` returned `200` locally.
## 2026-08-12 - Product-page navigation CTA

- Change: changed the navigation CTA from "选择产品" to "申请内测" on `/proposalpilot` and `/scholarpilot`, including the mobile menu CTA.
- Navigation: product-page CTAs now target a shared `#early-access` anchor on each page; the homepage and other routes continue to use "选择产品" and link to the homepage product matrix.
- Scope: no product submenu labels or application form destinations changed.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; both product routes returned `200` locally.
## 2026-08-12 - ProposalPilot early-access CTA color

- Change: replaced the legacy blue application button in the ProposalPilot closing section with the site's fluorescent yellow signal color, black text, and a restrained yellow hover glow.
- Copy: aligned the button label with the navigation CTA by changing it to "申请内测".
- Reason: a broad legacy `.proposal-page .bg-accent` rule was overriding the current monochrome product palette.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/proposalpilot` returned `200` locally.

## 2026-08-12 - About method heading alignment

- Layout: centered the “我们的工作方式” section heading and kept the desktop title on one line without changing the two-column method steps below it.
- Responsive: restored natural wrapping and left alignment below `800px` so the title remains readable on narrow screens.

## 2026-08-12 - About typography scale alignment

- Typography: aligned About section headings to a `34-52px` responsive scale, lead copy to `18px`, and kept supporting body copy at `16px`; the Hero display title remains unchanged.
- Reason: the previous section heading and lead scale made the “我们是谁” content visually heavier than the rest of the site.

## 2026-08-12 - About product-section contrast fix

- Fix: removed the remaining dark-section text colors from the About product section after the page was converted to a white monochrome surface.
- Result: the section heading, kicker, product names, English labels, descriptions, borders, and hover states now consistently render as black on white.
## 2026-08-12 - ProposalPilot early-access motion feedback

- Entrance: added a staged reveal for the closing icon, heading, description, application button, and return link through the existing route-level IntersectionObserver.
- Ambient motion: added a restrained low-frequency pulse to the confirmation icon to keep the final conversion area alive while idle.
- Interaction: the fluorescent application button now lifts slightly on hover, carries a single light sweep, increases its glow, and compresses on press.
- Accessibility: all continuous and transition motion is disabled under `prefers-reduced-motion`.
- Scope: structure, wording, mail destination, and navigation anchor remain unchanged.
- Verification: `npx.cmd tsc --noEmit` and `git diff --check` passed; `/proposalpilot` returned `200` locally.
## 2026-08-12 - ScholarPilot use-case card pointer motion

- Motion: changed the two white ScholarPilot use-case cards from static panels into pointer-responsive cards with damped 3D tilt, restrained content parallax, and a soft fluorescent-yellow cursor light.
- Return behavior: cards ease back to their neutral position through a continuous requestAnimationFrame interpolation instead of snapping when the pointer leaves.
- Scope: the interaction is isolated in `ScholarUseCaseCards`; roadmap content, layout, wording, and surrounding cards are unchanged.
- Accessibility: motion runs only for fine hover pointers and is fully disabled under `prefers-reduced-motion`.

## 2026-08-12 - PuYuan logo V2 concept assets

- Assets: created a refined primary mark, navigation lockup, and dedicated favicon under `assets/logo-v2/`, based on the supplied fluorescent-yellow revision of the original water-source logo.
- Geometry: retained the upward source silhouette, central droplet, two ripple lines, and four digital nodes; removed gradients, the heavy internal S-shaped cut, excess ripple layers, and decorative shadows.
- Palette: reduced fluorescent yellow to the central source accent while moving the main structure to the site's off-white and near-black system.
- Preview: added `public/logo-concepts/puyuan-logo-v2-board.svg` plus rendered PNG application and favicon-size boards for review. Existing live-site logo files were intentionally not replaced before approval.
- Verification: rendered the application board in Chromium and tested the dedicated favicon at `16px`, `24px`, `32px`, `64px`, and `128px`; the source silhouette and fluorescent center remain distinguishable at the smallest size. Script syntax and patch whitespace checks passed.

## 2026-08-12 - Original-logo color study

- Direction: returned to the supplied original water-source silhouette instead of adopting the V2 geometric enclosure.
- Color: changed only the visual system to near-black, off-white, and fluorescent yellow while retaining the droplet, flow lines, ripples, and four digital nodes.
- Deliverables: added dark and light SVG variants plus a complete presentation-board SVG/PNG under `assets/logo-v3/` and `public/logo-concepts/`.

## 2026-08-12 - Original logo color-only application

- Asset: regenerated `public/logo-concepts/logo-v2.png` directly from the uploaded original PNG rather than from a redrawn SVG.
- Geometry: preserved the original raster silhouette, internal flow shapes, ripple count, pixel-node positions, proportions, and spacing without adding, removing, or redrawing paths.
- Palette: changed only the detected original blue/cyan pixels: the isolated central droplet is fluorescent yellow, all other original logo pixels are off-white, and the application background is near-black.
- Scope: this remains a review asset; the live site logo and shared components are unchanged.
- Verification: confirmed `2048x1152` output dimensions and visually compared the result against the uploaded reference for unchanged geometry.

## 2026-08-12 - Original logo gradient color pass

- Palette: retained the color-only treatment while replacing flat white with an off-white-to-silver gradient and the flat droplet with a fluorescent yellow-to-chartreuse gradient.
- Background: added a restrained near-black radial lift behind the unchanged mark to improve separation without introducing a visible decorative shape.
- Geometry: the source crop, silhouette, internal flow, ripple layers, and pixel-node positions remain unchanged from the uploaded raster reference.
- Verification: regenerated the `2048x1152` PNG and visually checked that the gradient does not alter the original structure.

## 2026-08-12 - Original logo reverse-color treatment

- Reference: matched the supplied concept board's bottom-right reverse application: a dark field with the complete logo rendered as one fluorescent yellow-green system.
- Palette: removed the white-versus-yellow split; the outer form, internal flow, droplet, ripple layers, and pixel nodes now share a restrained pale-lime-to-fluorescent-lime gradient.
- Geometry: continued to recolor the uploaded raster directly, with no structural redraw, path removal, or proportion changes.
- Verification: regenerated the `2048x1152` PNG and visually checked the reverse treatment against the supplied reference.

## 2026-08-12 - Logo concept board website-gradient alignment

- Asset: recolored and repaired the supplied Logo concept board as `output/imagegen/puyuan-logo-board-website-gradient-v2.png` without changing the core source-and-ripple mark.
- Palette: aligned the board with the current site using warm paper `#F5F5F2`, graphite `#171717`, fluorescent signal `#D7FF00`, and pale-lemon `#EFFFA0`; the primary mark now transitions from pale lemon to fluorescent yellow-green instead of using the legacy blue/teal colors.
- Typography: replaced the unreadable raster copy with the confirmed Chinese company, concept, interpretation, and application text; corrected the company name, visual-language wording, and `AI Agent` label.
- Applications: rebuilt the three right-side icon states from a clean central-mark mask, removing the source tile fringe and duplicated outlines; gradients remain limited to the mark, palette strip, and application icons.
- Verification: inspected the final `4096x3058` PNG at original resolution and confirmed no square color fringe, duplicated icon outline, missing section label, or blue/teal remnant remains.

## 2026-08-12 - Official reverse logo applied

- Brand: approved the reverse application as the live Logo treatment: graphite rounded tile with the pale-lemon to fluorescent yellow-green source mark and the `PRYOS AI` wordmark.
- Shared placements: added the mark to the navigation and footer, updated the About hero mark, and set the same PNG as the Next.js favicon and Organization schema logo.
- Compatibility: kept the existing `logo-mark.svg` mask used by the homepage source interaction unchanged; no page structure or animation behavior was altered.
- Verification: generated and visually inspected `public/brand-logo-mark.png` at `512x512`, ran TypeScript/build checks, and confirmed the new assets are referenced without legacy hue filters.
## 2026-08-13 - Homepage source node logo update

- Homepage: replaced the legacy masked mark inside the central source node with the approved `brand-logo-mark.png` asset.
- Layout: enlarged and vertically balanced the new mark inside the circular node, with responsive sizing to keep the `溥源` label clear on desktop and mobile.
- Scope: changed only the homepage source-network center node; navigation, footer, favicon, and interaction behavior remain unchanged.
## 2026-08-13 - Transparent navigation logo

- Navigation: removed the graphite tile behind the approved fluorescent mark and switched the header brand lockup to a transparent-background asset.
- Scope: only the top navigation uses the transparent mark; footer, favicon, About page, and homepage source node remain unchanged.
- Cache fix: moved the transparent asset to a new versioned URL so the development browser cannot reuse the previous black-tile image.
## 2026-08-13 - High-contrast navigation logo

- Legibility: introduced a graphite positive version of the approved mark for the white navigation surface, replacing the low-contrast fluorescent-only default.
- Interaction: the fluorescent mark fades in on brand hover and keyboard focus; dark-page placements keep their existing reverse treatment.
## 2026-08-13 - Adaptive navigation logo contrast

- Navigation: connected the dual logo treatment to the existing homepage, ProposalPilot, and ScholarPilot dark navigation states.
- Contrast: dark navigation surfaces now show the fluorescent mark by default; light navigation surfaces show graphite, with the alternate color retained as hover and focus feedback.
