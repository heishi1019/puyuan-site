# DESIGN.md — 溥源科技品牌契约

> 这是 open-design 生成一切产物(logo、官网、文案、PPT)时必须遵守的品牌契约。
> 所有 artifact 的视觉、语气、结构都从这里长出来。

---

## 1. 公司 / Company

- **全称**：杭州溥源智能科技有限公司
- **简称**：溥源科技
- **英文**：PuYuan Tech（可用 PUYUAN 作字标）
- **品牌 tagline**：**从知识源头，驱动专业 agent**（已确认，2026-08-05）
  - 刻意不枚举产品、不锁数量 —— 未来新增 agent 时无需改动。
  - 机器可读层（`metadata` / Schema.org / `llms.txt`）另用信息更全的事实句，见 `lib/schema.ts`。
  - ⚠️ 旧稿「面向**科研人群**的 AI-native 公司」**已废弃** —— 无法覆盖面向企业的智小申。
- **命名含义**：「溥源」取「广博之源」——知识与创造之源。视觉与文案可围绕「源点扩散 / 一源驱动多 agent」展开。

## 2. 产品矩阵 / Products

| 产品 | 英文 | 定位 | 解决什么 | 状态 |
|---|---|---|---|---|
| **智小申** | **ProposalPilot Agent** | **企业政府项目申报** agent | 高企认定、研发费用加计扣除、专项资金等**政府项目**申报全流程 | **主推**，内容做深 |
| **科小文** | **ScholarPilot Agent** | 学术写作 agent | 论文选题、文献综述、初稿、润色、投稿准备的全流程副驾 | 轻量占位，暂缓 |

> **英文名含 `Agent` 后缀**（用户 2026-08-06 确认），构成 `*Pilot Agent` 家族命名。`Agent` 是名称的一部分，字标、页面标题、Schema.org 均需带上。**路由例外**：`/proposalpilot`、`/scholarpilot` 不含后缀。

> ⚠️ **旧稿错误已纠正**：此前把智小申写成「国自然 / 课题 / 基金申报书」，**是错的**。智小申做的是**政府项目申报**（面向企业，六大主管部门、三个层级、150+ 项目类型），**不是**学术基金申报。产品事实一律以 `_prd_zhishen.md` v3.4 为准。
>
> 对外品牌名统一「**智小申**」（PRD 内部写「智申」，勿带进对外文案）。

- 两个 agent 是同一「源」驱动的兄弟产品，视觉上应有家族一致性（同一 logo 语言、同色系、可用不同强调色微区分）。
- **产品矩阵会持续扩展** —— 视觉与文案系统需容纳未来新增 agent，不做「恰好两个」的硬编码。

## 3. 受众 / Audience

**站点为多页结构，受众按页分层，不要在同一屏里同时讨好所有人。**

| 页面 | 受众 | 语气侧重 |
|---|---|---|
| 公司主页 `/` | 伞级：企业决策者 **或** 科研人员，身份不预设 | 务实可信，一眼看懂「溥源是谁、有哪些 agent」，靠产品矩阵分流 |
| 智小申 `/proposalpilot` | **企业**：中小企业主、行政/财务兼职申报人、大企业政府事务专员、第三方申报代理 | 政策匹配、证据核查、数据主权、诚实边界（不承诺包过） |
| 科小文 `/scholarpilot` | **科研个人**（研究生/博士/青年教师）与科研机构/实验室 | 个人侧亲和、省时省力；机构侧私有化、数据安全、批量席位 |

> ⚠️ **旧稿错误已纠正**：此前只写了「个人科研者 / 科研机构」两类，**漏掉了智小申的企业受众** —— 而智小申是主推产品。伞级定位必须同时覆盖企业与科研。

信息密度：主页首屏保持伞级、低门槛；深度信息（安全、部署、能力边界）下沉到各产品页。

## 4. 语气 / Voice & Tone

- 中文为主，专业但不端着；避免「赋能」「颠覆」「革命性 AI」这类空话。
- **说具体的事**：不说「AI 驱动的科研平台」，说「帮你把 30 篇文献读成一段可引用的综述」。
- 可信 > 炫技。给数字、给流程、给边界（诚实说明 AI 能做和不能做的）。

## 5. 视觉方向 / Visual Direction —— 极客科技 · 克制

| 令牌 | 值 | 用途 |
|---|---|---|
| `--bg` | `#0A0A0B` | 主背景（近黑） |
| `--surface` | `#141416` | 卡片 / 分区背景 |
| `--border` | `#26262A` | 描边 / 分隔线 |
| `--text` | `#F5F5F7` | 主文字（近白） |
| `--text-muted` | `#8A8A93` | 次要文字 |
| `--accent` | `#00E5A0` | 强调 · 荧光青绿（CTA、logo、高亮） |
| `--accent-dim` | `#00B37E` | 强调色暗态（hover / 渐变收尾） |

> **令牌已落地到代码**：上表的值在 `tailwind.config.ts`（Tailwind utility）+ `app/globals.css`（CSS 变量）中各有一份，两处需保持同步。
> ⚠️ Tailwind v4 下 `globals.css` 必须保留 `@config "../tailwind.config.ts"`，否则整份 config 被静默忽略（详见 `AGENTS.md` §6「技术约定」）。

- **字体**：标题用几何无衬线（如 Inter / Geist / HarmonyOS Sans）；代码/数据/标签用等宽（如 JetBrains Mono / Geist Mono）。
- **气质**：开发者 · 硬核 · 未来感。大量留白，克制用色——荧光青绿只在关键处点睛，不要满屏。
- **元素语言**：源点 + 涟漪环 + 卫星节点（呼应「一源驱动多 agent」）；细描边、微光晕、等宽标签、终端/代码质感的点缀。
- **禁忌**：不用彩色渐变堆砌、不用拟物阴影、不用 emoji 当装饰、不用廉价 3D 机器人插画。

## 6. Logo

- 意象：**中心源点 + 外扩涟漪环 + 右上卫星节点**（在近黑背景上用 `--accent` 荧光青绿）。
- 需要三种形态：纯图标 mark（favicon / app icon）、图标 + 字标横版、字标单独版。
- 已有草稿：`assets/logo-mark.svg`、`assets/logo-full.svg`（可作为 open-design 迭代的起点，或推翻重做）。

## 7. 官网站点地图 / Sitemap

> ⚠️ **旧稿的「单页长滚动」方案已废弃**（用户 2026-08-05 明确要求多页）。
> **站点地图的权威定义在 `AGENTS.md` §5，本节不再重复维护。**

当前为**多页结构**，已用代码落地：

| 路由 | 页面 | 状态 |
|---|---|---|
| `/` | 公司主页（Hero → 产品矩阵 → 为什么选溥源 → FAQ → CTA） | ✅ 已交付 |
| `/proposalpilot` | 智小申产品页（内容做深） | ✅ 已交付 |
| `/scholarpilot` | 科小文占位页（轻量） | ✅ 已交付 |

多页对 GEO 是加分：每个 agent 独立 URL + 独立 `SoftwareApplication` 实体，比挤在单页里更容易被 AI 引擎识别为独立产品并引用。

## 8. 交付 / Handoff

- 官网最终落到 **Next.js + Tailwind**，部署 Vercel；open-design 出的 HTML/CSS 作为高保真原型和视觉来源。
- 所有产物需可导出为真实文件，便于交给编码 agent 继续开发。
